# MyCar+ V5.35 — Capacitor 7

## Comandos Windows
```powershell
npm.cmd install
npm.cmd run sync:web
npm.cmd run sync:android
npm.cmd run open:android
```

O diretório Web do Android é `www/`. Não altere manualmente `android/app/src/main/assets/public/`; use a sincronização do Capacitor.
