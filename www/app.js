const APP_NAME = "MyCar+",
  APP_VERSION = "5.92",
  REPORT_LOGO_DATA_URI = "data:image/png;base64,",
  APP_CREATED = "julho de 2026";
const $ = (s) => document.querySelector(s),
  $$ = (s) => [...document.querySelectorAll(s)];
const money = (n) =>
  new Intl.NumberFormat("pt-BR", { style: "currency", currency: "BRL" }).format(
    Number(n) || 0,
  );
const num = (n, d = 2) =>
  new Intl.NumberFormat("pt-BR", {
    minimumFractionDigits: d,
    maximumFractionDigits: d,
  }).format(Number(n) || 0);
const intFmt = (n) =>
  new Intl.NumberFormat("pt-BR", { maximumFractionDigits: 0 }).format(
    Number(n) || 0,
  );
function localDateISO(value = new Date()) {
  const date = value instanceof Date ? value : new Date(value);
  const pad = (number) => String(number).padStart(2, "0");
  return `${date.getFullYear()}-${pad(date.getMonth() + 1)}-${pad(date.getDate())}`;
}
function normalizeText(value) {
  return String(value || "").normalize("NFD").replace(/[\u0300-\u036f]/g, "").toLowerCase();
}
const GROUPS = ["COMBUSTÍVEL", "MANUTENÇÃO", "ADMINISTRATIVO", "RECEITA"];
const alpha = (list, field = "nome") =>
  [...list].sort((a, b) =>
    String(a?.[field] || "").localeCompare(String(b?.[field] || ""), "pt-BR", {
      sensitivity: "base",
      numeric: true,
    }),
  );
let movements = [],
  registers = [],
  drivers = [],
  vehicles = [],
  suppliers = [],
  paymentMethods = [],
  alerts = [];
let entryContext = null;
let entryReturnPage = "movimentos";
const NI = "NI — Não informado";
const defaults = [
  ["COMBUSTÍVEL", "Etanol", 1],
  ["COMBUSTÍVEL", "Gasolina", 0],
  ["COMBUSTÍVEL", "Diesel", 0],
  ["ADMINISTRATIVO", "Adesivos/Soleiras", 0],
  ["ADMINISTRATIVO", "Gorjeta", 0],
  ["ADMINISTRATIVO", "Impostos (IPVA/DPVAT)", 1],
  ["ADMINISTRATIVO", "Macaco", 0],
  ["ADMINISTRATIVO", "Multa", 0],
  ["ADMINISTRATIVO", "Protetor Solar/Parabrisa", 0],
  ["ADMINISTRATIVO", "Seguro", 0],
  ["RECEITA", "Reembolso", 1],
  ["MANUTENÇÃO", "Bateria", 0],
  ["MANUTENÇÃO", "Filtro de Ar", 0],
  ["MANUTENÇÃO", "Filtro de Ar da Cabine", 0],
  ["MANUTENÇÃO", "Filtro de Combustível", 0],
  ["MANUTENÇÃO", "Filtro de Óleo", 1],
  ["MANUTENÇÃO", "Fluido de Freio", 0],
  ["MANUTENÇÃO", "Fluido Radiador", 0],
  ["MANUTENÇÃO", "Lava-jato", 0],
  ["MANUTENÇÃO", "Mão de obra", 0],
  ["MANUTENÇÃO", "Pneus - Calibragem", 0],
  ["MANUTENÇÃO", "Troca de Freio", 0],
  ["MANUTENÇÃO", "Troca de Óleo", 0],
  ["MANUTENÇÃO", "Vidros/Espelhos", 0],
].map((x, i) => ({
  id: "r" + i,
  grupo: x[0],
  item: x[1],
  padrao: !!x[2],
}));
function showToast(message, tone = "success") {
  let host = document.getElementById("appToast");
  if (!host) {
    host = document.createElement("div");
    host.id = "appToast";
    host.className = "app-toast";
    host.setAttribute("role", "status");
    host.setAttribute("aria-live", "polite");
    document.body.appendChild(host);
  }
  clearTimeout(showToast.timer);
  host.textContent = message;
  host.dataset.tone = tone;
  host.classList.add("visible");
  showToast.timer = setTimeout(() => host.classList.remove("visible"), 3600);
}
function persistLocalState() {
  const entries = [
    ["mycar_movements_v1", movements],
    ["mycar_registers_v1", registers],
    ["mycar_drivers_v1", drivers],
    ["mycar_vehicles_v1", vehicles],
    ["mycar_suppliers_v1", suppliers],
    ["mycar_payment_methods_v1", paymentMethods],
    ["mycar_alerts_v1", alerts],
  ];
  const previous = new Map(entries.map(([key]) => [key, localStorage.getItem(key)]));
  try {
    entries.forEach(([key, value]) => localStorage.setItem(key, JSON.stringify(value)));
  } catch (error) {
    previous.forEach((value, key) => {
      if (value == null) localStorage.removeItem(key);
      else localStorage.setItem(key, value);
    });
    throw error;
  }
}
function save(syncCloud = true) {
  ensureAlertData();
  persistLocalState();
  let renderError = null;
  try {
    renderAll();
  } catch (error) {
    renderError = error;
    console.error("Os dados foram salvos, mas a atualização da tela falhou:", error);
  }
  let syncError = null;
  if (syncCloud) {
    try {
      window.cloudSync?.queueSave();
    } catch (error) {
      syncError = error;
      console.warn("Os dados foram salvos localmente, mas não entraram na fila de sincronização:", error);
    }
  }
  return { persisted: true, renderError, syncError };
}
function parseCSV(t) {
  const a = [];
  let r = [],
    f = "",
    q = false;
  for (let i = 0; i < t.length; i++) {
    const c = t[i],
      n = t[i + 1];
    if (c === '"') {
      if (q && n === '"') {
        f += '"';
        i++;
      } else q = !q;
    } else if (c === "," && !q) {
      r.push(f);
      f = "";
    } else if ((c === "\n" || c === "\r") && !q) {
      if (c === "\r" && n === "\n") i++;
      r.push(f);
      if (r.some(Boolean)) a.push(r);
      r = [];
      f = "";
    } else f += c;
  }
  return a;
}
function movementIdOf(movement) {
  return String(movement?.movimento_id ?? movement?.id ?? "");
}
function sameMovement(movement, id) {
  return movementIdOf(movement) === String(id ?? "");
}
function normalizeMovement(o, i = 0) {
  o.grupo = String(o.grupo || o.categoria || "").toUpperCase();
  if (o.grupo === "ADMINISTRATIVA") o.grupo = "ADMINISTRATIVO";
  if (!o.grupo) {
    o.grupo = o.tipo === "ABASTECIMENTO" ? "COMBUSTÍVEL" :
      o.tipo === "SERVICO" ? "MANUTENÇÃO" :
      o.tipo === "RECEITA" ? "RECEITA" : "ADMINISTRATIVO";
  }
  o.item = o.item || o.subcategoria || "";
  o.item_id = o.item_id || o.subcategoria_id || "";
  delete o.tipo;
  delete o.categoria;
  delete o.subcategoria;
  delete o.subcategoria_id;
  const fuel = normalizeText(o.item || "");
  if (fuel.includes("gasolina")) o.item = "Gasolina";
  else if (fuel.includes("etanol") || fuel.includes("alco" + "ol")) o.item = "Etanol";
  else if (fuel.includes("diesel")) o.item = "Diesel";
  o.id = String(o.id || "m" + i);
  o.movimento_id = String(o.movimento_id || o.id);
  o.ordem_lancamento = Number.isFinite(+o.ordem_lancamento)
    ? +o.ordem_lancamento
    : i + 1;
  o.valor = +o.valor || 0;
  o.hodometro_km = +o.hodometro_km || 0;
  o.quantidade_litros = +o.quantidade_litros || null;
  o.preco_unitario = +o.preco_unitario || null;
  o.distancia_km = +o.distancia_km || null;
  const rawVehicle = o.veiculo || o.veiculo_nome || "";
  const legacyVehicleId = { v1: "vei_001", v2: "vei_002" }[String(o.veiculo_id || "").trim()];
  const vehicleId = legacyVehicleId || String(o.veiculo_id || "").trim();
  const registered = vehicles.find((v) => v.id === vehicleId) ||
    vehicles.find((v) => normalizeText(v.nome) === normalizeText(rawVehicle));
  if (registered) {
    o.veiculo_id = registered.id;
    o.veiculo = registered.nome;
  } else {
    const fallback = selectedVehicleObject?.() || vehicles.find((v) => v.padrao) || vehicles[0];
    o.veiculo_id = vehicleId || fallback?.id || "";
    o.veiculo = rawVehicle || fallback?.nome || "N.I.";
  }
  o.motorista = o.motorista || drivers[0]?.nome || "N.I.";
  o.fornecedor = o.fornecedor || o.local || NI;
  o.local = o.fornecedor;
  return o;
}
function normalizeRegister(register = {}) {
  register.grupo = String(register.grupo || register.categoria || "").toUpperCase();
  if (register.grupo === "ADMINISTRATIVA") register.grupo = "ADMINISTRATIVO";
  register.item = register.item || register.subcategoria || register.nome || "";
  delete register.tipo;
  delete register.categoria;
  delete register.subcategoria;
  return register;
}
function enforceSingleDefaults() {
  GROUPS.forEach((group) => {
    const rows = registers.filter((r) => r.grupo === group && r.padrao);
    rows.slice(1).forEach((r) => (r.padrao = false));
  });
}
function enforceItemGroup(movement) {
  const registered = registers.find((r) =>
    (movement.item_id && r.id === movement.item_id) ||
    (!movement.item_id && r.item === movement.item),
  );
  if (registered) {
    movement.grupo = registered.grupo;
    movement.item = registered.item;
    movement.item_id = registered.id;
  }
  return movement;
}
function normalizeVehicle(v = {}) {
  const hasNewReferences = [
    "consumoEtanolCidade","consumoEtanolEstrada","consumoGasolinaCidade",
    "consumoGasolinaEstrada","consumoDieselCidade","consumoDieselEstrada"
  ].some((key) => v[key] !== undefined && v[key] !== "");
  if (!v.motorizacao) v.motorizacao = "FLEX";
  if (!hasNewReferences) {
    v.consumoGasolinaCidade = v.consumoRefCidade || "";
    v.consumoGasolinaEstrada = v.consumoRefEstrada || "";
  }
  return v;
}

function normalizeAlertRecord(item = {}) {
  const criterion = ["DATE", "KM", "BOTH"].includes(item.criterion)
    ? item.criterion
    : "DATE";
  const modelVersion = Number(item.modelVersion ?? item.modelo_versao ?? (item.manualSchedule ? 2 : 1)) || 1;
  const active = item.active !== false && item.statusMode !== "DISABLED";
  return {
    ...item,
    id: String(item.id || crypto.randomUUID()),
    modelVersion,
    vehicleId: String(item.vehicleId || item.veiculo_id || ""),
    group: "MANUTENÇÃO",
    itemId: String(item.itemId || item.item_id || ""),
    description: String(item.description || item.descricao || "").trim(),
    criterion,
    baseDate: String(item.baseDate || item.data_base || ""),
    baseKm: Number(item.baseKm ?? item.km_base ?? 0) || 0,
    recurrenceMonths: Number(item.recurrenceMonths ?? item.vida_util_meses ?? item.meses_recorrencia ?? 0) || 0,
    recurrenceKm: Number(item.recurrenceKm ?? item.vida_util_km ?? item.km_recorrencia ?? 0) || 0,
    dueDate: String(item.dueDate || item.data_prevista || ""),
    dueKm: Number(item.dueKm ?? item.km_previsto ?? 0) || 0,
    leadDays: Number(item.leadDays ?? item.antecedencia_dias ?? 0) || 0,
    leadKm: Number(item.leadKm ?? item.antecedencia_km ?? 0) || 0,
    observations: String(item.observations || item.observacao || "").slice(0, 300),
    statusMode: active ? "ACTIVE" : "DISABLED",
    active,
  };
}
function newestFirst(a, b) {
  const byDate = new Date(b.data_hora || 0) - new Date(a.data_hora || 0);
  return (
    byDate ||
    (+b.ordem_lancamento || 0) - (+a.ordem_lancamento || 0) ||
    String(b.id || "").localeCompare(String(a.id || ""))
  );
}
function movementKey(m) {
  return [
    m.veiculo || "",
    m.grupo || "",
    m.data_hora || "",
    Number(m.hodometro_km) || 0,
    Number(m.valor) || 0,
    m.item || "",
    m.origem || "",
  ].join("|");
}
async function load() {
  const migrationVersion = "mycarplus-v5-8-movimentos-multi-itens";
  const official = await MyCarPlusDB.load();
  const previousVersion = localStorage.getItem("mycar_data_migration");

  // Preserva o banco local e converte sua classificação para Grupo + Item.
  movements = JSON.parse(localStorage.getItem("mycar_movements_v1") || "null") || official.movements;
  registers = JSON.parse(localStorage.getItem("mycar_registers_v1") || "null") || official.registers;
  drivers = JSON.parse(localStorage.getItem("mycar_drivers_v1") || "null") || official.drivers;
  vehicles = JSON.parse(localStorage.getItem("mycar_vehicles_v1") || "null") || official.vehicles;
  suppliers = JSON.parse(localStorage.getItem("mycar_suppliers_v1") || "null") || official.suppliers;
  paymentMethods = JSON.parse(localStorage.getItem("mycar_payment_methods_v1") || "null") || official.paymentMethods;
  alerts = (JSON.parse(localStorage.getItem("mycar_alerts_v1") || "null") || official.alerts || [])
    .map(normalizeAlertRecord);
  localStorage.removeItem("mycar_alert_history_v1");
  localStorage.removeItem("mycar_technical_parameters_v1");

  vehicles = vehicles.map(normalizeVehicle);
  registers = registers.map(normalizeRegister);
  enforceSingleDefaults();
  movements = movements.map((m, i) => enforceItemGroup(normalizeMovement(m, i)));
  migrateToNewAlertModel();
  ensureAlertData();
  localStorage.setItem("mycar_data_migration", migrationVersion);
  recalculateDistances();
  save(false);
  console.info("Banco oficial MyCarPlus.xlsx carregado", {
    movimentos: movements.length,
    veiculos: vehicles.length,
    hb20: movements.filter(m => m.veiculo_id === "vei_001").length,
    sonata: movements.filter(m => m.veiculo_id === "vei_002").length
  });
}
function vehicleName(id) {
  return (
    vehicles.find((v) => v.id === id)?.nome ||
    id ||
    vehicles[0]?.nome ||
    "Sem veículo"
  );
}
function defaultVehicle() {
  return vehicles.find((v) => v.padrao && v.ativo !== false) || null;
}
function vehicleSummary(v) {
  const ms = movements.filter((m) => m.veiculo === v.nome),
    odos = ms.map((m) => +m.hodometro_km || 0).filter(Boolean),
    last = odos.length ? Math.max(...odos) : +v.kmInicial || 0,
    initial = +v.kmInicial || 0;
  return {
    initial,
    last,
    driven: Math.max(0, last - initial),
    stats: stats(ms),
  };
}
function recalculateDistances() {
  vehicles.forEach((v) => {
    const ms = movements
      .filter((m) => m.veiculo === v.nome && +m.hodometro_km > 0)
      .sort(
        (a, b) =>
          new Date(a.data_hora) - new Date(b.data_hora) ||
          +a.hodometro_km - +b.hodometro_km,
      );
    let prev = +v.kmInicial || 0;
    ms.forEach((m) => {
      const km = +m.hodometro_km || 0;
      m.distancia_km = Math.max(0, km - prev);
      prev = Math.max(prev, km);
    });
    const fuels = ms
      .filter((m) => m.grupo === "COMBUSTÍVEL")
      .sort(
        (a, b) =>
          new Date(a.data_hora) - new Date(b.data_hora) ||
          +a.hodometro_km - +b.hodometro_km,
      );
    let prevFuel = +v.kmInicial || 0;
    fuels.forEach((m) => {
      const km = +m.hodometro_km || 0,
        dist = Math.max(0, km - prevFuel);
      m.distancia_abastecimento_km = dist;
      m.consumo_km_l =
        m.tanque_completo !== "NAO" && +m.quantidade_litros > 0 && dist > 0
          ? dist / +m.quantidade_litros
          : null;
      prevFuel = Math.max(prevFuel, km);
    });
  });
}
function activeVehicle() {
  return (
    vehicles.find((v) => v.ativo !== false) ||
    defaultVehicle() ||
    vehicles[0] ||
    null
  );
}
function selectedVehicleName() {
  const saved = localStorage.getItem("mycar_selected_vehicle_v1") || "";
  if (vehicles.some((v) => v.nome === saved)) return saved;
  return defaultVehicle()?.nome || activeVehicle()?.nome || vehicles[0]?.nome || "";
}
function selectedVehicleObject() {
  const name = selectedVehicleName();
  return vehicles.find((v) => v.nome === name) || null;
}
function updateStaticVehicleSummary(nameId, statusId, vehicle) {
  const name = $("#" + nameId), status = $("#" + statusId);
  if (name) name.textContent = vehicle?.nome || "Nenhum veículo selecionado";
  if (status) {
    const inactive = vehicle?.ativo === false;
    status.textContent = vehicle ? (inactive ? "Inativo" : "Ativo") : "Não selecionado";
    status.className = `vehicle-status ${inactive || !vehicle ? "inactive" : "active"}`;
  }
}
function selectVehicle(name) {
  if (!vehicles.some((v) => v.nome === name)) return;
  localStorage.setItem("mycar_selected_vehicle_v1", name);
  if (document.getElementById("chartStart")) ensureChartPeriodDefaults(true);
  renderAll();
}
function fillVehicleSelects() {
  const active = activeVehicle(),
    selected = selectedVehicleName(),
    vehicleOpts = alpha(vehicles)
      .map(
        (v) =>
          `<option value="${v.nome}">${v.nome}${v.ativo === false ? " (Inativo)" : " (Ativo)"}</option>`,
      )
      .join(""),
    home = $("#homeVehicle");
  home.innerHTML = vehicleOpts || '<option value="">Nenhum veículo cadastrado</option>';
  home.value = selected;
  ["movementVehicle"].forEach((id) => {
    const e = $("#" + id),
      old = e.value;
    e.innerHTML = vehicleOpts;
    const valid = vehicles.some((v) => v.nome === selected);
    e.value = valid ? selected : active?.nome || vehicles[0]?.nome || "";
    e.disabled = true;
    e.setAttribute("aria-label", "Veículo selecionado na tela inicial");
  });
  ["reportVehicle", "chartVehicle"].forEach((id) => {
    const e = $("#" + id),
      old = selected;
    e.innerHTML = vehicleOpts;
    const valid = vehicles.some((v) => v.nome === old);
    e.value = valid ? old : active?.nome || vehicles[0]?.nome || "";
  });
  const selectedObject = vehicles.find((v) => v.nome === selected) || active || vehicles[0] || null;
  updateStaticVehicleSummary("reportVehicleName", "reportVehicleMeta", selectedObject);
  updateStaticVehicleSummary("chartVehicleName", "chartVehicleStatus", selectedObject);
}
function fillDrivers() {
  const e = $("#entryForm [name=motorista]");
  e.innerHTML =
    '<option value="">Não informado</option>' +
    alpha(drivers)
      .map(
        (d) =>
          `<option value="${d.nome}" ${d.padrao ? "selected" : ""}>${d.nome}</option>`,
      )
      .join("");
  syncEntrySelectTrigger(e);
}

function fillOperationalLists() {
  const f = $("#entryForm");
  if (f.fornecedor) {
    f.fornecedor.innerHTML = '<option value="">Não informado</option>' +
      alpha(suppliers.filter(x => x.ativo !== false)).map(x => `<option value="${x.nome}">${x.nome}${x.local ? " · " + x.local : ""}</option>`).join("");
    syncEntrySelectTrigger(f.fornecedor);
  }
  if (f.formaPagamento) {
    f.formaPagamento.innerHTML = '<option value="">Não informado</option>' +
      alpha(paymentMethods.filter(x => x.ativo !== false)).map(x => `<option value="${x.nome}" ${x.padrao ? "selected" : ""}>${x.nome}</option>`).join("");
    syncEntrySelectTrigger(f.formaPagamento);
  }
}
function gpsSupplierRadiusMeters() {
  const saved = Number(localStorage.getItem("mycar_gps_supplier_radius") || 150);
  return [50, 100, 150, 250, 500].includes(saved) ? saved : 150;
}
function validCoordinate(value) {
  return value !== "" && value != null && Number.isFinite(Number(value));
}
function distanceMeters(lat1, lon1, lat2, lon2) {
  const rad = (value) => Number(value) * Math.PI / 180;
  const dLat = rad(Number(lat2) - Number(lat1));
  const dLon = rad(Number(lon2) - Number(lon1));
  const a = Math.sin(dLat / 2) ** 2 +
    Math.cos(rad(lat1)) * Math.cos(rad(lat2)) * Math.sin(dLon / 2) ** 2;
  return 6371000 * 2 * Math.atan2(Math.sqrt(a), Math.sqrt(1 - a));
}
function nearestSupplier(latitude, longitude) {
  return suppliers
    .filter((supplier) => supplier.ativo !== false &&
      validCoordinate(supplier.latitude) && validCoordinate(supplier.longitude))
    .map((supplier) => ({
      supplier,
      distance: distanceMeters(latitude, longitude, supplier.latitude, supplier.longitude),
    }))
    .sort((a, b) => a.distance - b.distance)[0] || null;
}
function mapUrl(latitude, longitude) {
  return `https://www.google.com/maps?q=${encodeURIComponent(`${latitude},${longitude}`)}`;
}
function updateMapLink(link, latitude, longitude) {
  const available = validCoordinate(latitude) && validCoordinate(longitude);
  link.hidden = !available;
  if (available) link.href = mapUrl(latitude, longitude);
}
function addressFromGeocode(result) {
  const address = result?.address || {};
  const street = [address.road || address.pedestrian || address.residential, address.house_number]
    .filter(Boolean).join(", ");
  const district = address.suburb || address.neighbourhood || address.city_district;
  const city = address.city || address.town || address.municipality || address.village;
  return [street, district, city, address.state].filter(Boolean).join(" · ") ||
    result?.display_name || "";
}
function supplierFromGeocode(result) {
  const address = result?.address || {};
  const generic = new Set(["yes", "building", "commercial", "retail", "service"]);
  return [
    result?.name,
    address.amenity,
    address.shop,
    address.office,
    address.tourism,
    address.leisure,
  ].find((value) => value && !generic.has(String(value).toLowerCase())) || "";
}
async function reverseGeocode(latitude, longitude) {
  const url = new URL("https://nominatim.openstreetmap.org/reverse");
  url.searchParams.set("format", "jsonv2");
  url.searchParams.set("lat", Number(latitude).toFixed(7));
  url.searchParams.set("lon", Number(longitude).toFixed(7));
  url.searchParams.set("zoom", "18");
  url.searchParams.set("addressdetails", "1");
  url.searchParams.set("accept-language", "pt-BR");
  const response = await fetch(url, {
    headers: { Accept: "application/json" },
    referrerPolicy: "no-referrer",
  });
  if (!response.ok) throw new Error(`Geocodificação indisponível (${response.status})`);
  const result = await response.json();
  return {
    supplier: supplierFromGeocode(result),
    address: addressFromGeocode(result),
  };
}
function selectDetectedSupplier(select, supplierName) {
  if (!supplierName) return false;
  const normalized = supplierName.trim().toLocaleLowerCase("pt-BR");
  let option = [...select.options].find((item) =>
    item.value.trim().toLocaleLowerCase("pt-BR") === normalized);
  if (!option) {
    option = new Option(`${supplierName} · detectado pelo GPS`, supplierName);
    option.dataset.gpsDetected = "true";
    select.add(option);
  }
  select.value = option.value;
  return true;
}
function requestCurrentPosition(button, status, onSuccess) {
  if (!window.isSecureContext || !navigator.geolocation) {
    status.textContent = "GPS indisponível. Abra o app por HTTPS e autorize a localização.";
    return;
  }
  button.disabled = true;
  status.textContent = "Obtendo localização atual…";
  navigator.geolocation.getCurrentPosition(
    (position) => {
      button.disabled = false;
      onSuccess(position.coords);
    },
    (error) => {
      button.disabled = false;
      status.textContent = error.code === 1
        ? "Permissão de localização negada. Você pode continuar sem usar o GPS."
        : "Não foi possível obter a localização. Tente novamente em local aberto.";
    },
    { enableHighAccuracy: true, timeout: 15000, maximumAge: 30000 },
  );
}
function filtered(vehicle = "") {
  return vehicle ? movements.filter((m) => m.veiculo === vehicle) : movements;
}
function periodValues(prefix) {
  return {
    start: $("#" + prefix + "Start")?.value || "",
    end: $("#" + prefix + "End")?.value || "",
  };
}
function movementPeriodBounds(ms) {
  const dates = ms
    .map((movement) => String(movement.data_hora || "").slice(0, 10))
    .filter((date) => /^\d{4}-\d{2}-\d{2}$/.test(date))
    .sort();
  return dates.length ? { first: dates[0], last: dates.at(-1) } : { first: "", last: "" };
}
function formatPeriodDate(date) {
  return date ? new Date(date + "T12:00:00").toLocaleDateString("pt-BR") : "—";
}
function resolveEffectivePeriod(ms, requestedStart, requestedEnd) {
  const bounds = movementPeriodBounds(ms);
  if (!bounds.first || !bounds.last) {
    return { hasRecords: false, hasOverlap: false, wasAdjusted: false, ...bounds,
      requestedStart, requestedEnd, effectiveStart: "", effectiveEnd: "" };
  }
  const start = requestedStart || bounds.first;
  const end = requestedEnd || bounds.last;
  const hasOverlap = start <= bounds.last && end >= bounds.first;
  if (!hasOverlap) {
    return { hasRecords: true, hasOverlap: false, wasAdjusted: false, ...bounds,
      requestedStart, requestedEnd, effectiveStart: start, effectiveEnd: end };
  }
  const effectiveStart = start < bounds.first ? bounds.first : start;
  const effectiveEnd = end > bounds.last ? bounds.last : end;
  return {
    hasRecords: true,
    hasOverlap: true,
    wasAdjusted: effectiveStart !== requestedStart || effectiveEnd !== requestedEnd,
    ...bounds,
    requestedStart,
    requestedEnd,
    effectiveStart,
    effectiveEnd,
  };
}
function applyEffectivePeriodToFields(prefix, ms) {
  const { start, end } = periodValues(prefix);
  const info = resolveEffectivePeriod(ms, start, end);
  if (info.hasOverlap) {
    const startField = $("#" + prefix + "Start"), endField = $("#" + prefix + "End");
    if (startField && startField.value !== info.effectiveStart) startField.value = info.effectiveStart;
    if (endField && endField.value !== info.effectiveEnd) endField.value = info.effectiveEnd;
  }
  return info;
}
function effectivePeriodMessage(info) {
  if (!info.hasRecords) return "O veículo selecionado ainda não possui movimentações.";
  if (!info.hasOverlap) {
    const requestedEndBeforeHistory = info.requestedEnd && info.requestedEnd < info.first;
    return requestedEndBeforeHistory
      ? `Não existem movimentações nesse período. O histórico disponível começa em ${formatPeriodDate(info.first)}.`
      : `Não existem movimentações nesse período. O histórico disponível termina em ${formatPeriodDate(info.last)}.`;
  }
  return info.wasAdjusted
    ? `Período ajustado: ${formatPeriodDate(info.effectiveStart)} a ${formatPeriodDate(info.effectiveEnd)}.`
    : `Período utilizado: ${formatPeriodDate(info.effectiveStart)} a ${formatPeriodDate(info.effectiveEnd)}.`;
}
function periodIsValid(prefix) {
  const { start, end } = periodValues(prefix),
    error = $("#" + prefix + "PeriodError");
  if (start && end && end < start) {
    if (error) error.textContent = "A data final não pode ser anterior à data inicial.";
    return false;
  }
  if (error) error.textContent = "";
  return true;
}
function filterByPeriod(ms, prefix) {
  const { start, end } = periodValues(prefix);
  if (!periodIsValid(prefix)) {
    const empty = [];
    empty.periodFiltered = true;
    return empty;
  }
  const result = ms.filter((m) => {
    const date = String(m.data_hora || "").slice(0, 10);
    return date && (!start || date >= start) && (!end || date <= end);
  });
  result.periodFiltered = !!(start || end);
  result.periodStart = start;
  result.periodEnd = end;
  return result;
}
function periodText(prefix) {
  const { start, end } = periodValues(prefix),
    fmt = (date) => date ? new Date(date + "T12:00:00").toLocaleDateString("pt-BR") : "…";
  return start || end ? `${fmt(start)} a ${fmt(end)}` : "Geral";
}
function stats(ms) {
  const valid = ms.filter((m) => m.data_hora),
    cost = valid
      .filter((m) => m.grupo !== "RECEITA")
      .reduce((a, m) => a + (+m.valor || 0), 0),
    income = valid
      .filter((m) => m.grupo === "RECEITA")
      .reduce((a, m) => a + (+m.valor || 0), 0),
    names = [...new Set(valid.map((m) => m.veiculo).filter(Boolean))];
  let km = 0;
  if (ms.periodFiltered) {
    km = valid.reduce((total, m) => total + Math.max(0, +(m.distancia_km || 0)), 0);
  } else if (names.length > 1) {
    km = names.reduce(
      (sum, n) => sum + stats(valid.filter((m) => m.veiculo === n)).km,
      0,
    );
  } else {
    const name = names[0],
      vehicle = vehicles.find((v) => v.nome === name),
      odos = valid.map((m) => +m.hodometro_km || 0).filter((n) => n > 0),
      last = odos.length ? Math.max(...odos) : 0,
      initial = +vehicle?.kmInicial || (odos.length ? Math.min(...odos) : 0);
    km = Math.max(0, last - initial);
  }
  const dates = valid.map((m) => new Date(m.data_hora)),
    selectedDays = ms.periodStart && ms.periodEnd
      ? Math.floor(
          (Date.parse(ms.periodEnd + "T12:00:00") -
            Date.parse(ms.periodStart + "T12:00:00")) /
            86400000,
        ) + 1
      : 0,
    days = selectedDays > 0
      ? selectedDays
      : dates.length
        ? Math.max(1, (Math.max(...dates) - Math.min(...dates)) / 86400000)
        : 1,
    consumptionFuel = valid.filter((m) => m.grupo === "COMBUSTÍVEL" && m.tanque_completo !== "NAO"),
    liters = consumptionFuel
      .reduce((a, m) => a + (+m.quantidade_litros || 0), 0);
  const consumptionDistance = consumptionFuel.reduce((a, m) => a + (+m.distancia_abastecimento_km || 0), 0);
  return {
    cost,
    income,
    net: cost - income,
    km,
    days,
    cons: liters ? consumptionDistance / liters : 0,
  };
}
function icon(m) {
  return m.grupo === "COMBUSTÍVEL"
    ? "⛽"
    : m.grupo === "MANUTENÇÃO"
      ? "🔧"
      : m.grupo === "RECEITA"
        ? "↙"
        : "🧾";
}
function item(m, editable = false) {
  const rows = m._rows || [m],
    names = rows.map((row) => row.item).filter(Boolean),
    total = rows.reduce((sum, row) => sum + (+row.valor || 0), 0),
    movementId = movementIdOf(m);
  return `<article class="item ${m.grupo === "RECEITA" ? "income" : ""}"><div><b>${icon(m)} ${names[0] || m.grupo}${names.length > 1 ? `<span class="movement-item-count">${names.length} itens</span>` : ""}</b>${names.length > 1 ? `<small class="movement-item-names">${names.join(" · ")}</small>` : ""}<small>${new Date(m.data_hora).toLocaleDateString("pt-BR")} · ${m.grupo} · ${m.veiculo || "Sem veículo"}</small></div><div class="amount"><b>${m.grupo === "RECEITA" ? "+" : "-"} ${money(total)}</b><small>${intFmt(m.hodometro_km)} km</small>${editable ? `<div class="movement-actions"><button type="button" class="view-movement" data-view-movement="${esc(movementId)}">Consultar</button><button type="button" class="edit-movement" data-edit-movement="${esc(movementId)}">Alterar</button><button type="button" class="delete-movement" data-delete-movement="${esc(movementId)}">Excluir</button></div>` : ""}</div></article>`;
}
function groupedMovements(list) {
  const groups = new Map();
  list.forEach((row) => {
    const key = row.movimento_id || row.id;
    if (!groups.has(key)) groups.set(key, []);
    groups.get(key).push(row);
  });
  return [...groups.values()].map((rows) => ({ ...rows[0], _rows: rows }));
}
function insightTrendText(value, positiveIsGood = true) {
  if (!Number.isFinite(value) || Math.abs(value) < 0.05) return { text: "Estável", tone: "neutral", arrow: "→" };
  const improved = positiveIsGood ? value > 0 : value < 0;
  return {
    text: `${value > 0 ? "Subiu" : "Caiu"} ${num(Math.abs(value), 1)}%`,
    tone: improved ? "good" : "warn",
    arrow: value > 0 ? "↑" : "↓",
  };
}
function renderSmartDashboard(ms, s) {
  const ordered = ms.filter((m) => m.data_hora).sort((a, b) => new Date(a.data_hora) - new Date(b.data_hora));
  const midpoint = Math.floor(ordered.length / 2);
  const previous = stats(ordered.slice(0, midpoint));
  const recent = stats(ordered.slice(midpoint));
  const previousCostKm = previous.km ? previous.net / previous.km : 0;
  const recentCostKm = recent.km ? recent.net / recent.km : 0;
  const costDelta = previousCostKm && recentCostKm ? (recentCostKm / previousCostKm - 1) * 100 : NaN;
  const consDelta = previous.cons && recent.cons ? (recent.cons / previous.cons - 1) * 100 : NaN;
  const costTrend = insightTrendText(costDelta, false);
  const consTrend = insightTrendText(consDelta, true);

  const expenses = ordered.filter((m) => m.grupo !== "RECEITA");
  const categoryTotals = expenses.reduce((acc, m) => {
    const key = m.grupo || "Outros";
    acc[key] = (acc[key] || 0) + (+m.valor || 0);
    return acc;
  }, {});
  const categories = Object.entries(categoryTotals).sort((a, b) => b[1] - a[1]);
  const latestFuel = [...ordered].filter((m) => m.grupo === "COMBUSTÍVEL").sort(newestFirst)[0];
  const latestFuelDays = latestFuel ? Math.max(0, Math.floor((Date.now() - new Date(latestFuel.data_hora)) / 86400000)) : null;
  const fuelCount = ordered.filter((m) => m.grupo === "COMBUSTÍVEL").length;
  const latestConsumptionFuel = [...ordered]
    .filter((m) => {
      if (m.grupo !== "COMBUSTÍVEL" || m.tanque_completo === "NAO") return false;
      const direct = +m.consumo_km_l || 0;
      const calculated = (+m.quantidade_litros || 0) > 0 && (+m.distancia_abastecimento_km || 0) > 0;
      return direct > 0 || calculated;
    })
    .sort(newestFirst)[0];
  const latestConsumption = latestConsumptionFuel
    ? (+latestConsumptionFuel.consumo_km_l || ((+latestConsumptionFuel.distancia_abastecimento_km || 0) / (+latestConsumptionFuel.quantidade_litros || 1)))
    : 0;

  const compositionGroups = [
    { key: "COMBUSTÍVEL", label: "Abastecimento", color: "#2ca9ff" },
    { key: "ADMINISTRATIVO", label: "Administrativo", color: "#8e6ee8" },
    { key: "MANUTENÇÃO", label: "Manutenção", color: "#ff9f43" },
  ];
  const totalExpenses = compositionGroups.reduce((sum, group) => sum + (categoryTotals[group.key] || 0), 0);
  const totalIncome = ordered
    .filter((movement) => movement.grupo === "RECEITA")
    .reduce((sum, movement) => sum + (+movement.valor || 0), 0);
  const netCost = totalExpenses - totalIncome;
  const shares = compositionGroups
    .map((group) => ({
      ...group,
      value: categoryTotals[group.key] || 0,
      pct: totalExpenses > 0 ? ((categoryTotals[group.key] || 0) / totalExpenses) * 100 : 0,
      deduction: false,
    }))
    .filter((group) => group.value > 0);
  if (totalIncome > 0) {
    shares.push({
      key: "RECEITA",
      label: "Receitas",
      color: "#28b779",
      value: totalIncome,
      pct: totalExpenses > 0 ? (totalIncome / totalExpenses) * 100 : 100,
      deduction: true,
    });
  }

  let score = 70;
  if (Number.isFinite(consDelta)) score += Math.max(-10, Math.min(10, consDelta * 1.5));
  if (Number.isFinite(costDelta)) score += Math.max(-12, Math.min(12, -costDelta));
  if (ordered.length >= 10) score += 5;
  if (fuelCount >= 3) score += 5;
  if (s.km > 0 && s.net >= 0) score += 5;
  score = Math.max(0, Math.min(100, Math.round(score)));
  const classification = score >= 90 ? "Excelente" : score >= 80 ? "Muito bom" : score >= 70 ? "Bom" : score >= 60 ? "Atenção" : "Crítico";
  const scoreTone = score >= 80 ? "good" : score >= 60 ? "neutral" : "warn";

  const bars = shares.length
    ? shares.map((x) => {
      const valueText = x.deduction ? `− ${money(x.value)}` : money(x.value);
      const pctText = `${num(x.pct, 1)}%`;
      return `<div class="insight-bar-row composition-row ${x.deduction ? "composition-deduction" : ""}"><div><span>${x.label}</span><strong>${valueText} · ${pctText}</strong></div><div class="insight-track"><i style="width:${Math.min(100, Math.max(0, x.pct))}%;--composition-color:${x.color}"></i></div></div>`;
    }).join("")
    : '<p class="insight-empty composition-empty">Nenhum lançamento financeiro registrado no período.</p>';
  const monthlyCost = s.days > 0 ? (s.net / s.days) * 30.44 : 0;

  return `<article class="smart-dashboard smart-dashboard-compact">
    <div class="smart-dashboard-head"><strong>Painel inteligente</strong><span class="insight-badge ${scoreTone}">${classification}</span></div>
    <div class="insight-metrics insight-metrics-six">
      <div><small>Custo por km</small><b>${s.km ? money(s.net / s.km) : "—"}</b></div>
      <div><small>Custo por dia</small><b>${money(s.net / s.days)}</b></div>
      <div><small>Custo mensal</small><b>${money(monthlyCost)}</b></div>
      <div><small>Custo total líquido</small><b>${money(s.net)}</b></div>
      <div><small>Consumo médio</small><b>${s.cons ? `${num(s.cons)} km/L` : "—"}</b></div>
      <div><small>Último consumo</small><b>${latestConsumption > 0 ? `${num(latestConsumption)} km/L` : "—"}</b></div>
    </div>
    <section class="insight-trends-primary"><h3>Tendências</h3>
      <p class="insight-status ${consTrend.tone}"><span>${consTrend.arrow}</span><b>Consumo</b><em>${Number.isFinite(consDelta) ? consTrend.text : "Histórico insuficiente"}</em></p>
      <p class="insight-status ${costTrend.tone}"><span>${costTrend.arrow}</span><b>Custo por km</b><em>${Number.isFinite(costDelta) ? costTrend.text : "Histórico insuficiente"}</em></p>
      <p class="insight-status neutral"><span>↔</span><b>Distância</b><em>${intFmt(s.km)} km no período</em></p>
    </section>
    <div class="insight-grid insight-grid-secondary insight-grid-score-first">
      <div class="score-box insight-score-card"><div><small>MyCar Score</small><strong>${score}<span>/100</span></strong><p>Índice gerencial baseado na evolução do consumo, custo por km e qualidade do histórico.</p></div><div class="score-ring" style="--score:${score}"><span>${score}</span></div></div>
      <section><h3>Utilização</h3>
        <dl class="insight-list"><div><dt>Média diária</dt><dd>${num(s.km / s.days)} km/dia</dd></div>
        <div><dt>Abastecimentos</dt><dd>${fuelCount}</dd></div>
        <div><dt>Último abastecimento</dt><dd>${latestFuelDays == null ? "Sem registro" : latestFuelDays === 0 ? "Hoje" : `Há ${latestFuelDays} dia(s)`}</dd></div>
        <div><dt>Lançamentos analisados</dt><dd>${ordered.length}</dd></div></dl>
      </section>
    </div>
    <div class="insight-grid insight-grid-composition-wide">
      <section class="composition-cost-card">
        <header class="composition-card-head"><h3>Composição dos Grupos</h3></header>
        <div class="composition-card-body">${bars}</div>
        ${shares.length ? `<footer class="composition-card-total"><span>Custo líquido</span><strong>${money(netCost)}</strong></footer>` : ""}
      </section>
    </div>
  </article>`;
}
function renderHome() {
  const v = $("#homeVehicle").value,
    ms = filtered(v),
    s = stats(ms);
  $("#smartInsights").innerHTML = renderSmartDashboard(ms, s);
  const selectedVehicle = vehicles.find((x) => x.nome === v) || vehicles[0] || null;
  const orderedVehicles = [...vehicles].sort((a, b) => {
    if (a.nome === v) return -1;
    if (b.nome === v) return 1;
    const activeDifference = Number(a.ativo === false) - Number(b.ativo === false);
    return activeDifference || a.nome.localeCompare(b.nome, "pt-BR");
  });
  const selectedStatus = selectedVehicle?.ativo === false ? "Inativo" : "Ativo";
  $("#vehicleCards").innerHTML = selectedVehicle
    ? `<div class="vehicle-select-shell">
        <button type="button" id="vehicleSelectToggle" class="vehicle-select-toggle" aria-expanded="false" aria-haspopup="listbox">
          <b>${selectedVehicle.nome}</b>
          <span class="vehicle-status ${selectedVehicle.ativo === false ? "inactive" : "active"}">${selectedStatus}</span>
          <span class="vehicle-select-arrow" aria-hidden="true">▼</span>
        </button>
        <div id="vehicleSelectMenu" class="vehicle-select-menu" role="listbox" hidden>
          ${orderedVehicles.map((x) => `<button type="button" class="vehicle-select-option ${x.nome === v ? "selected" : ""}" data-select-vehicle="${x.nome}" role="option" aria-selected="${x.nome === v}"><b>${x.nome}</b><span class="vehicle-status ${x.ativo === false ? "inactive" : "active"}">${x.ativo === false ? "Inativo" : "Ativo"}</span></button>`).join("")}
        </div>
      </div>`
    : `<button type="button" class="vehicle-select-toggle empty" data-go-register-vehicles><b>Nenhum veículo cadastrado</b><span class="vehicle-select-arrow">▼</span></button>`;
  const toggle = $("#vehicleSelectToggle");
  const menu = $("#vehicleSelectMenu");
  if (toggle && menu) {
    toggle.onclick = (event) => {
      event.stopPropagation();
      const open = menu.hidden;
      menu.hidden = !open;
      toggle.setAttribute("aria-expanded", String(open));
      toggle.querySelector(".vehicle-select-arrow").textContent = open ? "▲" : "▼";
    };
    menu.onclick = (event) => event.stopPropagation();
  }
  $$("[data-select-vehicle]").forEach((button) => {
    button.onclick = () => selectVehicle(button.dataset.selectVehicle);
  });
}
function deleteMovement(id) {
  const rows = movements.filter((x) => sameMovement(x, id));
  const m = rows[0];
  if (!m) return;
  const total = rows.reduce((sum, row) => sum + (+row.valor || 0), 0);
  const resumo = `${rows.length} item(ns) de ${m.grupo}, em ${new Date(m.data_hora).toLocaleDateString("pt-BR")}, no valor de ${money(total)}`;
  if (
    !confirm(
      `Excluir este lançamento?\n\n${resumo}\n\nEsta ação não poderá ser desfeita.`,
    )
  )
    return;
  const stateBeforeSave = cloneDataState();
  try {
    movements = movements.filter((x) => !sameMovement(x, id));
    recalculateDistances();
    synchronizeAlertsFromMaintenance(rows);
    save();
    showToast("Lançamento excluído com sucesso.");
  } catch (error) {
    restoreDataState(stateBeforeSave);
    console.error("Falha ao excluir o lançamento:", error);
    alert("Não foi possível excluir o lançamento. Nenhum dado foi alterado.");
  }
}
function viewMovement(id) {
  const rows = movements.filter((x) => sameMovement(x, id));
  const m = rows[0];
  if (!m) return;
  const total = rows.reduce((sum, row) => sum + (+row.valor || 0), 0);
  const details = rows.map((row) => `• ${row.item}: ${money(row.valor)}`).join("\n");
  alert(
    `${ENTRY_GROUP_NAMES[m.grupo] || m.grupo}\n\n` +
    `Data: ${new Date(m.data_hora).toLocaleDateString("pt-BR")}\n` +
    `Veículo: ${m.veiculo || "Não informado"}\n` +
    `Hodômetro: ${intFmt(m.hodometro_km)} km\n` +
    `Local: ${m.fornecedor || m.local || "Não informado"}\n\n` +
    `${details}\n\nTotal: ${money(total)}\n` +
    `Observação: ${m.observacao && m.observacao !== "N.I." ? m.observacao : "Não informada"}`,
  );
}
function renderMovements() {
  const v = selectedVehicleName(),
    t = $("#typeFilter").value,
    q = $("#search").value.toLowerCase();
  const rows = filterByPeriod(filtered(v), "movement")
    .filter(
      (m) =>
        (!t || m.grupo === t) &&
        (!q || JSON.stringify(m).toLowerCase().includes(q)),
    )
    .sort(newestFirst);
  const ms = groupedMovements(rows);
  $("#movementCount").textContent = `${ms.length} movimento(s) · ${rows.length} item(ns) · Período: ${periodText("movement")}`;
  $("#movementList").innerHTML =
    ms.map((m) => item(m, true)).join("") ||
    '<p class="muted">Nenhum lançamento.</p>';
  $$("[data-edit-movement]").forEach(
    (b) => (b.onclick = () => {
      const row = movements.find((m) => sameMovement(m, b.dataset.editMovement));
      if (!row) return alert("Não foi possível localizar este movimento. Atualize a tela e tente novamente.");
      openEntry(b.dataset.editMovement);
    }),
  );
  $$("[data-view-movement]").forEach(
    (b) => (b.onclick = () => viewMovement(b.dataset.viewMovement)),
  );
  $$("[data-delete-movement]").forEach(
    (b) => (b.onclick = () => deleteMovement(b.dataset.deleteMovement)),
  );
}
function groupTotals(ms) {
  const g = { Combustível: 0, Administrativo: 0, Manutenção: 0, Receitas: 0 };
  ms.forEach((m) => {
    if (m.grupo === "COMBUSTÍVEL") g.Combustível += +m.valor || 0;
    else if (m.grupo === "ADMINISTRATIVO") g.Administrativo += +m.valor || 0;
    else if (m.grupo === "MANUTENÇÃO") g.Manutenção += +m.valor || 0;
    else if (m.grupo === "RECEITA") g.Receitas += +m.valor || 0;
  });
  return g;
}
function categoryCostTable(ms) {
  const s = stats(ms),
    g = groupTotals(ms),
    expenses = [
      ["Combustível", g.Combustível],
      ["Administrativo", g.Administrativo],
      ["Manutenção", g.Manutenção],
    ],
    totalExpenses = expenses.reduce((a, [, v]) => a + (+v || 0), 0),
    income = +g.Receitas || 0,
    net = totalExpenses - income,
    perKm = (v) => (s.km ? v / s.km : 0),
    perDay = (v) => (s.days ? v / s.days : 0);
  const rows = expenses
    .map(
      ([name, value]) =>
        `<tr><th scope="row">${name}</th><td>${num(totalExpenses ? (value / totalExpenses) * 100 : 0, 1)}%</td><td>${money(value)}</td><td>${money(perKm(value))}</td><td>${money(perDay(value))}</td></tr>`,
    )
    .join("");
  return `<div class="category-table-wrap"><table class="category-cost-table"><thead><tr><th>Grupo</th><th>Participação</th><th>Valor</th><th>Custo/km</th><th>Custo/dia</th></tr></thead><tbody>${rows}<tr class="total-expenses"><th scope="row">Total de gastos</th><td>${totalExpenses ? num(100, 1) + "%" : "0,0%"}</td><td>${money(totalExpenses)}</td><td>${money(perKm(totalExpenses))}</td><td>${money(perDay(totalExpenses))}</td></tr><tr class="income-row"><th scope="row">Receitas</th><td>—</td><td>− ${money(income)}</td><td>− ${money(perKm(income))}</td><td>− ${money(perDay(income))}</td></tr><tr class="net-cost-row"><th scope="row">Custo líquido</th><td>—</td><td>${money(net)}</td><td>${money(perKm(net))}</td><td>${money(perDay(net))}</td></tr></tbody></table></div>`;
}
function reportDefaultRange() {
  const end = new Date();
  end.setHours(12, 0, 0, 0);
  const start = new Date(end);
  start.setMonth(start.getMonth() - 12);
  const iso = (value) => value.toISOString().slice(0, 10);
  return { start: iso(start), end: iso(end) };
}
function ensureReportGuideDefaults(force = false) {
  const startInput = $("#reportStart"), endInput = $("#reportEnd");
  if (!startInput || !endInput) return;
  if (force || (!startInput.value && !endInput.value)) {
    const range = reportDefaultRange();
    startInput.value = range.start;
    endInput.value = range.end;
  }
}
function renderReports() {
  ensureReportGuideDefaults();
  const vehicle = selectedVehicleObject();
  const allVehicleMovements = filtered(vehicle?.nome || "");
  updateStaticVehicleSummary("reportVehicleName", "reportVehicleMeta", vehicle);
  const periodInfo = $("#reportPeriodLabel");
  if (!periodInfo) return;
  const valid = periodIsValid("report");
  if (!valid) {
    periodInfo.className = "report-guide-note error";
    periodInfo.textContent = "Corrija o período informado para gerar os relatórios.";
    return;
  }
  const effective = applyEffectivePeriodToFields("report", allVehicleMovements);
  periodInfo.className = `report-guide-note ${effective.hasOverlap ? "info" : "error"}`;
  periodInfo.textContent = effectivePeriodMessage(effective);
}
function openIntelligenceFromReports() {
  const vehicle = selectedVehicleObject();
  if (!periodIsValid("report")) return alert("Corrija o período antes de gerar a análise por I.A.");
  const effective = applyEffectivePeriodToFields("report", filtered(vehicle?.nome || ""));
  if (!effective.hasOverlap) return alert(effectivePeriodMessage(effective));
  const start = $("#reportStart")?.value || "";
  const end = $("#reportEnd")?.value || "";
  go("intelligence");
  if ($("#aiStart")) $("#aiStart").value = start;
  if ($("#aiEnd")) $("#aiEnd").value = end;
}
function reportScopedState() {
  const vehicle = selectedVehicleObject();
  const vehicleName = vehicle?.nome || "";
  const allVehicleMovements = filtered(vehicleName);
  const effective = applyEffectivePeriodToFields("report", allVehicleMovements);
  const scopedMovements = effective.hasOverlap ? filterByPeriod(allVehicleMovements, "report") : [];
  return {
    vehicle,
    movements: scopedMovements,
    registers,
    vehicles,
    drivers,
    suppliers,
    paymentMethods,
    alerts,
  };
}
async function exportReportXlsx() {
  const vehicle = selectedVehicleObject();
  if (!vehicle) return alert("Selecione um veículo na tela inicial antes de gerar o relatório XLSX.");
  if (!periodIsValid("report")) return alert("Corrija o período antes de gerar o relatório XLSX.");
  const state = reportScopedState();
  if (!state.movements.length) return alert("Não existem movimentos no veículo e período selecionados.");
  const button = $("#exportXlsx");
  const originalText = button?.textContent || "Gerar Relatório XLSX";
  try {
    if (button) {
      button.disabled = true;
      button.textContent = "Gerando XLSX...";
    }
    await MyCarPlusDB.exportDatabase(state);
    showToast("Relatório XLSX gerado com sucesso.", "success");
  } catch (error) {
    console.error("Falha ao gerar o relatório XLSX:", error);
    alert("Não foi possível gerar o relatório XLSX. Tente novamente.");
  } finally {
    if (button) {
      button.disabled = false;
      button.textContent = originalText;
    }
  }
}
let selectedReportType = null;
function showReportMenu() {
  selectedReportType = null;
  const menu = $("#reportMenuView"), period = $("#reportPeriodView");
  if (menu) menu.hidden = false;
  if (period) period.hidden = true;
  window.scrollTo({ top: 0, behavior: "smooth" });
}
function showReportPeriod(type) {
  selectedReportType = type;
  ensureReportGuideDefaults(true);
  const titles = { executive: "Relatório Executivo", ai: "Relatório por I.A.", xlsx: "Relatório XLSX" };
  const menu = $("#reportMenuView"), period = $("#reportPeriodView"), title = $("#reportSelectedTypeTitle"), generate = $("#reportGenerateSelected");
  if (menu) menu.hidden = true;
  if (period) period.hidden = false;
  if (title) title.textContent = titles[type] || "Período do relatório";
  if (generate) generate.textContent = type === "ai" ? "GERAR RELATÓRIO POR I.A." : type === "xlsx" ? "GERAR RELATÓRIO XLSX" : "GERAR RELATÓRIO EXECUTIVO";
  renderReports();
  window.scrollTo({ top: 0, behavior: "smooth" });
}
function generateSelectedReport() {
  if (!periodIsValid("report")) return alert("Corrija o período antes de gerar o relatório.");
  const vehicle = selectedVehicleObject();
  const effective = applyEffectivePeriodToFields("report", filtered(vehicle?.nome || ""));
  renderReports();
  if (!effective.hasOverlap) return alert(effectivePeriodMessage(effective));
  if (selectedReportType === "executive") return exportPdfReport();
  if (selectedReportType === "xlsx") return exportReportXlsx();
  if (selectedReportType === "ai") return openIntelligenceFromReports();
}
function setupReportGuideControls() {
  const start = $("#reportStart"), end = $("#reportEnd");
  [start, end].forEach((field) => {
    if (field && !field.dataset.boundReportGuide) {
      field.dataset.boundReportGuide = "1";
      field.addEventListener("change", renderReports);
      field.addEventListener("input", renderReports);
    }
  });
  const executive = $("#exportPdf"), ai = $("#reportAiButton"), xlsx = $("#exportXlsx");
  if (executive) executive.onclick = () => showReportPeriod("executive");
  if (ai) ai.onclick = () => showReportPeriod("ai");
  if (xlsx) xlsx.onclick = () => showReportPeriod("xlsx");
  if ($("#reportGenerateSelected")) $("#reportGenerateSelected").onclick = generateSelectedReport;
  if ($("#reportPeriodBack")) $("#reportPeriodBack").onclick = showReportMenu;
}

function withTotal(labels, values) {
  return {
    labels: [...labels, "Total"],
    values: [...values, values.reduce((a, v) => a + (+v || 0), 0)],
  };
}
function compactValue(v, format = "money") {
  if (format === "percent") return num(v, 1) + "%";
  const a = Math.abs(v);
  if (a >= 1000000) return "R$ " + num(v / 1000000, 1) + " mi";
  if (a >= 1000) return "R$ " + num(v / 1000, 1) + " mil";
  return money(v);
}
function chartTextColor() {
  return document.documentElement.dataset.theme === "light" ? "#172b3a" : "#d8e9f6";
}
function drawChart(canvas, labels, values, format = "money", options = {}) {
  const dpr = devicePixelRatio || 1,
    w = canvas.clientWidth || 600,
    h = Math.max(canvas.clientHeight || 300, 300),
    bottom = labels.length > 5 ? 72 : 56;
  canvas.width = w * dpr;
  canvas.height = h * dpr;
  const c = canvas.getContext("2d");
  c.scale(dpr, dpr);
  c.clearRect(0, 0, w, h);
  const max = Math.max(...values.map(Math.abs), 1),
    left = 48,
    right = 18,
    bw = (w - left - right) / Math.max(labels.length, 1);
  c.font = "11px system-ui";
  c.fillStyle = chartTextColor();
  labels.forEach((l, i) => {
    const x = left + i * bw + bw * 0.18,
      y = h - bottom - (Math.abs(values[i]) / max) * (h - bottom - 58),
      bh = h - bottom - y;
    c.fillStyle = options.colors?.[i] || (values[i] < 0 ? "#1f8a70" : "#246b9e");
    c.fillRect(x, y, Math.max(8, bw * 0.64), bh);
    const valueLabel = compactValue(values[i], format);
    c.font = "800 10px system-ui";
    c.fillStyle = options.valueTextColor || "#ffffff";
    c.textAlign = "center";
    c.fillText(valueLabel, x + Math.max(8, bw * 0.64) / 2, Math.max(16, y - 7));
    c.font = "11px system-ui";
    c.save();
    c.translate(x + Math.max(8, bw * 0.64) / 2, h - bottom + 15);
    if (labels.length > 5 || bw < 72) c.rotate(-Math.PI / 5);
    c.textAlign = labels.length > 5 || bw < 72 ? "right" : "center";
    c.fillStyle = chartTextColor();
    c.fillText(String(l), 0, 0);
    c.restore();
  });
}
function drawGroupedChart(canvas, labels, series, format = "money") {
  const dpr = devicePixelRatio || 1,
    w = canvas.clientWidth || 600,
    h = Math.max(canvas.clientHeight || 300, 300),
    top = series.length > 2 ? 54 : 38,
    bottom = 72;
  canvas.width = w * dpr;
  canvas.height = h * dpr;
  const c = canvas.getContext("2d");
  c.scale(dpr, dpr);
  c.clearRect(0, 0, w, h);
  const all = series.flatMap((x) => x.values),
    max = Math.max(...all.map(Math.abs), 1),
    left = 48,
    right = 18,
    gw = (w - left - right) / Math.max(labels.length, 1),
    barW = Math.max(5, Math.min(26, gw / Math.max(series.length + 1, 2))),
    showValues = true;
  c.font = "10px system-ui";
  labels.forEach((label, i) => {
    series.forEach((ser, j) => {
      const v = +ser.values[i] || 0,
        x = left + i * gw + (gw - series.length * barW) / 2 + j * barW,
        y = h - bottom - (Math.abs(v) / max) * (h - bottom - top),
        bh = h - bottom - y;
      c.fillStyle = ser.active ? "#246b9e" : "#d9822b";
      c.fillRect(x, y, barW * 0.82, bh);
      if (showValues) {
        const valueLabel = compactValue(v, format);
        c.font = "800 10px system-ui";
        c.fillStyle = "#ffffff";
        c.textAlign = "center";
        c.fillText(valueLabel, x + barW * 0.41, Math.max(14, y - 6));
        c.font = "10px system-ui";
      }
    });
    c.fillStyle = chartTextColor();
    c.save();
    c.translate(left + i * gw + gw / 2, h - bottom + 16);
    if (gw < 85) c.rotate(-Math.PI / 5);
    c.textAlign = gw < 85 ? "right" : "center";
    c.fillText(String(label), 0, 0);
    c.restore();
  });
  let lx = left,
    ly = 14;
  series.forEach((ser) => {
    const label = ser.name + (ser.active ? " (Ativo)" : " (Inativo)"),
      need = c.measureText(label).width + 30;
    if (lx + need > w - right) {
      lx = left;
      ly += 18;
    }
    c.fillStyle = ser.active ? "#246b9e" : "#d9822b";
    c.fillRect(lx, ly - 9, 10, 10);
    c.fillStyle = chartTextColor();
    c.textAlign = "left";
    c.fillText(label, lx + 14, ly);
    lx += need;
  });
  c.textAlign = "center";
}
function chartSeriesFor(vehicle, metric) {
  const ms = filterByPeriod(filtered(vehicle.nome), "chart"),
    s = stats(ms),
    g = groupTotals(ms),
    labels = Object.keys(g),
    raw = Object.values(g).map((v, i) => (labels[i] === "Receitas" ? -v : v));
  let vals = raw;
  if (metric === "km") vals = raw.map((v) => (s.km ? v / s.km : 0));
  if (metric === "day") vals = raw.map((v) => (s.days ? v / s.days : 0));
  return withTotal(labels, vals).values;
}
function yearlyFor(vehicle) {
  const y = {};
  filterByPeriod(filtered(vehicle.nome), "chart").forEach((m) => {
    const k = (m.data_hora || "").slice(0, 4);
    if (k)
      y[k] =
        (y[k] || 0) + (m.grupo === "RECEITA" ? -(+m.valor || 0) : +m.valor || 0);
  });
  return y;
}
function canvasBase(canvas, minHeight = 280) {
  const dpr = devicePixelRatio || 1,
    w = canvas.clientWidth || 600,
    h = Math.max(canvas.clientHeight || minHeight, minHeight);
  canvas.width = w * dpr;
  canvas.height = h * dpr;
  const c = canvas.getContext("2d");
  c.scale(dpr, dpr);
  c.clearRect(0, 0, w, h);
  return { c, w, h };
}
function axisLabel(value, format) {
  if (format === "consumption") return num(value, 1);
  if (format === "km") return intFmt(value);
  if (format === "thousands") return value === 0 ? "R$ 0" : "R$ " + num(value / 1000, value >= 1000 ? 0 : 1) + "k";
  return money(value);
}
function niceAxisStep(range, targetTicks = 4) {
  const safeRange = Math.max(Math.abs(range), 1);
  const raw = safeRange / Math.max(targetTicks, 1);
  const exponent = Math.floor(Math.log10(raw));
  const fraction = raw / (10 ** exponent);
  const niceFraction = fraction <= 1 ? 1 : fraction <= 2 ? 2 : fraction <= 5 ? 5 : 10;
  return niceFraction * (10 ** exponent);
}
function calculateNiceAxis(minValue, maxValue, targetTicks = 4) {
  let min = Number(minValue), max = Number(maxValue);
  if (!Number.isFinite(min) || !Number.isFinite(max)) return { min: 0, max: 1, ticks: [0, .25, .5, .75, 1] };
  if (max < min) [min, max] = [max, min];
  const originalSpan = max - min;
  const padding = originalSpan > 0
    ? Math.max(10, originalSpan * .06)
    : Math.max(50, Math.abs(max) * .002);
  let rawMin = min - padding, rawMax = max + padding;
  if (min >= 0) rawMin = Math.max(0, rawMin);
  let step = niceAxisStep(rawMax - rawMin, targetTicks);
  let axisMin = Math.floor(rawMin / step) * step;
  let axisMax = Math.ceil(rawMax / step) * step;
  if (axisMax <= axisMin) axisMax = axisMin + step;
  let count = Math.round((axisMax - axisMin) / step);
  while (count > 7) {
    step = niceAxisStep(step * 1.6, targetTicks);
    axisMin = Math.floor(rawMin / step) * step;
    axisMax = Math.ceil(rawMax / step) * step;
    count = Math.round((axisMax - axisMin) / step);
  }
  const ticks = [];
  for (let i = 0; i <= count; i++) ticks.push(axisMin + step * i);
  return { min: axisMin, max: axisMax, step, ticks };
}
function drawLineChart(canvas, labels, values, format = "money", color = "#246b9e", options = {}) {
  const { showYAxisLabels = true, showPointValues = true, yMin, yMax, niceAxis = format === "km" } = options;
  const numericValues = values.map((value) => value === null || value === undefined || value === "" ? null : Number(value));
  const finiteValues = numericValues.filter(Number.isFinite);
  const { c, w, h } = canvasBase(canvas), right = 22, top = 28, bottom = 58,
    dataMax = finiteValues.length ? Math.max(...finiteValues) : 1,
    dataMin = finiteValues.length ? Math.min(...finiteValues) : 0,
    requestedMin = Number.isFinite(Number(yMin)) ? Number(yMin) : Math.min(0, dataMin),
    requestedMax = Number.isFinite(Number(yMax)) ? Number(yMax) : Math.max(dataMax, requestedMin + 1),
    axis = niceAxis ? calculateNiceAxis(requestedMin, requestedMax, 4) : {
      min: requestedMin,
      max: requestedMax,
      ticks: Array.from({ length: 5 }, (_, i) => requestedMin + (requestedMax - requestedMin) * i / 4),
    },
    min = axis.min,
    max = axis.max,
    span = Math.max(max - min, 1);
  const tickLabels = [...axis.ticks].reverse().map((value) => axisLabel(value, format));
  c.font = "11px system-ui";
  const widestLabel = showYAxisLabels ? Math.max(...tickLabels.map((label) => c.measureText(label).width), 0) : 0;
  const left = showYAxisLabels ? Math.min(Math.max(58, Math.ceil(widestLabel) + 18), Math.max(58, w * .34)) : 28;
  const plotW = Math.max(40, w - left - right), plotH = h - top - bottom;
  c.strokeStyle = "#6f8ba166"; c.fillStyle = chartTextColor(); c.lineWidth = 1; c.font = "11px system-ui";
  [...axis.ticks].reverse().forEach((value, index, ticks) => {
    const y = ticks.length === 1 ? top + plotH / 2 : top + plotH * index / Math.max(ticks.length - 1, 1);
    c.beginPath(); c.moveTo(left, y); c.lineTo(w - right, y); c.stroke();
    if (showYAxisLabels) { c.textAlign = "right"; c.fillText(axisLabel(value, format), left - 7, y + 4); }
  });
  const points = numericValues.map((value, i) => ({
    value,
    x: labels.length === 1 ? left + plotW / 2 : left + plotW * i / Math.max(labels.length - 1, 1),
    y: Number.isFinite(value) ? top + (max - value) / span * plotH : null,
  }));
  c.strokeStyle = color; c.lineWidth = 3; c.beginPath();
  let drawing = false;
  points.forEach((point) => {
    if (!Number.isFinite(point.value)) { drawing = false; return; }
    if (drawing) c.lineTo(point.x, point.y); else { c.moveTo(point.x, point.y); drawing = true; }
  });
  c.stroke();
  points.forEach((point, i) => {
    if (Number.isFinite(point.value)) {
      c.fillStyle = color; c.beginPath(); c.arc(point.x, point.y, 4, 0, Math.PI * 2); c.fill();
      if (showPointValues) {
        c.fillStyle = "#ffffff"; c.font = "800 11px system-ui"; c.textAlign = "center";
        c.fillText(axisLabel(point.value, format), point.x, Math.max(14, point.y - 10));
      }
    }
    c.fillStyle = chartTextColor(); c.font = "11px system-ui";
    c.save(); c.translate(point.x, h - bottom + 18);
    if (labels.length > 6) c.rotate(-Math.PI / 5);
    c.textAlign = labels.length > 6 ? "right" : "center"; c.fillText(labels[i], 0, 0); c.restore();
  });
  if (!finiteValues.length) {
    c.fillStyle = chartTextColor(); c.font = "700 13px system-ui"; c.textAlign = "center";
    c.fillText("Sem dados suficientes no período", left + plotW / 2, top + plotH / 2);
  }
}
function drawMonthlyChart(canvas, labels, values) {
  const { c, w, h } = canvasBase(canvas, 300), left = 58, right = 18, top = 24, bottom = 70,
    plotH = h - top - bottom, max = Math.max(...values, 1), step = (w - left - right) / Math.max(labels.length, 1);
  c.font = "11px system-ui";
  for (let i = 0; i <= 4; i++) {
    const y = top + plotH * i / 4, value = max * (1 - i / 4);
    c.strokeStyle = "#6f8ba166"; c.beginPath(); c.moveTo(left, y); c.lineTo(w - right, y); c.stroke();
    c.fillStyle = chartTextColor(); c.textAlign = "right"; c.fillText(axisLabel(value, "thousands"), left - 7, y + 4);
  }
  values.forEach((value, i) => {
    const barW = Math.max(5, step * .62), x = left + i * step + (step - barW) / 2,
      bh = value / max * plotH, y = top + plotH - bh;
    c.fillStyle = "#246b9e"; c.fillRect(x, y, barW, bh);
    c.fillStyle = chartTextColor(); c.save(); c.translate(x + barW / 2, h - bottom + 17);
    if (labels.length > 6) c.rotate(-Math.PI / 5);
    c.textAlign = labels.length > 6 ? "right" : "center"; c.fillText(labels[i], 0, 0); c.restore();
  });
}
function chartDate(value) {
  return new Date(String(value || "").slice(0, 10) + "T12:00:00");
}
function chartIso(date) {
  return `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}-${String(date.getDate()).padStart(2, "0")}`;
}
function chartPeriodGranularity(start, end) {
  if (!start || !end) return "year";
  const first = chartDate(start), last = chartDate(end), anniversary = new Date(first);
  anniversary.setFullYear(first.getFullYear() + 1);
  anniversary.setDate(anniversary.getDate() - 1);
  return last < anniversary ? "month" : "year";
}
function chartBucketKey(value, granularity) {
  const date = typeof value === "string" ? chartDate(value) : value;
  return granularity === "month"
    ? `${date.getFullYear()}-${String(date.getMonth() + 1).padStart(2, "0")}`
    : String(date.getFullYear());
}
function chartBucketLabel(date, granularity) {
  return granularity === "month"
    ? date.toLocaleDateString("pt-BR", { month: "short", year: "2-digit" }).replace(" de ", "/").replace(".", "")
    : String(date.getFullYear());
}
function buildChartBuckets(start, end, granularity) {
  if (!start || !end) return [];
  const selectedStart = chartDate(start), selectedEnd = chartDate(end), cursor = new Date(selectedStart), buckets = [];
  if (granularity === "month") cursor.setDate(1);
  else { cursor.setMonth(0); cursor.setDate(1); }
  while (cursor <= selectedEnd) {
    const unitStart = new Date(cursor), unitEnd = new Date(cursor);
    if (granularity === "month") {
      unitEnd.setMonth(unitEnd.getMonth() + 1); unitEnd.setDate(0);
    } else {
      unitEnd.setFullYear(unitEnd.getFullYear() + 1); unitEnd.setDate(0);
    }
    const bucketStart = unitStart < selectedStart ? new Date(selectedStart) : unitStart;
    const bucketEnd = unitEnd > selectedEnd ? new Date(selectedEnd) : unitEnd;
    buckets.push({
      key: chartBucketKey(unitStart, granularity),
      label: chartBucketLabel(unitStart, granularity),
      start: chartIso(bucketStart),
      end: chartIso(bucketEnd),
      days: Math.max(1, Math.round((bucketEnd - bucketStart) / 86400000) + 1),
    });
    if (granularity === "month") cursor.setMonth(cursor.getMonth() + 1);
    else cursor.setFullYear(cursor.getFullYear() + 1);
  }
  return buckets;
}
function fuelSeriesIdentity(rawName) {
  const raw = String(rawName || "").trim();
  const normalized = normalizeText(raw);
  if (normalized.includes("gasolina")) return { key: "gasolina", name: "Gasolina" };
  if (normalized.includes("etanol") || normalized.includes("alco" + "ol")) return { key: "etanol", name: "Etanol" };
  if (normalized.includes("diesel")) return { key: "diesel", name: "Diesel" };
  if (normalized === "gnv" || normalized.includes("gas natural")) return { key: "gnv", name: "GNV" };
  const name = raw || "Outro combustível";
  const key = normalized.replace(/[^a-z0-9]+/g, "-").replace(/^-|-$/g, "") || "outro-combustivel";
  return { key, name };
}
function fuelSeriesColor(key, occupied = new Set()) {
  if (key === "geral") return "#d94b4b";
  if (key === "gasolina") return "#f28b0c";
  if (key === "etanol") return "#246b9e";
  if (key === "diesel") return "#1f8a70";
  const palette = ["#7c3aed", "#0891b2", "#c026d3", "#a16207", "#475569", "#db2777", "#4f46e5", "#0f766e"];
  let hash = 0;
  String(key || "outro").split("").forEach((character) => { hash = ((hash << 5) - hash + character.charCodeAt(0)) | 0; });
  const start = Math.abs(hash) % palette.length;
  for (let offset = 0; offset < palette.length; offset++) {
    const color = palette[(start + offset) % palette.length];
    if (!occupied.has(color)) return color;
  }
  return palette[start];
}
function orderedFuelKeys(keys) {
  const priority = new Map([["gasolina", 0], ["etanol", 1], ["diesel", 2]]);
  return [...keys].sort((a, b) => {
    const pa = priority.has(a) ? priority.get(a) : 10;
    const pb = priority.has(b) ? priority.get(b) : 10;
    return pa - pb || String(a).localeCompare(String(b), "pt-BR", { sensitivity: "base" });
  });
}
function periodChartData(ms, start, end) {
  const granularity = chartPeriodGranularity(start, end), buckets = buildChartBuckets(start, end, granularity), byKey = new Map();
  buckets.forEach((bucket) => byKey.set(bucket.key, { net: 0, distance: 0, liters: 0, movementCount: 0, fuels: new Map() }));
  const fuelNames = new Map();
  ms.forEach((movement) => {
    const date = String(movement.data_hora || "").slice(0, 10);
    if (!date) return;
    const item = byKey.get(chartBucketKey(date, granularity));
    if (!item) return;
    item.movementCount += 1;
    item.net += movement.grupo === "RECEITA" ? -(+movement.valor || 0) : (+movement.valor || 0);
    if (movement.grupo === "COMBUSTÍVEL" && movement.tanque_completo !== "NAO" && +movement.quantidade_litros > 0 && +movement.distancia_abastecimento_km > 0) {
      const distance = +movement.distancia_abastecimento_km;
      const liters = +movement.quantidade_litros;
      const identity = fuelSeriesIdentity(movement.item);
      const fuel = item.fuels.get(identity.key) || { distance: 0, liters: 0 };
      fuel.distance += distance;
      fuel.liters += liters;
      item.fuels.set(identity.key, fuel);
      fuelNames.set(identity.key, identity.name);
      item.distance += distance;
      item.liters += liters;
    }
  });
  const labels = buckets.map((bucket) => bucket.label);
  const consumption = buckets.map((bucket) => {
    const item = byKey.get(bucket.key);
    return item?.liters > 0 ? item.distance / item.liters : null;
  });
  const fuelSeries = [{
    key: "geral",
    name: "Geral",
    color: fuelSeriesColor("geral"),
    values: consumption,
    showPointValues: true,
  }];
  const occupiedColors = new Set(fuelSeries.map((item) => item.color));
  orderedFuelKeys(fuelNames.keys()).forEach((fuelKey) => {
    const color = fuelSeriesColor(fuelKey, occupiedColors);
    occupiedColors.add(color);
    fuelSeries.push({
      key: fuelKey,
      name: fuelNames.get(fuelKey),
      color,
      values: buckets.map((bucket) => {
        const fuel = byKey.get(bucket.key)?.fuels.get(fuelKey);
        return fuel?.liters > 0 ? fuel.distance / fuel.liters : null;
      }),
      showPointValues: false,
    });
  });
  return {
    granularity,
    labels,
    consumption,
    fuelSeries,
    daily: buckets.map((bucket) => {
      const item = byKey.get(bucket.key);
      return item?.movementCount > 0 ? item.net / bucket.days : null;
    }),
  };
}
function financialChartData(ms, start, end) {
  const granularity = chartPeriodGranularity(start, end), buckets = buildChartBuckets(start, end, granularity), byKey = new Map();
  buckets.forEach((bucket) => byKey.set(bucket.key, { gross: 0, income: 0 }));
  ms.forEach((movement) => {
    const date = String(movement.data_hora || "").slice(0, 10);
    if (!date) return;
    const item = byKey.get(chartBucketKey(date, granularity));
    if (!item) return;
    if (movement.grupo === "RECEITA") item.income += +movement.valor || 0;
    else item.gross += +movement.valor || 0;
  });
  let gross = 0, income = 0;
  const grossValues = [], incomeValues = [], netValues = [];
  buckets.forEach((bucket) => {
    const item = byKey.get(bucket.key);
    gross += item?.gross || 0; income += item?.income || 0;
    grossValues.push(gross); incomeValues.push(income); netValues.push(gross - income);
  });
  return { granularity, labels: buckets.map((bucket) => bucket.label), gross: grossValues, income: incomeValues, net: netValues };
}
function odometerBaseline(vehicle, allMovements, start) {
  const valid = allMovements
    .filter((movement) => +movement.hodometro_km > 0 && String(movement.data_hora || "").slice(0, 10) <= start)
    .sort((a, b) => String(a.data_hora || "").localeCompare(String(b.data_hora || "")) || (+a.hodometro_km || 0) - (+b.hodometro_km || 0));
  const sameDay = valid.filter((movement) => String(movement.data_hora || "").slice(0, 10) === start);
  if (sameDay.length) return Math.max(...sameDay.map((movement) => +movement.hodometro_km || 0));
  if (valid.length) return +valid.at(-1).hodometro_km || 0;
  if (+vehicle?.kmInicial > 0) return +vehicle.kmInicial;
  const first = allMovements.filter((movement) => +movement.hodometro_km > 0)
    .sort((a, b) => String(a.data_hora || "").localeCompare(String(b.data_hora || "")) || (+a.hodometro_km || 0) - (+b.hodometro_km || 0))[0];
  return +first?.hodometro_km || 0;
}
function odometerGlobalLabelIndexes(points, start, end) {
  const indexes = new Set();
  if (!points.length) return indexes;
  indexes.add(0);
  if (points.length > 1) indexes.add(points.length - 1);
  if (points.length > 2) {
    const startTime = chartDate(start || points[0].date).getTime();
    const endTime = chartDate(end || points.at(-1).date).getTime();
    const middleTime = startTime + (endTime - startTime) / 2;
    let middleIndex = 1, middleDistance = Infinity;
    points.forEach((point, index) => {
      if (index === 0 || index === points.length - 1) return;
      const distance = Math.abs(chartDate(point.date).getTime() - middleTime);
      if (distance < middleDistance) { middleIndex = index; middleDistance = distance; }
    });
    indexes.add(middleIndex);
  }
  return indexes;
}
function odometerMonthLabel(dateValue, includeYear = false) {
  const date = chartDate(dateValue);
  const month = date.toLocaleDateString("pt-BR", { month: "short" }).replace(".", "").toLowerCase();
  return includeYear ? `${month}/${String(date.getFullYear()).slice(-2)}` : month;
}
function odometerChartData(vehicle, allMovements, start, end) {
  const baseline = odometerBaseline(vehicle, allMovements, start), points = [];
  if (baseline > 0) points.push({ date: start, km: baseline });
  allMovements
    .filter((movement) => {
      const date = String(movement.data_hora || "").slice(0, 10), km = +movement.hodometro_km || 0;
      return date >= start && date <= end && km > 0 && (!baseline || km >= baseline);
    })
    .sort((a, b) => String(a.data_hora || "").localeCompare(String(b.data_hora || "")) || (+a.hodometro_km || 0) - (+b.hodometro_km || 0))
    .forEach((movement) => {
      const point = { date: String(movement.data_hora || "").slice(0, 10), km: +movement.hodometro_km || 0 };
      const last = points.at(-1);
      if (!last || last.date !== point.date || last.km !== point.km) points.push(point);
    });
  const normalizedPoints = [...points.reduce((map, point) => {
    const current = map.get(point.date);
    if (!current || point.km > current.km) map.set(point.date, point);
    return map;
  }, new Map()).values()].sort((a, b) => a.date.localeCompare(b.date) || a.km - b.km);
  const displayPoints = normalizedPoints;
  const labelIndexes = odometerGlobalLabelIndexes(displayPoints, start, end);
  const values = displayPoints.map((point) => point.km);
  const yMin = values.length ? Math.min(...values) : (baseline || 0);
  const yMax = values.length ? Math.max(...values) : (baseline || 1);
  return {
    start,
    end,
    points: displayPoints,
    dates: displayPoints.map((point) => point.date),
    labelIndexes,
    labels: displayPoints.map((point, index) => labelIndexes.has(index) ? odometerMonthLabel(point.date, false) : ""),
    years: displayPoints.map((point, index) => labelIndexes.has(index) ? chartDate(point.date).getFullYear() : null),
    values,
    yMin,
    yMax,
  };
}
function drawOdometerChart(canvas, data) {
  const { c, w, h } = canvasBase(canvas, 330), right = 24, top = 28,
    bottom = 78,
    axis = calculateNiceAxis(data.yMin, data.yMax, 5), min = axis.min, max = axis.max,
    span = Math.max(max - min, 1), tickValues = [...axis.ticks].reverse();
  c.font = "11px system-ui";
  const widest = Math.max(...tickValues.map((value) => c.measureText(axisLabel(value, "km")).width), 0);
  const left = Math.min(Math.max(68, Math.ceil(widest) + 22), Math.max(68, w - right - 100));
  const plotW = Math.max(80, w - left - right), plotH = Math.max(90, h - top - bottom);
  c.strokeStyle = "#6f8ba166"; c.fillStyle = chartTextColor(); c.lineWidth = 1;
  tickValues.forEach((value, index) => {
    const y = tickValues.length === 1 ? top + plotH / 2 : top + plotH * index / Math.max(tickValues.length - 1, 1);
    c.beginPath(); c.moveTo(left, y); c.lineTo(w - right, y); c.stroke();
    c.textAlign = "right"; c.fillText(axisLabel(value, "km"), left - 8, y + 4);
  });
  const startTime = chartDate(data.start).getTime(), endTime = chartDate(data.end).getTime(), timeSpan = Math.max(endTime - startTime, 1);
  const points = data.values.map((value, index) => {
    const time = chartDate(data.dates[index]).getTime();
    const ratio = data.values.length === 1 ? .5 : Math.max(0, Math.min(1, (time - startTime) / timeSpan));
    return { index, value, date: data.dates[index], x: left + plotW * ratio, y: top + (max - value) / span * plotH };
  });
  c.strokeStyle = "#246b9e"; c.lineWidth = 3; c.beginPath();
  points.forEach((point, index) => index ? c.lineTo(point.x, point.y) : c.moveTo(point.x, point.y));
  if (points.length) c.stroke();
  points.forEach((point) => {
    c.fillStyle = "#246b9e"; c.beginPath(); c.arc(point.x, point.y, 4.5, 0, Math.PI * 2); c.fill();
  });
  c.fillStyle = chartTextColor();
  points.forEach((point, index) => {
    const label = data.labels[index], year = data.years[index];
    if (!label || !year) return;
    const isFirst = index === 0, isLast = index === points.length - 1;
    c.textAlign = isFirst ? "left" : isLast ? "right" : "center";
    c.font = "700 11px system-ui";
    c.fillText(label, point.x, h - bottom + 22);
    c.font = "11px system-ui";
    c.fillText(String(year), point.x, h - bottom + 40);
  });
  if (!points.length) {
    c.fillStyle = chartTextColor(); c.font = "700 13px system-ui"; c.textAlign = "center";
    c.fillText("Sem dados suficientes no período", left + plotW / 2, top + plotH / 2);
  }
  canvas._odometerHitPoints = points;
  canvas._odometerChartData = data;
  canvas.onclick = (event) => {
    const rect = canvas.getBoundingClientRect(), x = event.clientX - rect.left, y = event.clientY - rect.top;
    const nearest = points.reduce((best, point) => {
      const distance = Math.hypot(point.x - x, point.y - y);
      return !best || distance < best.distance ? { point, distance } : best;
    }, null);
    const card = canvas.closest(".chart-card");
    if (!card) return;
    let tooltip = card.querySelector(".odometer-point-tooltip");
    if (!tooltip) { tooltip = document.createElement("div"); tooltip.className = "odometer-point-tooltip"; card.appendChild(tooltip); }
    if (!nearest || nearest.distance > 26) { tooltip.hidden = true; return; }
    const { point } = nearest, previous = points[point.index - 1];
    const delta = previous ? point.value - previous.value : null;
    tooltip.innerHTML = `<strong>${formatPeriodDate(point.date)}</strong><span>${intFmt(point.value)} km</span>${delta === null ? "" : `<small>${delta >= 0 ? "+" : ""}${intFmt(delta)} km desde o ponto anterior</small>`}`;
    tooltip.style.left = `${Math.max(8, Math.min(card.clientWidth - 190, point.x - 75))}px`;
    tooltip.style.top = `${Math.max(48, point.y - 18)}px`;
    tooltip.hidden = false;
  };
}
function drawFuelConsumptionChart(canvas, labels, series) {
  const visibleSeries = series.filter((item) => item.values.some((value) => value !== null && value !== undefined && Number.isFinite(Number(value))));
  const { c, w, h } = canvasBase(canvas, 350), right = 22, bottom = 64,
    legendCols = w < 430 ? 2 : Math.min(4, Math.max(1, visibleSeries.length)),
    legendRows = Math.max(1, Math.ceil(visibleSeries.length / legendCols)),
    legendTop = 16, legendRowHeight = 19, top = 24 + legendRows * legendRowHeight,
    allValues = visibleSeries.flatMap((item) => item.values.map((value) => value === null ? null : Number(value))).filter(Number.isFinite),
    dataMax = allValues.length ? Math.max(...allValues) : 1,
    axis = calculateNiceAxis(0, Math.max(1, dataMax), 4), min = Math.max(0, axis.min), max = axis.max,
    span = Math.max(max - min, 1);
  const tickValues = [...axis.ticks].filter((value) => value >= min).reverse();
  c.font = "11px system-ui";
  const tickLabels = tickValues.map((value) => axisLabel(value, "consumption"));
  const widest = Math.max(...tickLabels.map((label) => c.measureText(label).width), 0);
  const left = Math.max(48, Math.ceil(widest) + 18), plotW = Math.max(80, w - left - right), plotH = Math.max(100, h - top - bottom);
  tickValues.forEach((value, index) => {
    const y = tickValues.length === 1 ? top + plotH / 2 : top + plotH * index / Math.max(tickValues.length - 1, 1);
    c.strokeStyle = "#6f8ba166"; c.lineWidth = 1; c.beginPath(); c.moveTo(left, y); c.lineTo(w - right, y); c.stroke();
    c.fillStyle = chartTextColor(); c.textAlign = "right"; c.fillText(axisLabel(value, "consumption"), left - 7, y + 4);
  });
  const legendWidth = plotW / legendCols;
  visibleSeries.forEach((item, index) => {
    const column = index % legendCols, row = Math.floor(index / legendCols),
      lx = left + column * legendWidth, ly = legendTop + row * legendRowHeight;
    c.strokeStyle = item.color; c.lineWidth = 3; c.beginPath(); c.moveTo(lx, ly - 4); c.lineTo(lx + 13, ly - 4); c.stroke();
    c.fillStyle = item.color; c.beginPath(); c.arc(lx + 6.5, ly - 4, 3, 0, Math.PI * 2); c.fill();
    c.fillStyle = chartTextColor(); c.font = "11px system-ui"; c.textAlign = "left"; c.fillText(item.name, lx + 18, ly);
  });
  const xFor = (index) => labels.length === 1 ? left + plotW / 2 : left + plotW * index / Math.max(labels.length - 1, 1);
  const drawOrder = [...visibleSeries].sort((a, b) => Number(a.key === "geral") - Number(b.key === "geral"));
  drawOrder.forEach((item) => {
    const points = item.values.map((rawValue, index) => {
      const value = rawValue === null || rawValue === undefined ? null : Number(rawValue);
      return { index, value, x: xFor(index), y: Number.isFinite(value) ? top + (max - value) / span * plotH : null };
    });
    c.strokeStyle = item.color; c.lineWidth = item.key === "geral" ? 3.5 : 2.7; c.beginPath();
    let drawing = false;
    points.forEach((point) => {
      if (!Number.isFinite(point.value)) { drawing = false; return; }
      if (drawing) c.lineTo(point.x, point.y); else { c.moveTo(point.x, point.y); drawing = true; }
    });
    c.stroke();
    points.forEach((point) => {
      if (!Number.isFinite(point.value)) return;
      c.fillStyle = item.color; c.beginPath(); c.arc(point.x, point.y, item.key === "geral" ? 4.5 : 3.7, 0, Math.PI * 2); c.fill();
      if (item.showPointValues) {
        c.save(); c.font = "800 10px system-ui"; c.textAlign = "center"; c.fillStyle = chartTextColor();
        c.shadowColor = "rgba(0,0,0,.65)"; c.shadowBlur = 3;
        c.fillText(axisLabel(point.value, "consumption"), point.x, Math.max(top + 10, point.y - 10)); c.restore();
      }
    });
  });
  labels.forEach((label, index) => {
    const x = xFor(index);
    c.fillStyle = chartTextColor(); c.font = "11px system-ui"; c.save(); c.translate(x, h - bottom + 20);
    if (labels.length > 6) c.rotate(-Math.PI / 5);
    c.textAlign = labels.length > 6 ? "right" : "center"; c.fillText(label, 0, 0); c.restore();
  });
  if (!allValues.length) {
    c.fillStyle = chartTextColor(); c.font = "700 13px system-ui"; c.textAlign = "center";
    c.fillText("Sem dados suficientes no período", left + plotW / 2, top + plotH / 2);
  }
}
function drawMultiLineChart(canvas, labels, series, format = "money") {
  const { c, w, h } = canvasBase(canvas, 320), left = 62, right = 22, bottom = 58,
    legendCols = w < 460 ? 2 : Math.max(1, series.length),
    legendRows = Math.ceil(series.length / legendCols),
    legendTop = 15, legendRowHeight = 19, top = 22 + legendRows * legendRowHeight,
    plotW = w - left - right, plotH = h - top - bottom,
    values = series.flatMap(s => s.values.map(Number)).filter(Number.isFinite),
    max = Math.max(...values, 1), min = Math.min(0, ...values), span = Math.max(max - min, 1);
  c.font = "11px system-ui";
  for (let i = 0; i <= 4; i++) {
    const y = top + plotH * i / 4, value = max - span * i / 4;
    c.strokeStyle = "#6f8ba166"; c.beginPath(); c.moveTo(left, y); c.lineTo(w - right, y); c.stroke();
    c.fillStyle = chartTextColor(); c.textAlign = "right"; c.fillText(axisLabel(value, format), left - 7, y + 4);
  }
  series.forEach((ser) => {
    const points = ser.values.map((value, i) => ({
      x: labels.length === 1 ? left + plotW / 2 : left + plotW * i / Math.max(labels.length - 1, 1),
      y: top + (max - Number(value)) / span * plotH,
    }));
    c.strokeStyle = ser.color; c.lineWidth = 3; c.beginPath();
    points.forEach((p, i) => i ? c.lineTo(p.x, p.y) : c.moveTo(p.x, p.y)); c.stroke();
    points.forEach(p => { c.fillStyle = ser.color; c.beginPath(); c.arc(p.x, p.y, 4, 0, Math.PI * 2); c.fill(); });
  });
  const legendWidth = plotW / legendCols;
  series.forEach((ser, index) => {
    const column = index % legendCols, row = Math.floor(index / legendCols),
      lx = left + column * legendWidth, ly = legendTop + row * legendRowHeight;
    c.fillStyle = ser.color; c.fillRect(lx, ly - 9, 11, 11);
    c.fillStyle = chartTextColor(); c.textAlign = "left"; c.fillText(ser.name, lx + 16, ly);
  });
  labels.forEach((label, i) => {
    const x = labels.length === 1 ? left + plotW / 2 : left + plotW * i / Math.max(labels.length - 1, 1);
    c.fillStyle = chartTextColor(); c.font = "11px system-ui"; c.save(); c.translate(x, h - bottom + 20);
    if (labels.length > 6) c.rotate(-Math.PI / 5);
    c.textAlign = labels.length > 6 ? "right" : "center"; c.fillText(label, 0, 0); c.restore();
  });
}
function costCompositionData(ms) {
  const s = stats(ms), g = groupTotals(ms), labels = ["Combustível", "Administrativo", "Manutenção"],
    totals = [g.Combustível, g.Administrativo, g.Manutenção],
    totalCosts = totals.reduce((sum, value) => sum + (+value || 0), 0),
    consolidatedLabels = [...labels, "Total dos custos"],
    consolidatedColors = ["#246b9e", "#246b9e", "#246b9e", "#f28b0c"],
    perKmGroups = totals.map(value => s.km ? value / s.km : 0),
    perDayGroups = totals.map(value => s.days ? value / s.days : 0);
  const sub = {};
  ms.filter(m => m.grupo !== "RECEITA").forEach(m => {
    const k = m.item || "Não informado"; sub[k] = (sub[k] || 0) + (+m.valor || 0);
  });
  const top = Object.entries(sub).sort((a,b)=>b[1]-a[1]).slice(0,5);
  return {
    labels, totals,
    totalLabels: consolidatedLabels,
    totalValues: [...totals, totalCosts],
    totalColors: consolidatedColors,
    perKmLabels: consolidatedLabels,
    perKm: [...perKmGroups, s.km ? totalCosts / s.km : 0],
    perKmColors: consolidatedColors,
    perDayLabels: consolidatedLabels,
    perDay: [...perDayGroups, s.days ? totalCosts / s.days : 0],
    perDayColors: consolidatedColors,
    topLabels: top.map(x=>x[0]), topValues: top.map(x=>x[1])
  };
}
function renderNewCharts(ms, { vehicle, allMovements, start, end } = {}) {
  const timeline = periodChartData(ms, start, end), financial = financialChartData(ms, start, end),
    odometer = odometerChartData(vehicle, allMovements || ms, start, end), composition = costCompositionData(ms),
    monthly = timeline.granularity === "month";
  if ($("#chartConsumptionTitle")) $("#chartConsumptionTitle").textContent = monthly
    ? "Consumo médio mensal por combustível (km/L)"
    : "Consumo médio anual por combustível (km/L)";
  if ($("#chartConsumptionNote")) $("#chartConsumptionNote").textContent = "Geral em vermelho, Gasolina em laranja, Etanol em azul e Diesel em verde. Somente a linha Geral exibe valores fixos nos pontos.";
  if ($("#chartFinancialTitle")) $("#chartFinancialTitle").textContent = "Evolução financeira acumulada no período (R$)";
  if ($("#chartFinancialNote")) $("#chartFinancialNote").textContent = "Valores acumulados desde a data inicial. As receitas reduzem o custo bruto para formar o custo líquido.";
  if ($("#chartDailyTitle")) $("#chartDailyTitle").textContent = monthly
    ? "Custo líquido médio diário por mês (R$/dia)"
    : "Custo líquido médio diário por ano (R$/dia)";
  if ($("#chartDailyNote")) $("#chartDailyNote").textContent = monthly
    ? "Custo líquido de cada mês ÷ dias do mês abrangidos pelo período."
    : "Custo líquido de cada ano ÷ dias do ano abrangidos pelo período.";
  drawFuelConsumptionChart($("#chartAnnualConsumption"), timeline.labels, timeline.fuelSeries);
  drawOdometerChart($("#chartOdometer"), odometer);
  drawMultiLineChart($("#chartAnnualCost"), financial.labels, [
    {name:"Custo bruto", values:financial.gross, color:"#d94b4b"},
    {name:"Receitas", values:financial.income, color:"#1f8a70"},
    {name:"Custo líquido", values:financial.net, color:"#246b9e"}
  ], "money");
  drawLineChart($("#chartAnnualDaily"), timeline.labels, timeline.daily, "money", "#d94b4b", { showPointValues:false });
  drawChart($("#chartCategoryKm"), composition.perKmLabels, composition.perKm, "money", { colors: composition.perKmColors });
  drawChart($("#chartCategoryDay"), composition.perDayLabels, composition.perDay, "money", { colors: composition.perDayColors });
  drawChart($("#chartCategoryTotal"), composition.totalLabels, composition.totalValues, "money", { colors: composition.totalColors });
  drawChart($("#chartTopSubcategories"), composition.topLabels, composition.topValues, "money");
}
function chartDefaultRange() {
  const end = new Date();
  end.setHours(12, 0, 0, 0);
  const start = new Date(end);
  start.setMonth(start.getMonth() - 12);
  const iso = (value) => value.toISOString().slice(0, 10);
  return { start: iso(start), end: iso(end) };
}
function ensureChartPeriodDefaults(force = false) {
  const start = $("#chartStart");
  const end = $("#chartEnd");
  if (!start || !end) return;
  if (force || (!start.value && !end.value)) {
    const range = chartDefaultRange();
    start.value = range.start;
    end.value = range.end;
  }
}
function renderCharts() {
  ensureChartPeriodDefaults(false);
  const selected = $("#chartVehicle").value;
  const selectedObject = vehicles.find((item) => item.nome === selected) || selectedVehicleObject();
  const vehicleName = selectedObject?.nome || selected;
  const allVehicleMovements = filtered(vehicleName);
  const validPeriod = periodIsValid("chart");
  updateStaticVehicleSummary("chartVehicleName", "chartVehicleStatus", selectedObject);
  if (!validPeriod) {
    $("#chartPeriodLabel").textContent = "Corrija o período informado.";
    $("#chartEmpty").hidden = true;
    $("#chartContent").hidden = true;
    return;
  }
  const effective = applyEffectivePeriodToFields("chart", allVehicleMovements);
  $("#chartPeriodLabel").textContent = effectivePeriodMessage(effective);
  if (!effective.hasOverlap) {
    $("#chartEmpty").textContent = effectivePeriodMessage(effective);
    $("#chartEmpty").hidden = false;
    $("#chartContent").hidden = true;
    return;
  }
  const allVisible = filterByPeriod(allVehicleMovements, "chart");
  $("#chartEmpty").textContent = "Nenhum lançamento encontrado no período selecionado.";
  $("#chartEmpty").hidden = allVisible.length > 0;
  $("#chartContent").hidden = allVisible.length === 0;
  if (!allVisible.length) return;
  const { start, end } = periodValues("chart"), vehicle = selectedObject;
  renderNewCharts(allVisible, { vehicle, allMovements: allVehicleMovements, start, end });
}

function renderRegisters() {
  const group = $("#registerGroup").value;
  let rows = [];
  if (group === "ITEM")
    rows = registers.map((r) => ({
      id: r.id,
      title: r.item,
      sub: `${r.grupo}${r.padrao ? " · Padrão" : ""}`,
      active: r.ativo !== false,
      standard: !!r.padrao,
    }));
  if (group === "MOTORISTA")
    rows = drivers.map((r) => ({
      id: r.id,
      title: r.nome,
      sub: r.padrao ? "Padrão" : "Motorista",
      active: r.ativo !== false,
      standard: !!r.padrao,
    }));
  if (group === "FORNECEDOR")
    rows = suppliers.map((r) => ({id:r.id,title:r.nome,sub:r.local || "Local não informado",active:r.ativo !== false,standard:!!r.padrao}));
  if (group === "FORMA_PAGAMENTO")
    rows = paymentMethods.map((r) => ({id:r.id,title:r.nome,sub:"Forma de pagamento",active:r.ativo !== false,standard:!!r.padrao}));
  if (group === "VEICULO")
    rows = vehicles.map((r) => {
      const z = vehicleSummary(r);
      return {
        id: r.id,
        title: r.nome,
        sub: `${r.placa || "Sem placa"} · ${r.anoFabricacao || "—"}/${r.anoModelo || "—"}`,
        active: r.ativo !== false,
        standard: !!r.padrao,
        vehicleMetrics: [
          ["Hodômetro inicial", `${intFmt(z.initial)} km`],
          ["Hodômetro atual", `${intFmt(z.last)} km`],
          ["Total rodado", `${intFmt(z.driven)} km`],
          ["Capacidade do tanque", `${num(r.capacidadeTanque || 0,1)} L`],
          ["Motorização", ({FLEX:"Flex",GASOLINA:"Gasolina",ETANOL:"Etanol",DIESEL:"Diesel"})[r.motorizacao] || "Flex"],
        ],
      };
    });
  const titles = {ITEM:["Classificação dos lançamentos","Itens de lançamento"],MOTORISTA:["Veículo e utilização","Motoristas"],VEICULO:["Veículo e utilização","Veículos"],FORNECEDOR:["Fornecedores e pagamentos","Fornecedores"],FORMA_PAGAMENTO:["Fornecedores e pagamentos","Formas de pagamento"]};
  const [eyebrow,title] = titles[group];
  $("#registerEyebrow").textContent = eyebrow;
  $("#registerGroupTitle").textContent = title;
  [["ITEM",registers],["MOTORISTA",drivers],["VEICULO",vehicles],["FORNECEDOR",suppliers],["FORMA_PAGAMENTO",paymentMethods]].forEach(([key,arr]) => $("#count"+key).textContent = arr.length);
  const term = ($("#registerSearch").value || "").trim().toLocaleLowerCase("pt-BR");
  const status = $("#registerStatusFilter").value;
  rows = rows.filter(r => {
    const source = group === "ITEM" ? registers : group === "MOTORISTA" ? drivers : group === "VEICULO" ? vehicles : group === "FORNECEDOR" ? suppliers : paymentMethods;
    const item = source.find(x => x.id === r.id) || {};
    const active = item.ativo !== false;
    return (!term || `${r.title} ${r.sub}`.toLocaleLowerCase("pt-BR").includes(term)) &&
      (status === "TODOS" || (status === "ATIVOS" && active) || (status === "INATIVOS" && !active) || (status === "PADRAO" && item.padrao));
  });
  rows.sort((a, b) =>
    a.title.localeCompare(b.title, "pt-BR", { sensitivity: "base", numeric: true }),
  );
  $("#registerCountText").textContent = `${rows.length} ${rows.length === 1 ? "registro exibido" : "registros exibidos"}`;
  $$("[data-register-group]").forEach(b => b.classList.toggle("active", b.dataset.registerGroup === group));
  $("#registerList").innerHTML =
    rows
      .map(
        (r) => {
          const badges = `<span class="register-badge ${r.active ? "active" : "inactive"}">${r.active ? "ATIVO" : "INATIVO"}</span>${r.standard ? '<span class="register-badge standard">PADRÃO</span>' : ""}`;
          const metrics = r.vehicleMetrics
            ? `<div class="register-metrics">${r.vehicleMetrics.map(([label,value]) => `<div><small>${label}</small><strong>${value}</strong></div>`).join("")}</div>`
            : "";
          return `<article class="register-card ${r.vehicleMetrics ? "vehicle-register-card" : ""}">
            <div class="register-card-main">
              <div class="register-card-title"><div><b>${r.title}</b><small>${r.sub}</small></div><div class="register-badges">${badges}</div></div>
              ${metrics}
            </div>
            <div class="register-card-actions">
              <button class="register-edit-action" data-edit="${r.id}"><span aria-hidden="true">✎</span> Alterar</button>
              <button class="register-delete-action" data-delete="${r.id}"><span aria-hidden="true">♲</span> Excluir</button>
            </div>
          </article>`;
        },
      )
      .join("") || '<p class="muted">Nenhum cadastro.</p>';
  $$("[data-edit]").forEach(
    (b) => (b.onclick = () => openRegister(b.dataset.edit)),
  );
  $$("[data-delete]").forEach(
    (b) => (b.onclick = () => deleteRegister(b.dataset.delete)),
  );
}
function renderAll() {
  fillVehicleSelects();
  fillDrivers();
  fillOperationalLists();
  renderHome();
  renderMovements();
  setupReportGuideControls();
  renderReports();
  renderRegisters();
  renderAlerts();
  updateHomeAlertIndicator();
  setTimeout(renderCharts, 50);
}
let currentPageId = "inicio";
const pageTrail = [];

function activeDialog() {
  return [...document.querySelectorAll("dialog[open]")].at(-1) || null;
}
function requestDialogClose(dialog) {
  if (!dialog?.open) return false;
  const cancelEvent = new Event("cancel", { cancelable: true });
  const shouldClose = dialog.dispatchEvent(cancelEvent);
  if (shouldClose && dialog.open) dialog.close();
  return true;
}
function go(id, options = {}) {
  if (!document.getElementById(id)) return;
  if (id !== currentPageId && !options.fromBack && !options.replace) {
    pageTrail.push(currentPageId);
  }
  currentPageId = id;
  $$(".page").forEach((p) => p.classList.toggle("active", p.id === id));
  $$("nav button").forEach((b) =>
    b.classList.toggle("active", b.dataset.page === id),
  );
  if (id === "graficos") { ensureChartPeriodDefaults(true); setTimeout(renderCharts, 50); }
  if (id === "relatorios") { ensureReportGuideDefaults(true); showReportMenu(); setTimeout(renderReports, 20); }
  if (id === "cadastros") showRegisterHub();
  if (id === "alertas") renderAlerts();
  window.scrollTo({ top: 0, behavior: options.instant ? "auto" : "smooth" });
}
function navigateBack() {
  const dialog = activeDialog();
  if (dialog) return requestDialogClose(dialog);
  const previous = pageTrail.pop();
  if (previous && previous !== currentPageId) {
    go(previous, { fromBack: true });
    return true;
  }
  if (currentPageId !== "inicio") {
    go("inicio", { fromBack: true });
    return true;
  }
  return false;
}
$$("[data-page]").forEach((b) => (b.onclick = () => go(b.dataset.page)));
$$("[data-go]").forEach((b) => (b.onclick = () => go(b.dataset.go)));
$$("[data-back]").forEach((b) => (b.onclick = navigateBack));
const homeAlertIndicator = $("#homeAlertIndicator");
if (homeAlertIndicator) homeAlertIndicator.onclick = () => {
  const vehicle = selectedVehicleObject();
  if (!vehicle || vehicle.ativo === false) return showToast("Selecione um veículo ativo para consultar os alertas.");
  go("alertas");
};

document.addEventListener("keydown", (event) => {
  if (event.key !== "Escape") return;
  if (activeDialog()) return; // O próprio dialog dispara o evento cancel.
  if (navigateBack()) event.preventDefault();
});

window.addEventListener("popstate", () => {
  navigateBack();
  history.pushState({ mycar: true }, "", window.location.href);
});
if (!history.state?.mycar) history.replaceState({ mycar: true }, "", window.location.href);
history.pushState({ mycar: true }, "", window.location.href);

// A Activity nativa do Android chama esta função para decidir se deve
// navegar internamente ou iniciar a confirmação de saída do aplicativo.
window.myCarHandleAndroidBack = function myCarHandleAndroidBack() {
  return navigateBack();
};

const headerMenu = $("#headerMenu"), menuBtn = $("#menuBtn");
menuBtn.onclick = (event) => {
  event.stopPropagation();
  headerMenu.hidden = !headerMenu.hidden;
  menuBtn.setAttribute("aria-expanded", String(!headerMenu.hidden));
};
$$("[data-menu-page]").forEach((button) => button.onclick = () => {
  headerMenu.hidden = true;
  menuBtn.setAttribute("aria-expanded", "false");
  go(button.dataset.menuPage);
});
document.addEventListener("click", (event) => {
  if (!event.target.closest(".header-menu-wrap")) {
    headerMenu.hidden = true;
    menuBtn.setAttribute("aria-expanded", "false");
  }
});
function updateEntryLists() {
  const f = $("#entryForm"),
    oldGroup = f.grupo.value,
    categories = [...new Set(registers.map((r) => r.grupo).filter(Boolean))];
  f.grupo.innerHTML = categories.map((c) => `<option>${c}</option>`).join("");
  if (categories.includes(oldGroup)) f.grupo.value = oldGroup;
  refreshMovementItemOptions();
  const fuel = f.grupo.value === "COMBUSTÍVEL";
  $("#fuelFields").hidden = !fuel;
  $("#tankCompleteField").hidden = !fuel;
}
const ENTRY_GROUP_NAMES = {
  "COMBUSTÍVEL": "Abastecimento",
  "MANUTENÇÃO": "Manutenção",
  "ADMINISTRATIVO": "Administrativo",
  "RECEITA": "Receita",
};
function configureEntryGroup(group, editing = false) {
  const f = $("#entryForm"), name = ENTRY_GROUP_NAMES[group] || group;
  f.grupo.value = group;
  $("#entryGroupName").textContent = name;
  $("#entryGroupEyebrow").textContent = "Lançamento";
  $("#entryTitle").textContent = editing ? `Alterar ${name.toLowerCase()}` : name;
  $("#changeEntryGroup").hidden = editing;
  $("#entrySupplierLabel").textContent = group === "COMBUSTÍVEL" ? "Posto" : group === "RECEITA" ? "Pagador" : "Fornecedor";
  $("#entrySupplierHint").textContent = "Não obrigatório";
  $("#entryPaymentLabel").textContent = group === "RECEITA" ? "Forma de recebimento" : "Forma de pagamento";
  $("#entryOptionalSummary").textContent = group === "COMBUSTÍVEL"
    ? "Posto, motorista, pagamento e observação"
    : group === "RECEITA" ? "Pagador, motorista, recebimento e observação" : "Fornecedor, motorista, pagamento e observação";
  updateEntryLists();
}
function groupRegisters(group) {
  return alpha(registers.filter((r) => r.grupo === group && r.ativo !== false), "item");
}
function entryContextTitle(type) {
  const group = $("#entryForm").grupo.value;
  if (type === "ITEM") {
    if (group === "COMBUSTÍVEL") return "Selecionar combustível";
    if (group === "MANUTENÇÃO") return "Selecionar tipo de manutenção";
    if (group === "ADMINISTRATIVO") return "Selecionar item administrativo / categoria";
    return "Selecionar tipo de receita";
  }
  if (type === "MOTORISTA") return "Selecionar motorista";
  if (type === "FORNECEDOR") return group === "COMBUSTÍVEL" ? "Selecionar posto" : group === "RECEITA" ? "Selecionar pagador" : "Selecionar fornecedor";
  return group === "RECEITA" ? "Selecionar forma de recebimento" : "Selecionar forma de pagamento";
}
function entryContextNewLabel(type) {
  const group = $("#entryForm").grupo.value;
  if (type === "ITEM") {
    if (group === "COMBUSTÍVEL") return "＋ Incluir novo combustível";
    if (group === "MANUTENÇÃO") return "＋ Incluir novo item de manutenção";
    if (group === "ADMINISTRATIVO") return "＋ Incluir novo item administrativo";
    return "＋ Incluir novo tipo de receita";
  }
  if (type === "FORNECEDOR") return group === "RECEITA" ? "＋ Incluir novo pagador" : "＋ Incluir novo fornecedor";
  if (type === "FORMA_PAGAMENTO") return group === "RECEITA" ? "＋ Incluir nova forma de recebimento" : "＋ Incluir nova forma de pagamento";
  return "";
}
function syncEntrySelectTrigger(select) {
  if (!select) return;
  const row = select.closest(".entry-item-select-row,.entry-lookup-row,.entry-driver-row");
  const trigger = row?.querySelector(".entry-select-trigger");
  if (!trigger) return;
  const value = select.value || "";
  const selectedOption = select.options?.[select.selectedIndex];
  const fallback = select.classList.contains("movement-item-select") ? "Selecione um item" : "Não informado";
  const label = value || selectedOption?.textContent || fallback;
  const target = trigger.querySelector(".entry-trigger-value");
  if (target) target.textContent = label;
  trigger.classList.toggle("is-empty", !value);
}
function syncAllEntrySelectTriggers() {
  $$("#entryForm select.entry-native-select,#movementItems .movement-item-select").forEach(syncEntrySelectTrigger);
}
function movementItemRow(data = {}) {
  const row = document.createElement("div");
  const group = $("#entryForm").grupo.value;
  const fuel = group === "COMBUSTÍVEL";
  const itemLabel = fuel ? "Combustível" :
    group === "MANUTENÇÃO" ? "Tipo de manutenção" :
    group === "ADMINISTRATIVO" ? "Item administrativo / categoria" : "Tipo de receita";
  row.className = "movement-item-row entry-movement-row";
  row.dataset.itemId = data.id || "";
  row.dataset.rateioMeses = Number(data.rateio_qtd_meses) > 1 ? String(data.rateio_qtd_meses) : "";
  row.innerHTML = `<div class="entry-item-select-row"><div class="entry-item-field"><span class="entry-field-label">${itemLabel} *</span><select class="movement-item-select entry-native-select" required hidden></select><button type="button" class="entry-select-trigger movement-item-trigger" data-context-type="ITEM"><span class="entry-trigger-value">Selecione um item</span><i>›</i></button></div><button type="button" class="remove-movement-item" aria-label="Remover item">×</button></div>${fuel
    ? `<div class="fuel-values-grid"><label>Preço/L *<input class="movement-item-price" type="text" inputmode="numeric" required placeholder="0,00"></label><label>Total *<input class="movement-item-value" type="text" inputmode="numeric" required placeholder="0,00"></label><label>Litros<output class="movement-item-liters">0,000</output></label></div>`
    : `<div class="entry-value-rate-row"><label class="entry-single-value">Valor (R$) *<input class="movement-item-value" type="text" inputmode="numeric" required placeholder="0,00"></label>${["MANUTENÇÃO", "ADMINISTRATIVO"].includes(group) ? '<button type="button" class="item-rate-button" title="Ratear por competência" aria-label="Ratear valor do item">R</button>' : ""}</div><small class="item-rate-summary" hidden></small>`}`;
  const select = row.querySelector(".movement-item-select"),
    trigger = row.querySelector(".movement-item-trigger"),
    value = row.querySelector(".movement-item-value"),
    price = row.querySelector(".movement-item-price");
  fillMovementItemSelect(select, data.item);
  trigger.onclick = () => openContextSelector("ITEM", select);
  if (data.valor !== undefined && data.valor !== null && data.valor !== "") value.value = num(data.valor, 2);
  if (price && data.preco_unitario !== undefined && data.preco_unitario !== null && data.preco_unitario !== "") price.value = num(data.preco_unitario, 2);
  [value, price].filter(Boolean).forEach((input) => input.addEventListener("input", () => {
    formatMoneyInput(input);
    updateMovementTotal();
    updateItemLiters(row);
  }));
  const rateButton = row.querySelector(".item-rate-button");
  const refreshRateSummary = () => {
    const months = Number(row.dataset.rateioMeses) || 0;
    const total = moneyInputNumber(value.value);
    const summary = row.querySelector(".item-rate-summary");
    if (!rateButton || !summary) return;
    rateButton.classList.toggle("active", months > 1);
    rateButton.textContent = months > 1 ? "R ✓" : "R";
    summary.hidden = months <= 1;
    if (months > 1) summary.textContent = `${months} competências · ${money(total / months)}/mês (diferença de centavos na última)`;
  };
  if (rateButton) rateButton.onclick = () => openRateDialog(row, value, refreshRateSummary);
  value.addEventListener("input", refreshRateSummary);
  refreshRateSummary();
  row.querySelector(".remove-movement-item").onclick = () => {
    row.remove();
    if (!$("#movementItems").children.length) addMovementItem();
    refreshMovementItemControls();
    updateMovementTotal();
  };
  $("#movementItems").appendChild(row);
  syncEntrySelectTrigger(select);
  updateItemLiters(row);
  refreshMovementItemControls();
}

function openRateDialog(row, valueInput, refreshRateSummary) {
  const dialog = $("#rateDialog"), form = $("#rateForm"), monthsInput = $("#rateMonths"), error = $("#rateError");
  const hadRate = Number(row.dataset.rateioMeses) > 1;
  monthsInput.value = String(hadRate ? Number(row.dataset.rateioMeses) : 12);
  $("#removeRate").hidden = !hadRate;
  error.textContent = "";
  const updatePreview = () => {
    const months = Math.min(120, Math.max(2, Number(monthsInput.value) || 2));
    const total = moneyInputNumber(valueInput.value);
    const dateValue = $("#entryForm").data.value || localDateISO();
    const start = new Date(`${dateValue.slice(0, 7)}-01T12:00:00`);
    const end = new Date(start.getFullYear(), start.getMonth() + months - 1, 1, 12);
    const monthLabel = (date) => date.toLocaleDateString("pt-BR", { month: "short", year: "numeric" }).replace(". de ", "/").replace(" de ", "/");
    $("#rateItemValue").textContent = money(total);
    $("#rateStart").textContent = monthLabel(start);
    $("#rateEnd").textContent = monthLabel(end);
    $("#rateInstallment").textContent = `${money(total / months)}/mês`;
  };
  monthsInput.oninput = updatePreview;
  $("#rateMinus").onclick = () => { monthsInput.value = String(Math.max(2, (Number(monthsInput.value) || 2) - 1)); updatePreview(); };
  $("#ratePlus").onclick = () => { monthsInput.value = String(Math.min(120, (Number(monthsInput.value) || 2) + 1)); updatePreview(); };
  $("#closeRateDialog").onclick = $("#cancelRate").onclick = () => dialog.close();
  $("#removeRate").onclick = () => { row.dataset.rateioMeses = ""; refreshRateSummary(); dialog.close(); };
  form.onsubmit = (event) => {
    event.preventDefault();
    const months = Number(monthsInput.value);
    if (!Number.isInteger(months) || months < 2 || months > 120) {
      error.textContent = "Informe uma quantidade entre 02 e 120 meses.";
      monthsInput.focus();
      return;
    }
    row.dataset.rateioMeses = String(months);
    refreshRateSummary();
    dialog.close();
  };
  updatePreview();
  dialog.showModal();
  setTimeout(() => { monthsInput.focus(); monthsInput.select(); }, 50);
}
function fillMovementItemSelect(select, selected = "") {
  const group = $("#entryForm").grupo.value;
  const allowed = groupRegisters(group);
  select.innerHTML = allowed.length
    ? allowed.map((r) => `<option value="${esc(r.item)}">${esc(r.item)}</option>`).join("")
    : '<option value="">Nenhum item cadastrado</option>';
  if (allowed.some((r) => r.item === selected)) {
    select.value = selected;
    syncEntrySelectTrigger(select);
    return;
  }
  const preferred = group === "COMBUSTÍVEL"
    ? allowed.find((r) => normalizeText(r.item).includes("etanol")) || allowed.find((r) => r.padrao)
    : allowed.find((r) => r.padrao);
  if (preferred) select.value = preferred.item;
  syncEntrySelectTrigger(select);
}
function refreshMovementItemOptions() {
  $$("#movementItems .movement-item-select").forEach((select) => {
    const previous = select.value;
    fillMovementItemSelect(select, previous);
  });
  refreshMovementItemControls();
}
function addMovementItem(data = {}) {
  movementItemRow(data);
}
function refreshMovementItemControls() {
  const rows = $$("#movementItems .movement-item-row"), locked = rows.length > 1;
  $("#entryForm").grupo.disabled = locked;
  $("#entryForm").grupo.classList.toggle("group-locked", locked);
  $("#groupLockHelp").textContent = locked
    ? "Todos os itens deste movimento pertencem ao mesmo grupo."
    : "Selecione um item cadastrado para o grupo.";
  rows.forEach((row) => row.querySelector(".remove-movement-item").disabled = rows.length === 1);
}
function updateItemLiters(row) {
  const output = row.querySelector(".movement-item-liters"),
    fuel = $("#entryForm").grupo.value === "COMBUSTÍVEL",
    price = moneyInputNumber(row.querySelector(".movement-item-price")?.value),
    value = moneyInputNumber(row.querySelector(".movement-item-value")?.value);
  if (fuel && output) output.textContent = price > 0 ? num(value / price, 3) : "0,000";
}
function updateMovementTotal() {
  const total = $$("#movementItems .movement-item-value")
    .reduce((sum, input) => sum + moneyInputNumber(input.value), 0);
  $("#movementTotal").textContent = money(total);
}
function movementDateTimeForEdit(date, current = null) {
  const day = String(date || "").slice(0, 10);
  const savedTime = String(current?.data_hora || "").slice(11, 19);
  const time = /^\d{2}:\d{2}:\d{2}$/.test(savedTime) ? savedTime : "12:00:00";
  return day ? `${day}T${time}` : "";
}
function kmBounds(date, vehicle, excludeId = "") {
  const current = excludeId
      ? movements.find((m) => sameMovement(m, excludeId)) || null
      : null,
    referenceDateTime = movementDateTimeForEdit(date, current),
    referenceTime = new Date(referenceDateTime).getTime(),
    referenceOrder = current
      ? (+current.ordem_lancamento || 0)
      : Number.MAX_SAFE_INTEGER,
    ms = movements.filter(
      (m) =>
        !sameMovement(m, excludeId) &&
        m.veiculo === vehicle &&
        +m.hodometro_km > 0 &&
        m.data_hora,
    ),
    position = (m) => ({
      time: new Date(m.data_hora).getTime(),
      order: +m.ordem_lancamento || 0,
    }),
    isBefore = (m) => {
      const p = position(m);
      return p.time < referenceTime ||
        (p.time === referenceTime && p.order < referenceOrder);
    },
    isAfter = (m) => {
      const p = position(m);
      return p.time > referenceTime ||
        (p.time === referenceTime && p.order > referenceOrder);
    };
  const previous = ms
    .filter(isBefore)
    .sort((a, b) => new Date(b.data_hora) - new Date(a.data_hora) ||
      (+b.ordem_lancamento || 0) - (+a.ordem_lancamento || 0))[0];
  const next = ms
    .filter(isAfter)
    .sort((a, b) => new Date(a.data_hora) - new Date(b.data_hora) ||
      (+a.ordem_lancamento || 0) - (+b.ordem_lancamento || 0))[0];
  return { prev: previous, next, referenceDateTime };
}
function updateKm() {
  const f = $("#entryForm"),
    id = f.movementId.value,
    current = id ? movements.find((m) => sameMovement(m, id)) : null,
    v = current
      ? vehicles.find((x) => x.nome === current.veiculo)
      : vehicles.find((x) => x.nome === selectedVehicleName()),
    b = kmBounds(f.data.value, v?.nome || "", id),
    base = b.prev?.hodometro_km ?? v?.kmInicial ?? 0;
  $("#lastKm").textContent = `${intFmt(base)} km`;
  if (!current && !String(f.km.value || "").replace(/\D/g, ""))
    f.km.value = intFmt(base);
  $("#kmRule").textContent = b.next
    ? `Hodômetro permitido: de ${intFmt(base)} a ${intFmt(b.next.hodometro_km)} km.`
    : `O hodômetro não pode ser menor que ${intFmt(base)} km.`;
}
function openEntry(id = "", presetGroup = "") {
  const f = $("#entryForm"),
    currentRows = id ? movements.filter((m) => sameMovement(m, id)) : [],
    current = currentRows[0] || null,
    v = current
      ? vehicles.find((x) => x.nome === current.veiculo)
      : selectedVehicleObject(),
    err = $("#formError");
  entryReturnPage = currentPageId || "movimentos";
  f.reset();
  f.movementId.value = id;
  $("#movementItems").innerHTML = "";
  err.textContent = "";
  if (!v || (!current && v.ativo === false)) {
    alert("Selecione um veículo ativo na tela inicial antes de realizar novos lançamentos.");
    go("inicio");
    return;
  }
  $("#entryVehicleName").textContent = `${v.nome}${current && v.ativo === false ? " · inativo (histórico editável)" : ""}`;
  f.data.value = current
    ? String(current.data_hora).slice(0, 10)
    : localDateISO();
  updateEntryLists();
  if (current) {
    configureEntryGroup(current.grupo, true);
    currentRows.forEach((row) => addMovementItem(row));
    f.km.value = intFmt(current.hodometro_km);
    f.tanqueCompleto.checked = current.tanque_completo !== "NAO";
    f.motorista.value = current.motorista || "";
    f.observacao.value =
      current.observacao === "N.I." ? "" : current.observacao || "";
    f.latitude.value = current.latitude ?? "";
    f.longitude.value = current.longitude ?? "";
    f.precisaoGps.value = current.precisao_gps_m ?? "";
  } else {
    configureEntryGroup(presetGroup || GROUPS[0], false);
    addMovementItem();
  }
  fillDrivers();
  fillOperationalLists();
  if (current) {
    f.motorista.value = current.motorista || "";
    f.fornecedor.value = current.fornecedor || current.local || "";
    f.formaPagamento.value = current.forma_pagamento || "";
    f.incluirIndicadores.checked = current.incluir_indicadores !== "NAO";
  }
  $("#entryGpsStatus").textContent = validCoordinate(f.latitude.value)
    ? `Localização salva${f.precisaoGps.value ? ` · precisão aproximada de ${Math.round(f.precisaoGps.value)} m` : ""}.`
    : "Localização opcional. O GPS é acionado somente ao tocar no botão.";
  updateMapLink($("#openEntryMap"), f.latitude.value, f.longitude.value);
  updateKm();
  updateMovementTotal();
  syncAllEntrySelectTriggers();
  $("#entryDialog").showModal();
}
function openEntryGroupChooser() {
  const v = selectedVehicleObject();
  if (!v || v.ativo === false) {
    openEntry();
    return;
  }
  $("#entryGroupDialog").showModal();
}
$("#closeEntryGroups").onclick = () => $("#entryGroupDialog").close();
$$("[data-entry-group]").forEach((button) => button.onclick = () => {
  if ($("#entryGroupDialog").open) $("#entryGroupDialog").close();
  openEntry("", button.dataset.entryGroup);
});
$("#changeEntryGroup").onclick = () => {
  $("#entryDialog").close();
  go("movimentos");
};
$("#entryForm [name=grupo]").onchange = () => {
  updateEntryLists();
  $$("#movementItems .movement-item-value,.movement-item-price").forEach((input) => input.value = "");
  updateMovementTotal();
};
$("#addMovementItem").onclick = () => addMovementItem();
$("#useCurrentLocation").onclick = () => {
  const f = $("#entryForm"),
    button = $("#useCurrentLocation"),
    status = $("#entryGpsStatus");
  requestCurrentPosition(button, status, async (coords) => {
    f.latitude.value = Number(coords.latitude).toFixed(7);
    f.longitude.value = Number(coords.longitude).toFixed(7);
    f.precisaoGps.value = Math.round(coords.accuracy || 0);
    updateMapLink($("#openEntryMap"), f.latitude.value, f.longitude.value);
    const nearest = nearestSupplier(coords.latitude, coords.longitude);
    if (nearest && nearest.distance <= gpsSupplierRadiusMeters()) {
      f.fornecedor.value = nearest.supplier.nome;
      status.textContent = `${nearest.supplier.nome} sugerido a aproximadamente ${Math.round(nearest.distance)} m. Confirme ou altere o fornecedor.`;
    } else {
      status.textContent = "Coordenadas obtidas. Procurando o fornecedor do local…";
      try {
        const place = await reverseGeocode(coords.latitude, coords.longitude);
        const detected = selectDetectedSupplier(f.fornecedor, place.supplier);
        status.textContent = detected
          ? `${place.supplier} identificado pelo GPS${place.address ? ` · ${place.address}` : ""}. Confirme ou altere.`
          : `Coordenadas salvas${place.address ? ` · ${place.address}` : ""}. O nome do fornecedor não foi identificado; selecione-o manualmente.`;
      } catch (error) {
        status.textContent = nearest
          ? `Coordenadas salvas. O fornecedor cadastrado mais próximo está a ${Math.round(nearest.distance)} m; selecione-o manualmente.`
          : "Coordenadas salvas. A consulta automática do fornecedor está indisponível; selecione-o manualmente.";
      }
    }
  });
};
$("#entryForm [name=data]").onchange = () => {
  const f = $("#entryForm");
  if (!f.movementId.value) f.km.value = "";
  updateKm();
};
$("#entryForm [name=km]").oninput = (e) =>
  (e.target.value = intFmt(String(e.target.value).replace(/\D/g, "")));
function moneyInputNumber(value) {
  const digits = String(value ?? "").replace(/\D/g, "");
  return digits ? Number(digits) / 100 : 0;
}
function formatMoneyInput(input) {
  const digits = String(input.value ?? "").replace(/\D/g, "");
  input.value = digits ? num(Number(digits) / 100, 2) : "";
}
const entryDialogElement = $("#entryDialog"),
  nativeShowEntry = entryDialogElement.showModal.bind(entryDialogElement);
entryDialogElement.showModal = () => {
  nativeShowEntry();
};
function entryHasChanges() {
  const f = $("#entryForm");
  return Boolean(f.movementId.value || f.observacao.value.trim() ||
    $$("#movementItems .movement-item-value").some((x) => x.value.trim()) ||
    f.latitude.value || f.longitude.value);
}
function cancelEntry() {
  if (entryHasChanges() && !confirm("Descartar as alterações deste lançamento?")) return;
  entryDialogElement.close();
}
$$('#entryForm [value="cancel"]').forEach((button) => {
  button.type = "button";
  button.onclick = cancelEntry;
});
entryDialogElement.addEventListener("cancel", (event) => {
  event.preventDefault();
  cancelEntry();
});
const contextSelectorDialog = $("#contextSelectorDialog");
function contextRecords() {
  if (!entryContext) return [];
  if (entryContext.type === "ITEM") return groupRegisters($("#entryForm").grupo.value)
    .map((row) => ({ value: row.item, label: row.item, meta: row.padrao ? "Padrão" : "" }));
  const source = entryContext.type === "MOTORISTA"
    ? drivers
    : entryContext.type === "FORMA_PAGAMENTO"
      ? paymentMethods.filter((row) => row.ativo !== false)
      : suppliers.filter((row) => row.ativo !== false);
  return alpha(source).map((row) => ({
    value: row.nome,
    label: row.nome,
    meta: [row.padrao ? "Padrão" : "", row.local || ""].filter(Boolean).join(" · "),
  }));
}
function renderContextSelector() {
  if (!entryContext) return;
  const term = $("#contextSelectorSearch").value.trim().toLocaleLowerCase("pt-BR");
  const selected = entryContext.target?.value || "";
  const rows = contextRecords().filter((row) =>
    `${row.label} ${row.meta}`.toLocaleLowerCase("pt-BR").includes(term));
  const selectorList = $("#contextSelectorList");
  const newButton = document.getElementById("newContextRegister");
  selectorList.innerHTML = rows.length
    ? rows.map((row) => `<button type="button" class="context-choice${row.value === selected ? " is-selected" : ""}" data-value="${esc(row.value)}"><span class="context-radio" aria-hidden="true"></span><span class="context-choice-copy"><b>${esc(row.label)}</b>${row.meta ? `<small>${esc(row.meta)}</small>` : ""}</span></button>`).join("")
    : '<div class="empty-state">Nenhuma opção cadastrada.</div>';
  // A inclusão fica dentro da área rolável e sempre ao final da lista,
  // evitando que desapareça em listas extensas ou telas menores.
  if (newButton && !newButton.hidden) selectorList.appendChild(newButton);
  $$("#contextSelectorList .context-choice").forEach((button) => button.onclick = () => {
    entryContext.target.value = button.dataset.value;
    syncEntrySelectTrigger(entryContext.target);
    contextSelectorDialog.close();
    entryContext = null;
  });
}
function openContextSelector(type, target) {
  entryContext = { type, target };
  $("#contextSelectorTitle").textContent = entryContextTitle(type);
  const selectorHint = $("#contextSelectorHint");
  if (selectorHint) { selectorHint.textContent = ""; selectorHint.hidden = true; }
  const newButton = $("#newContextRegister");
  newButton.hidden = type === "MOTORISTA";
  newButton.textContent = entryContextNewLabel(type);
  $("#contextSelectorSearch").value = "";
  const searchWrap = $("#contextSelectorSearch").closest("label");
  if (searchWrap) searchWrap.hidden = contextRecords().length <= 7;
  renderContextSelector();
  contextSelectorDialog.showModal();
}
function closeContextSelector() {
  if (contextSelectorDialog.open) contextSelectorDialog.close();
  entryContext = null;
}
$("#contextSelectorSearch").oninput = renderContextSelector;
$("#closeContextSelector").onclick = closeContextSelector;
$("#cancelContextSelector").onclick = closeContextSelector;
contextSelectorDialog.addEventListener("cancel", (event) => {
  event.preventDefault();
  closeContextSelector();
});
$$(".context-open[data-context-type]").forEach((button) => button.onclick = () => {
  const type = button.dataset.contextType;
  const name = type === "MOTORISTA" ? "motorista" : "fornecedor";
  openContextSelector(type, $(`#entryForm [name=${name}]`));
});
$$("#entryForm .entry-select-trigger[data-field]").forEach((button) => button.onclick = () => {
  const target = $(`#entryForm [name=${button.dataset.field}]`);
  openContextSelector(button.dataset.contextType, target);
});
$("#newContextRegister").onclick = () => {
  if (!entryContext || entryContext.type === "MOTORISTA") return;
  const type = entryContext.type;
  contextSelectorDialog.close();
  $("#registerGroup").value = type;
  openRegister();
  if (type === "ITEM") {
    const groupField = $("#registerForm [name=grupo]");
    groupField.value = $("#entryForm").grupo.value;
    groupField.disabled = true;
  }
};
$("#entryForm").onsubmit = (e) => {
  if (e.submitter?.value === "cancel") return;
  e.preventDefault();
  const f = e.target,
    d = Object.fromEntries(new FormData(f)),
    id = f.movementId.value,
    currentRows = id ? movements.filter((m) => sameMovement(m, id)) : [],
    current = currentRows[0] || null,
    kmText = String(d.km || "").replace(/\D/g, ""),
    km = +kmText,
    err = $("#formError"),
    v = current
      ? vehicles.find((x) => x.nome === current.veiculo)
      : vehicles.find((x) => x.nome === selectedVehicleName());
  d.grupo = f.grupo.value;
  const kmInput = f.km;
  const showEntryError = (message, invalidKm = false) => {
    err.textContent = message;
    err.classList.add("show");
    kmInput.classList.toggle("field-invalid", invalidKm);
    (invalidKm ? kmInput : err).scrollIntoView({ behavior: "smooth", block: "center" });
    if (invalidKm) kmInput.focus();
  };
  err.textContent = "";
  err.classList.remove("show");
  kmInput.classList.remove("field-invalid");
  if (!v || (!current && v.ativo === false)) {
    showEntryError("Selecione um veículo ativo na tela inicial antes de realizar novos lançamentos.");
    return;
  }
  d.veiculo = v.nome;
  if (!d.data || !kmText) {
    showEntryError("Data e hodômetro são obrigatórios para salvar.", true);
    return;
  }
  const itemRows = $$("#movementItems .movement-item-row").map((row) => ({
    sourceId: row.dataset.itemId,
    item: row.querySelector(".movement-item-select").value,
    valor: moneyInputNumber(row.querySelector(".movement-item-value").value),
    preco: moneyInputNumber(row.querySelector(".movement-item-price")?.value),
    rateioMeses: Number(row.dataset.rateioMeses) || 0,
  }));
  const allowedItems = new Set(groupRegisters(d.grupo).map((r) => r.item));
  if (!GROUPS.includes(d.grupo) || !itemRows.length) {
    showEntryError("Grupo e pelo menos um item de lançamento são obrigatórios.");
    return;
  }
  if (itemRows.some((row) => !row.item || !allowedItems.has(row.item))) {
    showEntryError("Existe item incompatível com o Grupo selecionado. Remova-o ou escolha um item pertencente a este Grupo.");
    return;
  }
  if (itemRows.some((row) => !(row.valor >= 0))) {
    showEntryError("Informe o valor de todos os itens.");
    return;
  }
  if (d.grupo === "COMBUSTÍVEL" && itemRows.some((row) => !(row.preco > 0 && row.valor >= 0))) {
    showEntryError("Informe o preço por litro de cada abastecimento. O valor total pode ser R$ 0,00.");
    return;
  }
  const b = kmBounds(d.data, d.veiculo, id);
  if (b.prev && km < +b.prev.hodometro_km) {
    showEntryError(`Quilometragem inconsistente com a data. Para ${dateBR(d.data)}, o hodômetro deve ser igual ou superior a ${intFmt(b.prev.hodometro_km)} km.`, true);
    return;
  }
  if (b.next && km > +b.next.hodometro_km) {
    const minimum = b.prev ? +b.prev.hodometro_km : +v.kmInicial || 0;
    showEntryError(`Quilometragem inconsistente com a data. Informe um valor entre ${intFmt(minimum)} km e ${intFmt(b.next.hodometro_km)} km. Existe um lançamento posterior registrado.`, true);
    return;
  }
  const duplicate = movements.some((m) =>
    !sameMovement(m, id) &&
    m.veiculo === d.veiculo &&
    String(m.data_hora).slice(0, 10) === d.data &&
    itemRows.some((row) => row.item === m.item && Number(row.valor) === Number(m.valor)));
  if (duplicate && !confirm("Já existe lançamento para o mesmo veículo, data, item e valor. Deseja salvar mesmo assim?")) return;
  const prevAny = b.prev,
    dist = Math.max(0, km - (+prevAny?.hodometro_km || +v.kmInicial || 0)),
    prevFuel = [...movements]
      .filter(
        (m) =>
          !sameMovement(m, id) &&
          m.veiculo === d.veiculo &&
          m.grupo === "COMBUSTÍVEL" &&
          String(m.data_hora).slice(0, 10) <= d.data &&
          +m.hodometro_km <= km,
      )
      .sort((a, b) => b.hodometro_km - a.hodometro_km)[0],
    fuelDist = Math.max(0, km - (+prevFuel?.hodometro_km || +v.kmInicial || 0)),
    movementId = id || crypto.randomUUID(),
    common = {
      movimento_id: movementId,
      ordem_lancamento:
        current?.ordem_lancamento ||
        Math.max(0, ...movements.map((m) => +m.ordem_lancamento || 0)) + 1,
      data_hora: movementDateTimeForEdit(d.data, current),
      hodometro_km: km,
      grupo: d.grupo,
      distancia_km: dist,
      distancia_abastecimento_km: d.grupo === "COMBUSTÍVEL" ? fuelDist : null,
      tanque_completo:
        d.grupo === "COMBUSTÍVEL"
          ? d.tanqueCompleto
            ? "SIM"
            : "NAO"
          : "N.I.",
      motorista: d.motorista || "N.I.",
      fornecedor: d.fornecedor || NI,
      local: d.fornecedor || NI,
      latitude: validCoordinate(d.latitude) ? Number(d.latitude) : "",
      longitude: validCoordinate(d.longitude) ? Number(d.longitude) : "",
      precisao_gps_m: d.precisaoGps ? Number(d.precisaoGps) : "",
      localizacao_confirmada: validCoordinate(d.latitude) ? "SIM" : "NAO",
      forma_pagamento: d.formaPagamento || "",
      incluir_indicadores: d.incluirIndicadores ? "SIM" : "NAO",
      veiculo: d.veiculo,
      observacao: d.observacao || "N.I.",
      origem: current?.origem || "APP",
    };
  const replacements = itemRows.map((row, index) => {
    const litros = d.grupo === "COMBUSTÍVEL" ? row.valor / row.preco : null;
    return {
      ...common,
      id: row.sourceId || crypto.randomUUID(),
      ordem_item: index + 1,
      item: row.item,
      item_id: registers.find((r) => r.grupo === d.grupo && r.item === row.item)?.id || "",
      valor: row.valor,
      quantidade_litros: litros,
      preco_unitario: d.grupo === "COMBUSTÍVEL" ? row.preco : null,
      rateio_ativo: ["MANUTENÇÃO", "ADMINISTRATIVO"].includes(d.grupo) && row.rateioMeses > 1,
      rateio_qtd_meses: ["MANUTENÇÃO", "ADMINISTRATIVO"].includes(d.grupo) && row.rateioMeses > 1 ? row.rateioMeses : null,
      rateio_competencia_inicial: ["MANUTENÇÃO", "ADMINISTRATIVO"].includes(d.grupo) && row.rateioMeses > 1 ? d.data.slice(0, 7) : null,
      rateio_valor_base_centavos: ["MANUTENÇÃO", "ADMINISTRATIVO"].includes(d.grupo) && row.rateioMeses > 1 ? Math.round(row.valor * 100) : null,
      distancia_abastecimento_km: d.grupo === "COMBUSTÍVEL" && index === 0 ? fuelDist : null,
      consumo_km_l:
        d.grupo === "COMBUSTÍVEL" && d.tanqueCompleto && index === 0 && fuelDist && litros
          ? fuelDist / litros
          : null,
    };
  });
  const stateBeforeSave = cloneDataState();
  try {
    const previousRows = current
      ? movements.filter((m) => sameMovement(m, movementId))
      : [];
    if (current) movements = movements.filter((m) => !sameMovement(m, movementId));
    movements.push(...replacements);
    recalculateDistances();
    synchronizeAlertsFromMaintenance([...previousRows, ...replacements]);
    save();
  } catch (error) {
    restoreDataState(stateBeforeSave, false);
    try { recalculateDistances(); } catch (_) {}
    console.error("Falha ao salvar o lançamento:", error);
    err.textContent = "Não foi possível salvar o lançamento. Os dados digitados foram preservados; verifique os campos e tente novamente.";
    return;
  }
  err.textContent = "";
  $("#entryDialog").close();
  const destination = document.getElementById(entryReturnPage) ? entryReturnPage : "consultaMovimentos";
  go(destination, { replace: true });
  try {
    evaluateAlerts(true);
    showToast("Lançamento salvo com sucesso.");
  } catch (error) {
    console.error("O lançamento foi salvo, mas as rotinas complementares falharam:", error);
    showToast("Lançamento salvo. A atualização complementar dos alertas deverá ser conferida.", "warning");
  }
};
function fillRegisterForm() {
  const f = $("#registerForm");
  f.grupo.innerHTML = GROUPS.map((group) => `<option>${group}</option>`).join("");
}
function updateFuelReferenceFields() {
  const f = $("#registerForm");
  const engine = f.motorizacao?.value || "FLEX";
  $$("[data-fuel-reference]").forEach((section) => {
    section.hidden = engine !== "FLEX" && section.dataset.fuelReference !== engine;
  });
}
function openRegister(id = "") {
  const group = $("#registerGroup").value,
    f = $("#registerForm");
  f.reset();
  f.querySelectorAll("details").forEach((section) => (section.open = false));
  f.id.value = "";
  f.grupoCadastro.value = group;
  $("#subcatFields").style.display =
    group === "ITEM" ? "block" : "none";
  $("#simpleFields").style.display =
    group === "ITEM" ? "none" : "block";
  $("#simpleLabel").firstChild.textContent =
    group === "VEICULO" ? "Veículo *" :
    group === "MOTORISTA" ? "Motorista *" :
    group === "FORNECEDOR" ? "Fornecedor *" : "Forma de pagamento *";
  $("#vehicleExtraFields").style.display = group === "VEICULO" ? "block" : "none";
  $("#driverExtraFields").style.display = group === "MOTORISTA" ? "block" : "none";
  $("#supplierExtraFields").style.display = group === "FORNECEDOR" ? "block" : "none";
  [...f.elements].forEach((field) => {
    if (!field.name || ["id", "grupoCadastro"].includes(field.name)) return;
    const section = field.closest("#subcatFields,#simpleFields,#vehicleExtraFields,#driverExtraFields,#supplierExtraFields");
    field.disabled = Boolean(section && getComputedStyle(section).display === "none");
  });

  $("#activeVehicleField").style.display =
    group === "VEICULO" ? "flex" : "none";
  fillRegisterForm();
  f.motorizacao.onchange = updateFuelReferenceFields;
  let obj;
  if (group === "ITEM") obj = registers.find((x) => x.id === id);
  else if (group === "MOTORISTA") obj = drivers.find((x) => x.id === id);
  else if (group === "VEICULO") obj = vehicles.find((x) => x.id === id);
  else if (group === "FORNECEDOR") obj = suppliers.find((x) => x.id === id);
  else obj = paymentMethods.find((x) => x.id === id);
  if (obj) {
    f.id.value = obj.id;
    if (group === "ITEM") {
      f.grupo.value = obj.grupo;
      f.item.value = obj.item;
      f.padrao.checked = obj.padrao;
    } else {
      f.nome.value = obj.nome;
      f.kmInicial.value = obj.kmInicial ? intFmt(obj.kmInicial) : "";
      f.simplePadrao.checked = !!obj.padrao;
      f.ativo.checked = obj.ativo !== false;
      if (group === "VEICULO") {
        f.placa.value = obj.placa || "";
        f.anoFabricacao.value = obj.anoFabricacao || "";
        f.anoModelo.value = obj.anoModelo || "";
        f.capacidadeTanque.value = obj.capacidadeTanque || "";
        f.motorizacao.value = obj.motorizacao || "FLEX";
        f.consumoEtanolCidade.value = obj.consumoEtanolCidade || "";
        f.consumoEtanolEstrada.value = obj.consumoEtanolEstrada || "";
        f.consumoGasolinaCidade.value = obj.consumoGasolinaCidade || obj.consumoRefCidade || "";
        f.consumoGasolinaEstrada.value = obj.consumoGasolinaEstrada || obj.consumoRefEstrada || "";
        f.consumoDieselCidade.value = obj.consumoDieselCidade || "";
        f.consumoDieselEstrada.value = obj.consumoDieselEstrada || "";
      }
      if (group === "MOTORISTA") {
        f.numeroCnh.value = obj.numeroCnh || "";
        f.grupoCnh.value = obj.grupoCnh || obj.categoriaCnh || "";
        f.validadeCnh.value = String(obj.validadeCnh || "").slice(0,10);
        f.obsMotorista.value = obj.observacao || "";
      }
      if (group === "FORNECEDOR") {
        f.localFornecedor.value = obj.local || "";
        f.latitudeFornecedor.value = obj.latitude ?? "";
        f.longitudeFornecedor.value = obj.longitude ?? "";
      }
    }
  }
  if (group === "VEICULO") updateFuelReferenceFields();
  if (group === "FORNECEDOR") {
    $("#supplierGpsStatus").textContent = validCoordinate(f.latitudeFornecedor.value)
      ? "Coordenadas cadastradas. Este fornecedor pode ser sugerido pelo GPS."
      : "Cadastre as coordenadas para permitir a sugestão automática deste fornecedor.";
    updateMapLink($("#openSupplierMap"), f.latitudeFornecedor.value, f.longitudeFornecedor.value);
  }
  $("#registerTitle").textContent = id
    ? "Alterar cadastro"
    : "Incluir cadastro";
  $("#registerDialog").showModal();
}
function finishRegisterFlow(savedValue = "") {
  const context = entryContext;
  if ($("#registerDialog").open) $("#registerDialog").close();
  if (context && $("#entryDialog").open) {
    fillDrivers();
    fillOperationalLists();
    refreshMovementItemOptions();
    if (savedValue && context.target) context.target.value = savedValue;
    syncEntrySelectTrigger(context?.target);
    syncAllEntrySelectTriggers();
    entryContext = null;
    setTimeout(() => $("#entryDialog").focus(), 0);
  } else {
    entryContext = null;
  }
}
function closeRegister() {
  finishRegisterFlow();
}
$$(".register-cancel").forEach((button) => button.onclick = closeRegister);
$("#registerDialog").addEventListener("cancel", (event) => {
  event.preventDefault();
  closeRegister();
});
function deleteRegister(id) {
  const g = $("#registerGroup").value;
  const target = g === "ITEM" ? registers.find((x) => x.id === id) : null;
  const linkedAlerts = target ? alerts.filter((item) => item.itemId === target.id) : [];
  const message = linkedAlerts.length
    ? `Este item possui ${linkedAlerts.length} alerta(s) do novo modelo. Ao excluir o item, esses alertas também serão excluídos. Os movimentos históricos serão preservados. Deseja continuar?`
    : "Excluir este cadastro?";
  if (!confirm(message)) return;
  const stateBeforeSave = cloneDataState();
  try {
    if (target) alerts = alerts.filter((item) => item.itemId !== target.id);
    if (g === "ITEM") registers = registers.filter((x) => x.id !== id);
    if (g === "MOTORISTA") drivers = drivers.filter((x) => x.id !== id);
    if (g === "VEICULO") {
      vehicles = vehicles.filter((x) => x.id !== id);
      alerts = alerts.filter((item) => item.vehicleId !== id);
    }
    if (g === "FORNECEDOR") suppliers = suppliers.filter((x) => x.id !== id);
    if (g === "FORMA_PAGAMENTO") paymentMethods = paymentMethods.filter((x) => x.id !== id);
    save();
    showToast("Cadastro excluído com sucesso.");
  } catch (error) {
    restoreDataState(stateBeforeSave);
    console.error("Falha ao excluir o cadastro:", error);
    alert("Não foi possível excluir o cadastro. Nenhum dado foi alterado.");
  }
}
$("#registerForm [name=kmInicial]").oninput = (e) =>
  (e.target.value = intFmt(String(e.target.value).replace(/\D/g, "")));
$("#addRegister").onclick = () => openRegister();
$("#useSupplierLocation").onclick = () => {
  const f = $("#registerForm"),
    button = $("#useSupplierLocation"),
    status = $("#supplierGpsStatus");
  requestCurrentPosition(button, status, async (coords) => {
    f.latitudeFornecedor.value = Number(coords.latitude).toFixed(7);
    f.longitudeFornecedor.value = Number(coords.longitude).toFixed(7);
    updateMapLink($("#openSupplierMap"), f.latitudeFornecedor.value, f.longitudeFornecedor.value);
    status.textContent = "Coordenadas obtidas. Procurando fornecedor e endereço…";
    try {
      const place = await reverseGeocode(coords.latitude, coords.longitude);
      if (!f.nome.value.trim() && place.supplier) f.nome.value = place.supplier;
      if (place.address) f.localFornecedor.value = place.address;
      status.textContent = place.supplier
        ? `${place.supplier} identificado · precisão aproximada de ${Math.round(coords.accuracy || 0)} m. Confirme os dados.`
        : `Endereço e coordenadas preenchidos · precisão aproximada de ${Math.round(coords.accuracy || 0)} m. Informe o nome do fornecedor.`;
    } catch (error) {
      status.textContent = `Coordenadas preenchidas · precisão aproximada de ${Math.round(coords.accuracy || 0)} m. Não foi possível consultar o nome/endereço agora.`;
    }
  });
};
$("#registerGroup").onchange = renderRegisters;
$("#registerSearch").oninput = renderRegisters;
$("#registerStatusFilter").onchange = renderRegisters;
function showRegisterHub() {
  $("#registerHub").hidden = false;
  $("#registerDetail").hidden = true;
  $("#registerSearch").value = "";
  $("#registerStatusFilter").value = "TODOS";
  renderRegisters();
}
function showRegisterDetail(group) {
  $("#registerGroup").value = group;
  $("#registerSearch").value = "";
  $("#registerStatusFilter").value = "TODOS";
  $("#registerHub").hidden = true;
  $("#registerDetail").hidden = false;
  renderRegisters();
  window.scrollTo({top:0,behavior:"smooth"});
}
$("#backRegisterHub").onclick = showRegisterHub;
$$("[data-register-group]").forEach(button => button.onclick = () => {
  showRegisterDetail(button.dataset.registerGroup);
});
$("#registerForm").onsubmit = (e) => {
  if (e.submitter?.value === "cancel") return;
  e.preventDefault();
  const f = e.target,
    g = f.grupoCadastro.value,
    id = f.id.value;
  const stateBeforeSave = cloneDataState();
  let savedContextValue = "";
  if (g === "ITEM") {
    const obj = {
      id: id || crypto.randomUUID(),
      grupo: f.grupo.value,
      item: f.item.value.trim(),
      padrao: f.padrao.checked,
    };
    if (!obj.item) return alert("Informe o item de lançamento.");
    savedContextValue = obj.item;
    if (obj.padrao)
      registers.forEach((r) => {
        if (r.grupo === obj.grupo)
          r.padrao = false;
      });
    if (id)
      Object.assign(
        registers.find((x) => x.id === id),
        obj,
      );
    else registers.push(obj);
  } else {
    const arr = g === "MOTORISTA" ? drivers : g === "VEICULO" ? vehicles : g === "FORNECEDOR" ? suppliers : paymentMethods,
      name = f.nome.value.trim(),
      isActive = ["VEICULO","FORNECEDOR","FORMA_PAGAMENTO"].includes(g) ? f.ativo.checked : true,
      isDefault = f.simplePadrao.checked && isActive,
      kmInitialText = String(f.kmInicial.value || "").replace(/\D/g, ""),
      kmInicial = +kmInitialText;
    if (!name) return alert("Informe o nome do cadastro.");
    savedContextValue = name;
    if (g === "VEICULO" && (!kmInitialText || !(+f.capacidadeTanque.value > 0)))
      return alert("Informe o hodômetro inicial e a capacidade do tanque.");
    if (isDefault) arr.forEach((x) => (x.padrao = false));
    if (id) {
      const item = arr.find((x) => x.id === id);
      item.nome = name;
      item.padrao = isDefault;
      if (g === "VEICULO") {
        item.kmInicial = kmInicial; item.ativo = isActive;
        item.placa = f.placa.value.trim();
        item.anoFabricacao = +f.anoFabricacao.value || "";
        item.anoModelo = +f.anoModelo.value || "";
        item.capacidadeTanque = +f.capacidadeTanque.value || 0;
        item.motorizacao = f.motorizacao.value;
        item.consumoEtanolCidade = +f.consumoEtanolCidade.value || "";
        item.consumoEtanolEstrada = +f.consumoEtanolEstrada.value || "";
        item.consumoGasolinaCidade = +f.consumoGasolinaCidade.value || "";
        item.consumoGasolinaEstrada = +f.consumoGasolinaEstrada.value || "";
        item.consumoDieselCidade = +f.consumoDieselCidade.value || "";
        item.consumoDieselEstrada = +f.consumoDieselEstrada.value || "";
        if (!isActive) item.padrao = false;
      }
      if (g === "MOTORISTA") {
        item.numeroCnh=f.numeroCnh.value.trim(); item.grupoCnh=f.grupoCnh.value.trim();
        item.validadeCnh=f.validadeCnh.value; item.observacao=f.obsMotorista.value.trim();
      }
      if (g === "FORNECEDOR") {
        item.local=f.localFornecedor.value.trim();
        item.latitude=validCoordinate(f.latitudeFornecedor.value) ? Number(f.latitudeFornecedor.value) : "";
        item.longitude=validCoordinate(f.longitudeFornecedor.value) ? Number(f.longitudeFornecedor.value) : "";
        item.ativo=isActive;
      }
      if (g === "FORMA_PAGAMENTO") item.ativo=isActive;
    } else
      arr.push({
        id: crypto.randomUUID(),
        nome: name,
        kmInicial: g === "VEICULO" ? kmInicial : undefined,
        placa: g === "VEICULO" ? f.placa.value.trim() : undefined,
        anoFabricacao: g === "VEICULO" ? (+f.anoFabricacao.value || "") : undefined,
        anoModelo: g === "VEICULO" ? (+f.anoModelo.value || "") : undefined,
        capacidadeTanque: g === "VEICULO" ? (+f.capacidadeTanque.value || 0) : undefined,
        motorizacao: g === "VEICULO" ? f.motorizacao.value : undefined,
        consumoEtanolCidade: g === "VEICULO" ? (+f.consumoEtanolCidade.value || "") : undefined,
        consumoEtanolEstrada: g === "VEICULO" ? (+f.consumoEtanolEstrada.value || "") : undefined,
        consumoGasolinaCidade: g === "VEICULO" ? (+f.consumoGasolinaCidade.value || "") : undefined,
        consumoGasolinaEstrada: g === "VEICULO" ? (+f.consumoGasolinaEstrada.value || "") : undefined,
        consumoDieselCidade: g === "VEICULO" ? (+f.consumoDieselCidade.value || "") : undefined,
        consumoDieselEstrada: g === "VEICULO" ? (+f.consumoDieselEstrada.value || "") : undefined,
        numeroCnh: g === "MOTORISTA" ? f.numeroCnh.value.trim() : undefined,
        grupoCnh: g === "MOTORISTA" ? f.grupoCnh.value.trim() : undefined,
        validadeCnh: g === "MOTORISTA" ? f.validadeCnh.value : undefined,
        observacao: g === "MOTORISTA" ? f.obsMotorista.value.trim() : undefined,
        local: g === "FORNECEDOR" ? f.localFornecedor.value.trim() : undefined,
        latitude: g === "FORNECEDOR" && validCoordinate(f.latitudeFornecedor.value) ? Number(f.latitudeFornecedor.value) : undefined,
        longitude: g === "FORNECEDOR" && validCoordinate(f.longitudeFornecedor.value) ? Number(f.longitudeFornecedor.value) : undefined,
        padrao: isDefault || arr.length === 0,
        ativo: isActive,
      });
    if (g === "VEICULO" && !arr.some((x) => x.padrao && x.ativo !== false)) {
      const first = arr.find((x) => x.ativo !== false);
      if (first) first.padrao = true;
    }
  }
  try {
    recalculateDistances();
    save();
  } catch (error) {
    restoreDataState(stateBeforeSave);
    console.error("Falha ao salvar o cadastro:", error);
    alert("Não foi possível salvar o cadastro. Verifique os dados e tente novamente.");
    return;
  }
  finishRegisterFlow(
    entryContext && ["ITEM", "MOTORISTA", "FORNECEDOR", "FORMA_PAGAMENTO"].includes(g)
      ? savedContextValue
      : "",
  );
  showToast("Cadastro salvo com sucesso.");
};
// O veículo dos movimentos é controlado exclusivamente pela seleção da tela inicial.
$("#typeFilter").innerHTML =
  '<option value="">Todos os grupos</option>' +
  GROUPS
    .map((group) => `<option>${group}</option>`)
    .join("");
$("#typeFilter").onchange = renderMovements;
$("#search").oninput = renderMovements;
[
  ["movement", renderMovements],
  ["report", renderReports],
  ["chart", renderCharts],
].forEach(([prefix, render]) => {
  ["Start", "End"].forEach((suffix) => {
    const field = $("#" + prefix + suffix);
    if (field) field.onchange = render;
  });
  const clearButton = $("#clear" + prefix[0].toUpperCase() + prefix.slice(1) + "Period");
  if (clearButton) clearButton.onclick = () => {
    $("#" + prefix + "Start").value = "";
    $("#" + prefix + "End").value = "";
    render();
  };
});
function esc(v) {
  return String(v ?? "").replace(
    /[&<>\"]/g,
    (c) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" })[c],
  );
}
function col(n) {
  let s = "";
  while (n) {
    n--;
    s = String.fromCharCode(65 + (n % 26)) + s;
    n = Math.floor(n / 26);
  }
  return s;
}
function sheet(rows) {
  return `<?xml version="1.0" encoding="UTF-8"?><worksheet xmlns="http://schemas.openxmlformats.org/spreadsheetml/2006/main"><sheetData>${rows.map((r, i) => `<row r="${i + 1}">${r.map((v, j) => (typeof v === "number" ? `<c r="${col(j + 1)}${i + 1}"><v>${v}</v></c>` : `<c r="${col(j + 1)}${i + 1}" t="inlineStr"><is><t>${esc(v)}</t></is></c>`)).join("")}</row>`).join("")}</sheetData></worksheet>`;
}
async function exportXlsx() {
  await exportReportXlsx();
}
window.vehicleAppBridge = {
  getState: () => ({ movements, registers, drivers, vehicles, suppliers, paymentMethods, alerts }),
  applyState: (state) => {
    if (!state) return;
    registers = (state.registers || defaults).map(normalizeRegister);
    enforceSingleDefaults();
    movements = (state.movements || []).map((m, i) => enforceItemGroup(normalizeMovement(m, i)));
    drivers = state.drivers || [];
    vehicles = (state.vehicles || []).map(normalizeVehicle);
    suppliers = state.suppliers || [];
    paymentMethods = state.paymentMethods || [];
    alerts = (state.alerts || []).map(normalizeAlertRecord);
    migrateToNewAlertModel();
    ensureAlertData();
    recalculateDistances();
    save(false);
    setTimeout(() => evaluateAlerts(true), 0);
  },
};
$("#homeVersion").textContent = "v" + APP_VERSION;
const aboutVersion = [...$$("#sobre p")].find((p) =>
  p.textContent.trim().startsWith("Versão:"),
);
if (aboutVersion)
  aboutVersion.innerHTML = "<strong>Versão:</strong> " + APP_VERSION;


let deferredInstallPrompt = null;
const installButtons = () => $$(".install-app-action");
const isNativeApp = () =>
  Boolean(window.Capacitor?.isNativePlatform?.());
const isStandalone = () =>
  isNativeApp() ||
  window.matchMedia("(display-mode: standalone)").matches ||
  window.navigator.standalone === true;
const isIos = () => /iphone|ipad|ipod/i.test(navigator.userAgent);
const isAndroid = () => /android/i.test(navigator.userAgent);
function manualInstallMessage() {
  if (isIos())
    return "No Safari, toque em Compartilhar e depois em Adicionar à Tela de Início.";
  if (isAndroid())
    return "No Chrome, toque no menu ⋮ e escolha Instalar aplicativo ou Adicionar à tela inicial.";
  return "Abra o menu do navegador e escolha Instalar aplicativo, Instalar MyCar+ ou Criar atalho.";
}
function refreshInstallControls() {
  const canInstall = Boolean(deferredInstallPrompt) && !isStandalone();
  installButtons().forEach((button) => {
    button.hidden = isStandalone();
    button.disabled = isStandalone();
  });
  const help = $("#installHelpText");
  if (help)
    help.textContent = isStandalone()
      ? "O MyCar+ já está instalado neste aparelho."
      : canInstall
        ? "Toque no botão para instalar o aplicativo neste aparelho."
        : manualInstallMessage();
}
window.addEventListener("beforeinstallprompt", (event) => {
  event.preventDefault();
  deferredInstallPrompt = event;
  refreshInstallControls();
});
installButtons().forEach((button) => {
  button.addEventListener("click", async () => {
    if (isStandalone()) {
      alert("O MyCar+ já está instalado neste aparelho.");
      return;
    }
    if (!deferredInstallPrompt) {
      alert(manualInstallMessage());
      return;
    }
    deferredInstallPrompt.prompt();
    const choice = await deferredInstallPrompt.userChoice;
    deferredInstallPrompt = null;
    refreshInstallControls();
    if (choice.outcome !== "accepted")
      alert("A instalação não foi concluída. Você pode tentar novamente pelo menu do navegador.");
  });
});
window.addEventListener("appinstalled", () => {
  deferredInstallPrompt = null;
  refreshInstallControls();
});
refreshInstallControls();

async function configureNativeStatusBar() {
  if (!isNativeApp()) return;
  document.documentElement.classList.add("native-app");
  const statusBar = window.Capacitor?.Plugins?.StatusBar;
  if (!statusBar) return;
  try {
    await statusBar.setOverlaysWebView({ overlay: false });
    await statusBar.setBackgroundColor({ color: "#0788E8" });
    await statusBar.setStyle({ style: "LIGHT" });
  } catch (error) {
    console.warn("Não foi possível ajustar a barra de status:", error);
  }
}
configureNativeStatusBar();

function closeReportViewer() {
  if (window.MyCarReportManager) window.MyCarReportManager.close();
}

async function createReportCoverFile(jobName) {
  const normalized = String(jobName || "").toLowerCase();
  const isAi = normalized.includes("inteligencia") || normalized.includes("ia");
  const isManual = normalized.includes("manual");
  const title = isManual ? "MANUAL DE AJUDA" : (isAi ? "RELATÓRIO DE INTELIGÊNCIA ARTIFICIAL" : "RELATÓRIO EXECUTIVO");
  const vehicle = selectedVehicleObject();
  const vehicleName = vehicle?.nome || "Veículo não informado";
  const prefix = isManual ? "MANUAL" : (isAi ? "RELATORIO_IA" : "RELATORIO_EXECUTIVO");
  const canvas = document.createElement("canvas");
  canvas.width = 1200; canvas.height = 1200;
  const ctx = canvas.getContext("2d");
  const gradient = ctx.createLinearGradient(0, 0, 1200, 1200);
  gradient.addColorStop(0, "#0b2b4f"); gradient.addColorStop(1, "#0788e8");
  ctx.fillStyle = gradient; ctx.fillRect(0, 0, 1200, 1200);
  ctx.fillStyle = "rgba(255,255,255,.10)"; ctx.fillRect(70, 70, 1060, 1060);
  const image = new Image();
  image.src = REPORT_LOGO_DATA_URI;
  try { await image.decode(); ctx.drawImage(image, 390, 95, 420, 420); } catch (_) {}
  ctx.textAlign = "center";
  ctx.fillStyle = "#ffffff"; ctx.font = "700 58px Arial"; ctx.fillText("MyCar+", 600, 600);
  ctx.font = "700 48px Arial";
  const words = title.split(" "); let line="", y=690;
  for (const word of words) { const test=(line+" "+word).trim(); if (ctx.measureText(test).width>960 && line) { ctx.fillText(line,600,y); line=word; y+=62; } else line=test; }
  if (line) ctx.fillText(line,600,y);
  ctx.font = "32px Arial"; ctx.fillStyle = "#dcefff";
  if (isManual) {
    ctx.fillText(`Versão ${APP_VERSION}`, 600, y + 85);
    ctx.fillText("Desenvolvedor: Marcelo Ribeiro", 600, y + 135);
    ctx.fillText("Criação do sistema: julho de 2026", 600, y + 185);
  } else {
    ctx.fillText(vehicleName, 600, y + 95);
    ctx.fillText(`${periodText("report")}`, 600, y + 145);
  }
  ctx.font = "26px Arial"; ctx.fillText(`Gerado em ${new Date().toLocaleString("pt-BR")} · V${APP_VERSION}`, 600, 1080);
  const blob = await new Promise(resolve => canvas.toBlob(resolve, "image/jpeg", 0.92));
  if (!blob) return null;
  return new File([blob], `CAPA_${prefix}_MYCAR_PLUS.jpg`, { type: "image/jpeg" });
}

window.MyCarReportManager?.registerCoverFactory(null);

async function shareReportHtmlFromViewer(data = {}) {
  const html = String(data.html || "");
  if (!html.trim()) { alert("Não foi possível preparar o relatório para compartilhamento."); return; }
  const jobName = String(data.jobName || "MyCarPlus_Relatorio").trim() || "MyCarPlus_Relatorio";
  if (window.MyCarReportManager) {
    await window.MyCarReportManager.share(jobName, html);
    return;
  }
  const safeName = jobName.replace(/[^a-zA-Z0-9._-]+/g, "_").replace(/_+/g, "_") || "MyCarPlus_Relatorio";
  const file = new File([html], `${safeName}.html`, { type: "text/html;charset=utf-8" });
  const url = URL.createObjectURL(file);
  const link = document.createElement("a");
  link.href = url; link.download = file.name; document.body.appendChild(link); link.click(); link.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1800);
}
function openReportDocument(content, { title = "Relatório MyCar+",
  popupMessage = "Permita janelas pop-up para abrir o relatório.",
  jobName = "MyCarPlus_Relatorio" } = {}) {
  if (window.MyCarReportManager) {
    return window.MyCarReportManager.open(content, { title, popupMessage, jobName });
  }
  const win = window.open("", "_blank");
  if (!win) { alert(popupMessage); return null; }
  win.document.open(); win.document.write(content); win.document.close();
  return win;
}

function exportPdfReport() {
  const selectedVehicle = selectedVehicleName();
  const vehicleLabel = selectedVehicle || "Nenhum veículo selecionado";
  if (!periodIsValid("report")) return alert("Corrija o período antes de gerar o relatório.");
  const allVehicleMovements = filtered(selectedVehicle);
  const effective = applyEffectivePeriodToFields("report", allVehicleMovements);
  if (!effective.hasOverlap) return alert(effectivePeriodMessage(effective));
  const ms = filterByPeriod(allVehicleMovements, "report");
  if (!ms.length) return alert("Não existem movimentos no veículo e período selecionados.");

  ensureAlertData();
  const { start: reportStart, end: reportEnd } = periodValues("report");
  const currentVehicle = selectedVehicleObject();
  const periodStats = stats(ms);
  const periodGroups = groupTotals(ms);
  const e = (value) => String(value ?? "").replace(/[&<>\"]/g, (character) => ({ "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;" })[character]);
  const dateBR = (value) => {
    const parts = String(value || "").slice(0, 10).split("-");
    return parts.length === 3 ? `${parts[2]}/${parts[1]}/${parts[0]}` : "—";
  };
  const pct = (value, total) => total ? num(value / total * 100, 1) + "%" : "0,0%";
  const emitted = new Intl.DateTimeFormat("pt-BR", { dateStyle: "short", timeStyle: "short" }).format(new Date());
  const period = periodText("report");
  const reportEndDate = chartDate(reportEnd);
  const reportStartDate = chartDate(reportStart);
  const historyToEnd = allVehicleMovements.filter((movement) => {
    const date = String(movement.data_hora || "").slice(0, 10);
    return date && date <= reportEnd;
  });
  const rollingStart = (months) => {
    const date = new Date(reportEndDate);
    date.setMonth(date.getMonth() - months);
    date.setDate(date.getDate() + 1);
    return chartIso(date);
  };
  const lastMonthStart = rollingStart(1);
  const lastYearStart = rollingStart(12);
  const dateInRange = (movement, start, end = reportEnd) => {
    const date = String(movement.data_hora || "").slice(0, 10);
    return date && date >= start && date <= end;
  };
  const validFuelMovements = (source, fuelKey = "") => source
    .filter((movement) => movement.grupo === "COMBUSTÍVEL" && movement.tanque_completo !== "NAO" && +movement.quantidade_litros > 0 && +(movement.distancia_abastecimento_km ?? movement.distancia_km) > 0)
    .filter((movement) => !fuelKey || fuelSeriesIdentity(movement.item).key === fuelKey)
    .sort((a, b) => String(a.data_hora || "").localeCompare(String(b.data_hora || "")));
  const weightedConsumption = (source) => {
    const totals = source.reduce((result, movement) => {
      result.distance += +(movement.distancia_abastecimento_km ?? movement.distancia_km) || 0;
      result.liters += +movement.quantidade_litros || 0;
      return result;
    }, { distance: 0, liters: 0 });
    return totals.liters > 0 ? totals.distance / totals.liters : null;
  };
  const consumptionSummary = (fuelKey = "") => {
    const all = validFuelMovements(historyToEnd, fuelKey);
    const currentMovement = all.at(-1);
    const current = currentMovement
      ? +(currentMovement.distancia_abastecimento_km ?? currentMovement.distancia_km) / +currentMovement.quantidade_litros
      : null;
    return {
      current,
      month: weightedConsumption(all.filter((movement) => dateInRange(movement, lastMonthStart))),
      year: weightedConsumption(all.filter((movement) => dateInRange(movement, lastYearStart))),
      total: weightedConsumption(all),
      currentDate: currentMovement?.data_hora || "",
      records: all.length,
    };
  };
  const consumptionGeneral = consumptionSummary();
  const consumptionEtanol = consumptionSummary("etanol");
  const consumptionGasolina = consumptionSummary("gasolina");
  const consumptionValue = (value) => Number.isFinite(value) && value > 0 ? `${num(value, 2)} km/L` : "Sem dados";
  const consumptionHint = (summary, kind) => {
    if (!summary.records) return "Nenhum ciclo válido";
    if (kind === "current" && summary.currentDate) return `ciclo de ${dateBR(summary.currentDate)}`;
    if (kind === "month") return `desde ${dateBR(lastMonthStart)}`;
    if (kind === "year") return `desde ${dateBR(lastYearStart)}`;
    return `${summary.records} ciclo(s) válido(s)`;
  };
  const consumptionCards = (title, summary, className = "") => `<div class="indicator-block ${className}"><h3>${e(title)}</h3><div class="kpis consumption-kpis">
    <div class="kpi"><small>Consumo atual</small><strong>${consumptionValue(summary.current)}</strong><span>${e(consumptionHint(summary, "current"))}</span></div>
    <div class="kpi"><small>Último mês</small><strong>${consumptionValue(summary.month)}</strong><span>${e(consumptionHint(summary, "month"))}</span></div>
    <div class="kpi"><small>Último ano</small><strong>${consumptionValue(summary.year)}</strong><span>${e(consumptionHint(summary, "year"))}</span></div>
    <div class="kpi"><small>Consumo total</small><strong>${consumptionValue(summary.total)}</strong><span>${e(consumptionHint(summary, "total"))}</span></div>
  </div></div>`;

  const inclusiveDays = Math.max(1, Math.round((reportEndDate - reportStartDate) / 86400000) + 1);
  const inclusiveMonths = Math.max(1, (reportEndDate.getFullYear() - reportStartDate.getFullYear()) * 12 + reportEndDate.getMonth() - reportStartDate.getMonth() + 1);
  const financialGroups = [
    { key: "COMBUSTÍVEL", title: "Abastecimento", className: "fuel" },
    { key: "MANUTENÇÃO", title: "Manutenção", className: "maintenance" },
    { key: "ADMINISTRATIVO", title: "Administrativo", className: "administrative" },
    { key: "RECEITA", title: "Receitas", className: "income" },
  ];
  const groupAmount = (source, group) => source.reduce((total, movement) => total + (movement.grupo === group ? (+movement.valor || 0) : 0), 0);
  const financialRows = financialGroups.map((group) => {
    const periodTotal = groupAmount(ms, group.key);
    const annual = groupAmount(historyToEnd.filter((movement) => dateInRange(movement, lastYearStart)), group.key);
    const total = groupAmount(historyToEnd, group.key);
    return {
      ...group,
      daily: periodTotal / inclusiveDays,
      monthly: periodTotal / inclusiveMonths,
      annual,
      total,
      periodTotal,
    };
  });
  const financialCards = financialRows.map((row) => `<article class="finance-card ${row.className}"><h3>${e(row.title)}</h3><div class="finance-values">
    <div><small>Média diária</small><strong>${money(row.daily)}</strong></div>
    <div><small>Média mensal</small><strong>${money(row.monthly)}</strong></div>
    <div><small>Último ano</small><strong>${money(row.annual)}</strong></div>
    <div><small>Total acumulado</small><strong>${money(row.total)}</strong></div>
  </div></article>`).join("");
  const expenseRows = financialRows.filter((row) => row.key !== "RECEITA");
  const periodGross = expenseRows.reduce((total, row) => total + row.periodTotal, 0);
  const periodIncome = financialRows.find((row) => row.key === "RECEITA")?.periodTotal || 0;
  const periodNet = periodGross - periodIncome;
  const periodCostKm = periodStats.km ? periodNet / periodStats.km : 0;
  const periodNetDaily = periodNet / inclusiveDays;
  const periodNetMonthly = periodNetDaily * 30.44;
  const netTotalizerCards = `<div class="kpis net-totalizers">
    <div class="kpi net-totalizer"><small>Custo líquido por km</small><strong>${periodStats.km ? money(periodCostKm) : "Não disponível"}</strong><span>${periodStats.km ? `${intFmt(periodStats.km)} km no período` : "sem distância válida no período"}</span></div>
    <div class="kpi net-totalizer"><small>Custo líquido diário</small><strong>${money(periodNetDaily)}</strong><span>${inclusiveDays} dia(s) no período</span></div>
    <div class="kpi net-totalizer"><small>Custo líquido mensal</small><strong>${money(periodNetMonthly)}</strong><span>média diária × 30,44 dias</span></div>
    <div class="kpi net-totalizer"><small>Custo líquido total</small><strong>${money(periodNet)}</strong><span>despesas dos grupos menos receitas</span></div>
  </div>`;
  const monthIndex = (value) => {
    const [year, month] = String(value || "").slice(0, 7).split("-").map(Number);
    return Number.isFinite(year) && Number.isFinite(month) ? year * 12 + month - 1 : null;
  };
  const reportStartMonth = monthIndex(reportStart), reportEndMonth = monthIndex(reportEnd);
  const competenceDetails = allVehicleMovements.filter((movement) => movement.rateio_ativo && ["MANUTENÇÃO", "ADMINISTRATIVO"].includes(movement.grupo)).map((movement) => {
    const totalCents = Math.round((+movement.valor || 0) * 100);
    const months = Math.max(2, Number(movement.rateio_qtd_meses) || 2);
    const initial = monthIndex(movement.rateio_competencia_inicial || movement.data_hora);
    const final = initial + months - 1;
    const overlapStart = Math.max(initial, reportStartMonth);
    const overlapEnd = Math.min(final, reportEndMonth);
    const considered = Math.max(0, overlapEnd - overlapStart + 1);
    const baseCents = Math.floor(totalCents / months);
    const lastCents = totalCents - baseCents * (months - 1);
    let appropriatedCents = considered * baseCents;
    if (considered && overlapEnd === final) appropriatedCents += lastCents - baseCents;
    const accumulatedEnd = Math.min(final, reportEndMonth);
    const accumulatedCount = Math.max(0, accumulatedEnd - initial + 1);
    let accumulatedCents = accumulatedCount * baseCents;
    if (accumulatedCount === months) accumulatedCents += lastCents - baseCents;
    return { movement, months, considered, appropriated: appropriatedCents / 100, balance: Math.max(0, totalCents - accumulatedCents) / 100 };
  }).filter((row) => row.considered > 0);
  const rateioOriginalInPeriod = competenceDetails.reduce((total, row) => {
    const date = String(row.movement.data_hora || "").slice(0, 10);
    return total + (date >= reportStart && date <= reportEnd ? (+row.movement.valor || 0) : 0);
  }, 0);
  const rateioAppropriated = competenceDetails.reduce((total, row) => total + row.appropriated, 0);
  const competenceNet = periodGross - rateioOriginalInPeriod + rateioAppropriated - periodIncome;
  const competenceCostKm = periodStats.km ? competenceNet / periodStats.km : 0;
  const competenceDaily = competenceNet / inclusiveDays;
  const competenceMonthly = competenceDaily * 30.44;
  const competenceCards = `<div class="kpis net-totalizers competence-totalizers">
    <div class="kpi net-totalizer"><small>Custo líquido por km</small><strong>${periodStats.km ? money(competenceCostKm) : "Não disponível"}</strong><span>visão por competência</span></div>
    <div class="kpi net-totalizer"><small>Custo líquido diário</small><strong>${money(competenceDaily)}</strong><span>${inclusiveDays} dia(s) no período</span></div>
    <div class="kpi net-totalizer"><small>Custo líquido mensal</small><strong>${money(competenceMonthly)}</strong><span>média diária × 30,44 dias</span></div>
    <div class="kpi net-totalizer"><small>Custo líquido total</small><strong>${money(competenceNet)}</strong><span>despesas ajustadas menos receitas</span></div>
  </div>`;
  const competenceTable = competenceDetails.length ? `<table class="responsive-table"><thead><tr><th>Item rateado</th><th>Valor original</th><th>Parcelas consideradas</th><th>Apropriado no período</th><th>Saldo futuro</th></tr></thead><tbody>${competenceDetails.map((row) => `<tr><td data-label="Item rateado">${e(row.movement.item || "—")}</td><td data-label="Valor original" class="num">${money(row.movement.valor)}</td><td data-label="Parcelas consideradas" class="num">${row.considered} de ${row.months}</td><td data-label="Apropriado no período" class="num">${money(row.appropriated)}</td><td data-label="Saldo futuro" class="num">${money(row.balance)}</td></tr>`).join("")}</tbody></table>` : '<p class="rule-note">Nenhum item de Manutenção ou Administrativo foi rateado no período. Os custos permanecem iguais à visão realizada.</p>';
  const biggestExpense = [...expenseRows].sort((a, b) => b.periodTotal - a.periodTotal)[0];

  const timeline = periodChartData(ms, reportStart, reportEnd);
  const financial = financialChartData(ms, reportStart, reportEnd);
  const odometer = odometerChartData(currentVehicle, allVehicleMovements, reportStart, reportEnd);
  const composition = costCompositionData(ms);
  const monthlyGranularity = timeline.granularity === "month";
  function renderChartImage(height, draw) {
    const stage = document.createElement("div");
    stage.style.cssText = `position:fixed;left:-10000px;top:0;width:760px;height:${height}px;visibility:hidden;pointer-events:none;`;
    const canvas = document.createElement("canvas");
    canvas.style.width = "760px";
    canvas.style.height = `${height}px`;
    stage.appendChild(canvas);
    document.body.appendChild(stage);
    try {
      draw(canvas);
      return canvas.toDataURL("image/png");
    } finally {
      stage.remove();
    }
  }
  const previousTheme = document.documentElement.dataset.theme;
  document.documentElement.dataset.theme = "light";
  let chartImages;
  try {
    chartImages = {
      consumption: renderChartImage(350, (canvas) => drawFuelConsumptionChart(canvas, timeline.labels, timeline.fuelSeries)),
      odometer: renderChartImage(330, (canvas) => drawOdometerChart(canvas, odometer)),
      financial: renderChartImage(320, (canvas) => drawMultiLineChart(canvas, financial.labels, [
        { name: "Custo bruto", values: financial.gross, color: "#d94b4b" },
        { name: "Receitas", values: financial.income, color: "#1f8a70" },
        { name: "Custo líquido", values: financial.net, color: "#246b9e" },
      ], "money")),
      daily: renderChartImage(300, (canvas) => drawLineChart(canvas, timeline.labels, timeline.daily, "money", "#d94b4b", { showPointValues: false })),
      perKm: renderChartImage(300, (canvas) => drawChart(canvas, composition.perKmLabels, composition.perKm, "money", { colors: composition.perKmColors, valueTextColor: "#111111" })),
      perDay: renderChartImage(300, (canvas) => drawChart(canvas, composition.perDayLabels, composition.perDay, "money", { colors: composition.perDayColors, valueTextColor: "#111111" })),
      totals: renderChartImage(300, (canvas) => drawChart(canvas, composition.totalLabels, composition.totalValues, "money", { colors: composition.totalColors, valueTextColor: "#111111" })),
      top: renderChartImage(300, (canvas) => drawChart(canvas, composition.topLabels, composition.topValues, "money", { valueTextColor: "#111111" })),
    };
  } finally {
    if (previousTheme) document.documentElement.dataset.theme = previousTheme;
    else delete document.documentElement.dataset.theme;
  }
  const chartCards = [
    {
      title: monthlyGranularity ? "Consumo médio mensal por combustível" : "Consumo médio anual por combustível",
      image: chartImages.consumption,
      description: "Compara o consumo em km/L no período. A linha Geral consolida os combustíveis; as demais linhas mostram cada combustível com ciclos válidos.",
    },
    {
      title: "Evolução do hodômetro",
      image: chartImages.odometer,
      description: "Apresenta a progressão real do hodômetro e permite visualizar a utilização do veículo entre o início e o fim do período.",
    },
    {
      title: "Evolução financeira acumulada",
      image: chartImages.financial,
      description: "Mostra o crescimento acumulado do custo bruto, das receitas e do custo líquido desde a data inicial selecionada.",
    },
    {
      title: monthlyGranularity ? "Custo líquido médio diário por mês" : "Custo líquido médio diário por ano",
      image: chartImages.daily,
      description: "Demonstra o custo líquido médio por dia em cada intervalo. Períodos sem movimentação não são convertidos em custo zero.",
    },
    {
      title: "Custo por quilômetro por grupo e total",
      image: chartImages.perKm,
      description: "Divide os custos de abastecimento, administrativo e manutenção pela distância percorrida. A barra laranja apresenta o total consolidado.",
    },
    {
      title: "Custo médio diário por grupo e total",
      image: chartImages.perDay,
      description: "Compara a média diária dos três grupos de despesas. A barra laranja representa o custo diário consolidado.",
    },
    {
      title: "Custos por grupo e total consolidado",
      image: chartImages.totals,
      description: "Apresenta os valores acumulados por grupo de despesa e o somatório consolidado do período, sem incluir receitas.",
    },
    {
      title: "Cinco itens com maior valor acumulado",
      image: chartImages.top,
      description: "Identifica os itens de lançamento que concentraram os maiores valores no período selecionado.",
    },
  ].map((chart) => `<article class="chart-card"><h3>${e(chart.title)}</h3><img src="${chart.image}" alt="${e(chart.title)}"><p>${e(chart.description)}</p></article>`).join("");

  const registeredItems = new Map(registers
    .filter((item) => item.grupo === "MANUTENÇÃO")
    .map((item) => [item.id, item]));
  const vehicleAlerts = alerts
    .filter((item) => item.vehicleId === currentVehicle?.id && item.modelVersion === 2 && registeredItems.has(item.itemId))
    .sort((a, b) => {
      const statusOrder = { "VENCIDO": 0, "ATENÇÃO": 1, "PROGRAMADO": 2, "INATIVO": 3 };
      const difference = (statusOrder[alertStatus(a)] ?? 9) - (statusOrder[alertStatus(b)] ?? 9);
      return difference || String(registeredItems.get(a.itemId)?.item || a.description).localeCompare(String(registeredItems.get(b.itemId)?.item || b.description), "pt-BR");
    });
  const maintenanceMovements = movements
    .filter((movement) => movement.veiculo === currentVehicle?.nome && movement.grupo === "MANUTENÇÃO")
    .sort((a, b) => new Date(b.data_hora) - new Date(a.data_hora));
  const maintenanceRows = vehicleAlerts.map((alertItem) => {
    const register = registeredItems.get(alertItem.itemId);
    const movement = maintenanceMovements.find((row) => row.item_id === register.id || row.item === register.item);
    return {
      name: register.item,
      lastDate: movement?.data_hora || "",
      lastKm: movement?.hodometro_km || 0,
      value: movement?.valor ?? null,
      supplier: movement?.fornecedor || movement?.local || "Não informado",
      forecast: alertForecast(alertItem),
      status: alertStatus(alertItem),
      active: alertItem.active,
    };
  });
  const statusClass = (status) => String(status || "").toLowerCase().normalize("NFD").replace(/[\u0300-\u036f]/g, "").replace(/\s+/g, "-");
  const maintenanceSummary = {
    expired: maintenanceRows.filter((row) => row.status === "VENCIDO").length,
    attention: maintenanceRows.filter((row) => row.status === "ATENÇÃO").length,
    scheduled: maintenanceRows.filter((row) => row.status === "PROGRAMADO").length,
    withoutHistory: maintenanceRows.filter((row) => !row.lastDate).length,
  };
  const maintenanceHtml = maintenanceRows.length
    ? maintenanceRows.map((row) => `<tr><td data-label="Serviço">${e(row.name)}</td><td data-label="Último registro">${row.lastDate ? dateBR(row.lastDate) : "Sem registro"}</td><td data-label="Valor" class="num">${row.lastDate && row.value != null ? money(row.value) : "—"}</td><td data-label="Fornecedor">${row.lastDate ? e(row.supplier) : "—"}</td><td data-label="Km">${row.lastKm ? intFmt(row.lastKm) + " km" : "—"}</td><td data-label="Próxima referência" class="next-reference ${statusClass(row.status)}">${e(row.forecast)}</td><td data-label="Situação técnica"><span class="status ${statusClass(row.status)}">${e(row.status === "INATIVO" ? "Monitoramento inativo" : row.status)}</span></td><td data-label="Alerta">${row.active ? "Ativo" : "Desativado"}</td></tr>`).join("")
    : '<tr><td colspan="8">Nenhum item do cadastro possui alerta do novo modelo para este veículo.</td></tr>';

  const groupOrder = ["COMBUSTÍVEL", "MANUTENÇÃO", "ADMINISTRATIVO", "RECEITA"];
  const groupTitles = { "COMBUSTÍVEL": "Abastecimento", "MANUTENÇÃO": "Manutenção", "ADMINISTRATIVO": "Administrativo", "RECEITA": "Receitas" };
  const latestSections = groupOrder.map((group) => {
    const rows = historyToEnd
      .filter((movement) => movement.grupo === group)
      .sort((a, b) => new Date(b.data_hora) - new Date(a.data_hora))
      .slice(0, 3);
    const body = rows.length ? rows.map((movement) => {
      const fuelDetail = group === "COMBUSTÍVEL" && +movement.quantidade_litros > 0 ? ` · ${num(movement.quantidade_litros, 2)} L` : "";
      const supplier = movement.fornecedor || movement.local || "—";
      return `<tr><td data-label="Data">${dateBR(movement.data_hora)}</td><td data-label="Item">${e(movement.item || "—")}${fuelDetail}</td><td data-label="Hodômetro" class="num">${movement.hodometro_km ? intFmt(movement.hodometro_km) + " km" : "—"}</td><td data-label="Fornecedor/Posto">${e(supplier)}</td><td data-label="Valor" class="num">${money(movement.valor)}</td></tr>`;
    }).join("") : '<tr><td colspan="5">Nenhum lançamento encontrado.</td></tr>';
    return `<article class="latest-card"><h3>${e(groupTitles[group])}</h3><table class="responsive-table"><thead><tr><th>Data</th><th>Item</th><th class="num">Hodômetro</th><th>Fornecedor/Posto</th><th class="num">Valor</th></tr></thead><tbody>${body}</tbody></table></article>`;
  }).join("");

  const trendText = (current, reference, referenceLabel) => {
    if (!(Number.isFinite(current) && current > 0) || !(Number.isFinite(reference) && reference > 0)) return `Não há dados suficientes para comparar o consumo atual com ${referenceLabel}.`;
    const variation = (current / reference - 1) * 100;
    if (Math.abs(variation) < 1) return `O consumo atual está estável em relação a ${referenceLabel}.`;
    return `O consumo atual está ${num(Math.abs(variation), 1)}% ${variation > 0 ? "acima" : "abaixo"} de ${referenceLabel}.`;
  };
  const expenseShare = biggestExpense ? pct(biggestExpense.periodTotal, periodGross) : "0,0%";
  const incomeOffset = periodGross ? pct(periodIncome, periodGross) : "0,0%";
  const executiveReading = [
    `<b>Consumo:</b> ${trendText(consumptionGeneral.current, consumptionGeneral.month, "a média do último mês")} ${trendText(consumptionGeneral.current, consumptionGeneral.year, "a média do último ano")}`,
    `<b>Financeiro:</b> ${biggestExpense ? `${e(biggestExpense.title)} foi o maior grupo de despesas, com ${money(biggestExpense.periodTotal)} (${expenseShare} do custo bruto).` : "Não houve despesas no período."} As receitas compensaram ${incomeOffset} das despesas e o custo líquido foi ${money(periodNet)}.`,
    `<b>Utilização:</b> o veículo percorreu ${intFmt(periodStats.km)} km em ${intFmt(inclusiveDays)} dia(s), com custo líquido de ${money(periodCostKm)} por km.`,
    `<b>Manutenções:</b> existem ${maintenanceSummary.expired} alerta(s) vencido(s), ${maintenanceSummary.attention} em atenção, ${maintenanceSummary.scheduled} programado(s) e ${maintenanceSummary.withoutHistory} item(ns) sem histórico de manutenção.`,
    `<b>Base analisada:</b> ${ms.length} lançamento(s) no período de ${e(period)}. Indicadores “Total acumulado” consideram o histórico do veículo até ${dateBR(reportEnd)}.`,
  ].map((item) => `<li>${item}</li>`).join("");

  const content = `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1"><title>Relatório Executivo MyCar+</title><style>
  :root{--navy:#12395b;--blue:#246b9e;--surface:#eaf4fb;--border:#8eb8d8;--text:#111;--muted:#5c6872;--number:#0b70c9;--green:#1f8a70;--orange:#d9822b;--red:#b3261e}*{box-sizing:border-box}html,body{margin:0;color:var(--text);font-family:Arial,Helvetica,sans-serif;background:#e9edf0}.actions{display:flex;justify-content:center;gap:10px;padding:14px 12px calc(14px + env(safe-area-inset-bottom));background:var(--navy);position:sticky;bottom:0;z-index:20}.actions button{border:0;border-radius:8px;padding:10px 16px;font-weight:800;cursor:pointer}.actions .primary{background:#0788e8;color:#fff}.actions .secondary{background:#fff;color:var(--navy)}.actions .danger{background:var(--red);color:#fff}.report{width:210mm;min-height:297mm;margin:8px auto;background:#fff;padding:10mm 11mm 9mm;box-shadow:0 4px 18px #0002}.header{display:grid;grid-template-columns:1fr auto;gap:14px;align-items:end;border-bottom:3px solid var(--navy);padding-bottom:8px}.header h1{margin:0;font-size:19px;color:var(--navy)}.brand{font-size:10px;font-weight:800;color:var(--blue)}.report-brand{display:flex;align-items:center;gap:9px}.report-brand img{width:44px;height:44px;object-fit:contain;border-radius:9px;flex:0 0 auto}.meta{font-size:8.5px;line-height:1.5;text-align:right;color:var(--muted)}.section{margin-top:11px}.section-title{margin:0 0 7px;padding-bottom:4px;border-bottom:1px solid var(--border);font-size:10.5px;text-transform:uppercase;letter-spacing:.45px;color:var(--navy)}.indicator-block{margin-top:7px}.indicator-block h3{margin:0 0 5px;font-size:9px;color:var(--navy)}.kpis{display:grid;grid-template-columns:repeat(4,minmax(0,1fr));gap:6px}.kpi,.card,.finance-card,.chart-card,.latest-card{background:var(--surface);border:1px solid var(--border);border-radius:7px;padding:7px}.kpi{break-inside:avoid}.kpi small{display:block;font-size:7px;color:var(--muted)}.kpi strong{display:block;margin-top:3px;font-size:12px;color:var(--number);line-height:1.15}.kpi span{display:block;margin-top:2px;font-size:6.6px;color:var(--muted)}.indicator-block.etanol .kpi{border-left:3px solid var(--blue)}.indicator-block.gasolina .kpi{border-left:3px solid #f28b0c}.finance-grid{display:grid;grid-template-columns:1fr 1fr;gap:7px}.finance-card{break-inside:avoid}.finance-card h3{margin:0 0 6px;font-size:9px;color:var(--navy)}.finance-card.income{border-left:4px solid var(--green)}.finance-values{display:grid;grid-template-columns:repeat(4,1fr);gap:5px}.finance-values div{background:#fff;border-radius:5px;padding:5px}.finance-values small{display:block;font-size:6.6px;color:var(--muted)}.finance-values strong{display:block;margin-top:2px;font-size:9px;color:var(--number)}.finance-card.income strong{color:var(--green)}.net-totalizers{margin-top:7px}.net-totalizers .net-totalizer{border:2px solid var(--red)}.summary-kpis{margin-top:7px}.period-summary{grid-template-columns:repeat(2,minmax(0,1fr))}.reading{background:#f2f8fd;border:1px solid var(--border);border-left:4px solid var(--blue);border-radius:6px;padding:8px;font-size:8px;line-height:1.45}.reading ul{margin:0;padding-left:17px}.reading li+li{margin-top:4px}.charts-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;align-items:start}.chart-card{background:#fff;break-inside:avoid}.chart-card h3{margin:0 0 5px;font-size:9px;color:var(--navy)}.chart-card img{display:block;width:100%;height:auto;border:1px solid #d8e5ee;border-radius:5px;background:#fff}.chart-card p{margin:5px 0 0;font-size:7px;line-height:1.35;color:var(--muted)}.maintenance-summary{margin-bottom:7px}.latest-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;align-items:start}.latest-card{background:#fff;break-inside:auto}.latest-card h3{margin:0 0 5px;font-size:9px;color:var(--navy)}table{width:100%;border-collapse:collapse;font-size:7.2px;background:#fff;border:1px solid var(--border)}thead{display:table-header-group}tr{break-inside:avoid}th{background:var(--surface);color:var(--navy);text-align:left}th,td{padding:4px 5px;border-bottom:1px solid #cfe0ec;vertical-align:middle}.num{text-align:right;color:var(--number);font-weight:700}.next-reference{font-weight:900;color:var(--blue)}.next-reference.vencido{color:var(--red)}.next-reference.atencao{color:#a95b00}.next-reference.inativo{color:var(--muted)}.status{display:inline-block;border-radius:999px;padding:2px 6px;font-size:6.4px;font-weight:800;white-space:nowrap}.status.programado{background:#e5f5ee;color:#176b50}.status.atencao{background:#fff0d9;color:#8a4c00}.status.vencido{background:#fde5e5;color:#9e2525}.status.inativo{background:#eceff1;color:#59636b}.rule-note{margin-top:6px;font-size:7px;color:var(--muted)}.footer{margin-top:12px;border-top:1px solid var(--border);padding-top:5px;display:flex;justify-content:space-between;gap:8px;font-size:7px;color:var(--muted)}@page{size:A4 portrait;margin:10mm 11mm 9mm}@media print{html,body{-webkit-print-color-adjust:exact!important;print-color-adjust:exact!important;background:#fff!important}.actions{display:none!important}.report{width:auto;min-height:0;margin:0!important;padding:0!important;box-shadow:none!important}.section{break-inside:auto}.section-title{break-after:avoid}.kpi,.finance-card,.chart-card,tr{break-inside:avoid}.footer{break-inside:avoid}}@media screen and (max-width:760px){html,body{width:100%;max-width:100%;overflow-x:hidden}.report{width:100%;max-width:100%;min-height:0;margin:0 0 8px;padding:14px 10px;box-shadow:none}.header{grid-template-columns:1fr;align-items:start;gap:7px}.header h1{font-size:20px}.meta{text-align:left;font-size:11px}.section{margin-top:16px}.section-title{font-size:13px}.indicator-block h3,.finance-card h3,.chart-card h3,.latest-card h3{font-size:12px}.kpis{grid-template-columns:repeat(2,minmax(0,1fr));gap:8px}.kpi{padding:10px}.kpi small{font-size:10px}.kpi strong{font-size:15px}.kpi span{font-size:9px}.finance-grid,.charts-grid,.latest-grid{grid-template-columns:1fr}.finance-card{padding:10px}.finance-values{grid-template-columns:repeat(2,1fr)}.finance-values small{font-size:9px}.finance-values strong{font-size:12px}.reading{font-size:11px;padding:10px}.chart-card{padding:9px}.chart-card p{font-size:10px}.responsive-table{display:block;width:100%;border:0;background:transparent}.responsive-table thead{display:none}.responsive-table tbody{display:block}.responsive-table tr{display:block;margin:0 0 10px;border:1px solid var(--border);border-radius:8px;overflow:hidden}.responsive-table td{display:grid;grid-template-columns:minmax(108px,40%) minmax(0,1fr);gap:8px;width:100%;padding:7px 8px;border-bottom:1px solid #dbe8f1;text-align:left!important;font-size:10px;overflow-wrap:anywhere}.responsive-table td:last-child{border-bottom:0}.responsive-table td::before{content:attr(data-label);font-weight:800;color:var(--navy);font-size:9px}.responsive-table td[colspan]{display:block;text-align:center!important}.responsive-table td[colspan]::before{content:none}.status{font-size:8px;white-space:normal}.rule-note{font-size:10px}.footer{font-size:9px}.actions{flex-wrap:wrap}.actions button{flex:1 1 140px;min-height:44px}}@media screen and (max-width:390px){.kpis,.finance-values{grid-template-columns:1fr}.responsive-table td{grid-template-columns:minmax(92px,38%) minmax(0,1fr)}.actions button{flex-basis:100%}}
  </style></head><body><main class="report"><header class="header"><div class="report-brand"><img src="${REPORT_LOGO_DATA_URI}" alt="MyCar+"><div><div class="brand">MyCar+</div><h1>Relatório Executivo Veicular</h1></div></div><div class="meta"><b>Veículo:</b> ${e(vehicleLabel)}<br><b>Período:</b> ${e(period)}<br><b>Emissão:</b> ${e(emitted)} · <b>Versão:</b> ${e(APP_VERSION)}</div></header>
  <section class="section"><h2 class="section-title">Indicadores principais de consumo</h2>${consumptionCards("Consumo geral", consumptionGeneral)}${consumptionCards("Somente etanol", consumptionEtanol, "etanol")}${consumptionCards("Somente gasolina", consumptionGasolina, "gasolina")}</section>
  <section class="section"><h2 class="section-title">Indicadores financeiros</h2><div class="finance-grid">${financialCards}</div>${netTotalizerCards}<div class="kpis summary-kpis period-summary"><div class="kpi"><small>Despesas do período</small><strong>${money(periodGross)}</strong><span>abastecimento + manutenção + administrativo</span></div><div class="kpi"><small>Receitas do período</small><strong>${money(periodIncome)}</strong><span>${incomeOffset} das despesas</span></div></div></section>
  <section class="section"><h2 class="section-title">Custos por Competência</h2><p class="rule-note">Itens rateados são apropriados mensalmente; parcelas posteriores ao período não são incluídas. O lançamento original permanece integral na visão financeira realizada.</p>${competenceCards}${competenceTable}</section>
  <section class="section"><h2 class="section-title">Leitura executiva</h2><div class="reading"><ul>${executiveReading}</ul></div></section>
  <section class="section"><h2 class="section-title">Gráficos</h2><div class="charts-grid">${chartCards}</div></section>
  <section class="section"><h2 class="section-title">Últimos lançamentos por grupo</h2><div class="latest-grid">${latestSections}</div></section>
  <section class="section"><h2 class="section-title">Manutenções com alertas cadastrados</h2><div class="kpis maintenance-summary"><div class="kpi"><small>Alertas vencidos</small><strong>${maintenanceSummary.expired}</strong><span>exigem providência</span></div><div class="kpi"><small>Em atenção</small><strong>${maintenanceSummary.attention}</strong><span>próximos da referência</span></div><div class="kpi"><small>Programados</small><strong>${maintenanceSummary.scheduled}</strong><span>monitoramento em dia</span></div><div class="kpi"><small>Sem histórico</small><strong>${maintenanceSummary.withoutHistory}</strong><span>alerta sem manutenção registrada</span></div></div><table class="responsive-table"><thead><tr><th>Serviço</th><th>Último registro</th><th class="num">Valor</th><th>Fornecedor</th><th>Km</th><th>Próxima referência</th><th>Situação técnica</th><th>Alerta</th></tr></thead><tbody>${maintenanceHtml}</tbody></table><p class="rule-note"><b>Regra:</b> somente itens existentes no Cadastro de Itens e com alerta do novo modelo vinculado ao veículo selecionado são apresentados.</p></section>
  <footer class="footer"><span>MyCar+ · Relatório Executivo contínuo</span><span>Dados consolidados até ${dateBR(reportEnd)} · v${e(APP_VERSION)}</span></footer></main><div class="actions"><button type="button" id="reportPrintButton" class="secondary">Imprimir</button><button type="button" id="reportShareButton" class="primary">Compartilhar</button><button type="button" id="reportCloseButton" class="danger">Fechar</button></div><script>(function(){
  function printableDocument(){var clone=document.documentElement.cloneNode(true);clone.querySelectorAll('.actions,script').forEach(function(node){node.remove()});return '<!doctype html>'+clone.outerHTML}
  function printHtml(){var html=printableDocument();try{if(window.parent&&window.parent.MyCarReportManager){window.parent.MyCarReportManager.print('RELATORIO_EXECUTIVO_MYCAR_PLUS',html);return}if(window.top&&window.top.MyCarReportManager){window.top.MyCarReportManager.print('RELATORIO_EXECUTIVO_MYCAR_PLUS',html);return}if(window.parent&&window.parent!==window){window.parent.postMessage({type:'mycar-print-report-html',jobName:'RELATORIO_EXECUTIVO_MYCAR_PLUS',html:html},'*');return}}catch(error){console.error('Falha ao acionar gerenciador de impressão:',error)}window.print()}
  function shareHtml(){try{var html=printableDocument();if(window.parent&&window.parent.MyCarReportManager){window.parent.MyCarReportManager.share('RELATORIO_EXECUTIVO_MYCAR_PLUS',html);return}if(window.top&&window.top.MyCarReportManager){window.top.MyCarReportManager.share('RELATORIO_EXECUTIVO_MYCAR_PLUS',html);return}if(window.parent&&window.parent!==window){window.parent.postMessage({type:'mycar-share-report-html',jobName:'RELATORIO_EXECUTIVO_MYCAR_PLUS',html:html},'*');return}}catch(error){}try{var html=printableDocument();var blob=new Blob([html],{type:'text/html;charset=utf-8'});var url=URL.createObjectURL(blob);var anchor=document.createElement('a');anchor.href=url;anchor.download='RELATORIO_EXECUTIVO_MYCAR_PLUS.html';document.body.appendChild(anchor);anchor.click();anchor.remove();setTimeout(function(){URL.revokeObjectURL(url)},1500)}catch(error){alert('Não foi possível preparar o arquivo HTML.')}}
  function closeReport(){try{if(window.parent&&window.parent.MyCarReportManager){window.parent.MyCarReportManager.close();return}if(window.parent&&window.parent!==window){window.parent.postMessage({type:'mycar-close-report'},'*');return}}catch(error){}try{window.close()}catch(error){}setTimeout(function(){if(!window.closed){try{if(history.length>1){history.back()}else{location.replace('about:blank')}}catch(error){}}},150)}
  document.getElementById('reportPrintButton').addEventListener('click',printHtml);document.getElementById('reportShareButton').addEventListener('click',shareHtml);document.getElementById('reportCloseButton').addEventListener('click',closeReport);document.addEventListener('keydown',function(event){if(event.key==='Escape')closeReport()});})();<\/script></body></html>`;
  openReportDocument(content, {
    title: "Relatório Executivo Veicular",
    jobName: "RELATORIO_EXECUTIVO_MYCAR_PLUS",
    popupMessage: "Permita janelas pop-up para gerar o relatório.",
  });
}

const DATA_TABLES = {
  movements: "Movimentos",
  registers: "Itens de lançamento",
  vehicles: "Veículos",
  drivers: "Motoristas",
  suppliers: "Fornecedores",
  paymentMethods: "Formas de pagamento",
  alerts: "Alertas do novo modelo",
};
function dataState() {
  return { movements, registers, vehicles, drivers, suppliers, paymentMethods, alerts };
}
function cloneDataState() {
  return JSON.parse(JSON.stringify(dataState()));
}
function restoreDataState(state, rerender = true) {
  movements = state.movements || [];
  registers = state.registers || [];
  vehicles = state.vehicles || [];
  drivers = state.drivers || [];
  suppliers = state.suppliers || [];
  paymentMethods = state.paymentMethods || [];
  alerts = (state.alerts || []).map(normalizeAlertRecord);
  if (rerender) {
    try { renderAll(); } catch (_) {}
  }
}
function downloadBackup(keys) {
  const tables = {};
  const state = dataState();
  keys.forEach((key) => (tables[key] = JSON.parse(JSON.stringify(state[key]))));
  const payload = {
    app: APP_NAME,
    version: APP_VERSION,
    schemaVersion: 9,
    createdAt: new Date().toISOString(),
    deviceId: localStorage.getItem("mycar_device_id") || "dispositivo-local",
    recordCounts: Object.fromEntries(Object.entries(tables).map(([key, rows]) => [key, rows.length])),
    tables,
  };
  const blob = new Blob([JSON.stringify(payload, null, 2)], { type: "application/json" });
  const link = document.createElement("a");
  link.href = URL.createObjectURL(blob);
  link.download = `MyCarPlus_Backup_${new Date().toISOString().slice(0, 10)}.json`;
  link.click();
  setTimeout(() => URL.revokeObjectURL(link.href), 1000);
}
function selectedDataTables(host) {
  return [...host.querySelectorAll("input[type=checkbox]:checked")].map((x) => x.value);
}
function openDataSelector(mode) {
  const dialog = document.createElement("dialog");
  dialog.className = "compact-dialog data-security-dialog";
  const title = mode === "backup" ? "Criar backup" : mode === "restore" ? "Restaurar backup" : "Excluir bases de dados";
  dialog.innerHTML = `<form method="dialog" class="sectioned-form"><div class="dialog-head"><div><small>Gestão de dados</small><h2>${title}</h2></div><button value="cancel">×</button></div><div class="form-scroll"><p class="field-help">${mode === "restore" ? "As tabelas selecionadas serão substituídas integralmente." : "Marque cada base individualmente."}</p><div class="data-table-choices">${Object.entries(DATA_TABLES).map(([key,label]) => `<label><input type="checkbox" value="${key}"><span><b>${label}</b><small>${dataState()[key].length} registro(s)</small></span></label>`).join("")}</div>${mode === "restore" ? '<label>Arquivo de backup<input class="backup-file" type="file" accept=".json,application/json"></label>' : ""}<p class="form-error"></p></div><div class="form-footer"><button class="form-cancel" value="cancel">Cancelar</button><button class="primary run-data-action" type="button">Continuar</button></div></form>`;
  document.body.appendChild(dialog);
  dialog.addEventListener("close", () => dialog.remove());
  dialog.querySelector(".run-data-action").onclick = async () => {
    const keys = selectedDataTables(dialog);
    const error = dialog.querySelector(".form-error");
    if (!keys.length) return (error.textContent = "Selecione pelo menos uma base.");
    if (mode === "backup") {
      downloadBackup(keys);
      dialog.close();
      return;
    }
    if (mode === "restore") {
      const file = dialog.querySelector(".backup-file").files[0];
      if (!file) return (error.textContent = "Selecione o arquivo de backup.");
      let stateBeforeRestore = null;
      try {
        const backup = JSON.parse(await file.text());
        if (backup.app !== APP_NAME || !backup.tables) throw new Error("Arquivo incompatível.");
        if (keys.some((key) => !Array.isArray(backup.tables[key]))) throw new Error("O backup não contém todas as bases selecionadas.");
        if (!confirm(`Substituir integralmente: ${keys.map((k) => DATA_TABLES[k]).join(", ")}?`)) return;
        stateBeforeRestore = cloneDataState();
        keys.forEach((key) => {
          if (key === "movements") movements = backup.tables[key].map(normalizeMovement);
          if (key === "registers") registers = backup.tables[key].map(normalizeRegister);
          if (key === "vehicles") vehicles = backup.tables[key].map(normalizeVehicle);
          if (key === "drivers") drivers = backup.tables[key];
          if (key === "suppliers") suppliers = backup.tables[key];
          if (key === "paymentMethods") paymentMethods = backup.tables[key];
          if (key === "alerts") alerts = backup.tables[key].map(normalizeAlertRecord);
        });
        ensureAlertData();
        enforceSingleDefaults();
        movements.forEach(enforceItemGroup);
        recalculateDistances();
        save();
        dialog.close();
        alert("Restauração concluída. As bases selecionadas foram substituídas.");
      } catch (e) {
        if (stateBeforeRestore) restoreDataState(stateBeforeRestore);
        console.error("Falha na restauração de dados:", e);
        error.textContent = e.message || "Não foi possível restaurar este backup. Nenhuma base foi alterada.";
      }
      return;
    }
    const dependent = keys.some((key) => key !== "movements");
    if (dependent && movements.length && !keys.includes("movements"))
      return (error.textContent = "Para excluir bases cadastrais com movimentos existentes, selecione também Movimentos. Essa proteção evita referências inconsistentes.");
    const makeBackup = confirm("Deseja fazer um backup das bases selecionadas antes de excluí-las?\n\nOK: fazer backup e continuar.\nCancelar: escolher entre continuar sem backup ou sair.");
    if (makeBackup) downloadBackup(keys);
    else if (!confirm("Continuar sem fazer backup?")) return;
    if (!confirm(`Primeira confirmação:\n\nExcluir ${keys.map((k) => DATA_TABLES[k]).join(", ")}?`)) return;
    if (prompt('Segunda confirmação: digite EXCLUIR') !== "EXCLUIR") return (error.textContent = "Confirmação inválida. Nada foi excluído.");
    const stateBeforeDelete = cloneDataState();
    keys.forEach((key) => {
      if (key === "movements") movements = [];
      if (key === "registers") registers = [];
      if (key === "vehicles") vehicles = [];
      if (key === "drivers") drivers = [];
      if (key === "suppliers") suppliers = [];
      if (key === "paymentMethods") paymentMethods = [];
      if (key === "alerts") alerts = [];
    });
    try {
      save();
      dialog.close();
      alert("As bases selecionadas foram excluídas.");
    } catch (e) {
      restoreDataState(stateBeforeDelete);
      console.error("Falha na exclusão das bases:", e);
      error.textContent = "Não foi possível excluir as bases. Nenhum dado foi alterado.";
    }
  };
  dialog.showModal();
}
$("#openBackup").onclick = () => openDataSelector("backup");
$("#openRestore").onclick = () => openDataSelector("restore");
$("#openDeleteData").onclick = () => openDataSelector("delete");

function addMonths(date, months) {
  const value = new Date(date);
  value.setMonth(value.getMonth() + Number(months || 0));
  return value;
}
function dateOnly(value) {
  return value ? new Date(String(value).slice(0, 10) + "T12:00:00") : null;
}
function migrateToNewAlertModel() {
  const validVehicles = new Set(vehicles.map((vehicle) => vehicle.id));
  const validItems = new Set(registers.filter((item) => item.grupo === "MANUTENÇÃO").map((item) => item.id));
  alerts = alerts
    .map(normalizeAlertRecord)
    .filter((item) => item.modelVersion === 2 && validVehicles.has(item.vehicleId) && validItems.has(item.itemId));
  localStorage.removeItem("mycar_alert_history_v1");
  localStorage.removeItem("mycar_technical_parameters_v1");
  localStorage.setItem("mycar_alert_model", "2");
}
function ensureAlertData() {
  const vehiclesById = new Map(vehicles.map((vehicle) => [vehicle.id, vehicle]));
  const itemsById = new Map(registers
    .filter((item) => item.grupo === "MANUTENÇÃO")
    .map((item) => [item.id, item]));
  alerts = alerts
    .map(normalizeAlertRecord)
    .filter((item) => item.modelVersion === 2 && vehiclesById.has(item.vehicleId) && itemsById.has(item.itemId))
    .map((item) => {
      const register = itemsById.get(item.itemId);
      const active = item.statusMode !== "DISABLED" && item.active !== false;
      return {
        ...item,
        modelVersion: 2,
        group: "MANUTENÇÃO",
        description: register.item,
        active,
        statusMode: active ? "ACTIVE" : "DISABLED",
      };
    });
}
function alertStatus(item) {
  if (!item.active) return "INATIVO";
  const vehicle = vehicles.find((v) => v.id === item.vehicleId);
  if (!vehicle || vehicle.ativo === false) return "INATIVO";
  const km = vehicle ? vehicleSummary(vehicle).last : 0;
  const today = new Date(); today.setHours(0, 0, 0, 0);
  const dueDate = dateOnly(item.dueDate);
  const dateDue = dueDate && today >= dueDate;
  const kmDue = Number(item.dueKm) > 0 && km >= Number(item.dueKm);
  if ((item.criterion !== "KM" && dateDue) || (item.criterion !== "DATE" && kmDue)) return "VENCIDO";
  const attentionDate = dueDate && today >= new Date(dueDate.getTime() - Number(item.leadDays || 0) * 86400000);
  const attentionKm = Number(item.dueKm) > 0 && km >= Number(item.dueKm) - Number(item.leadKm || 0);
  return ((item.criterion !== "KM" && attentionDate) || (item.criterion !== "DATE" && attentionKm)) ? "ATENÇÃO" : "PROGRAMADO";
}
function alertForecast(item) {
  const parts = [];
  if (item.criterion !== "KM" && item.dueDate) parts.push(new Date(item.dueDate + "T12:00:00").toLocaleDateString("pt-BR"));
  if (item.criterion !== "DATE" && Number(item.dueKm)) parts.push(`${intFmt(item.dueKm)} km`);
  return parts.join(" ou ") || "Sem previsão";
}
function updateHomeAlertIndicator() {
  const button = $("#homeAlertIndicator");
  const counter = $("#homeAlertCount");
  if (!button || !counter) return;
  const selected = selectedVehicleObject();
  const expired = !selected || selected.ativo === false ? [] : alerts.filter((item) => item.vehicleId === selected.id && alertStatus(item) === "VENCIDO");
  const neutral = !selected || selected.ativo === false;
  button.classList.toggle("safe", !neutral && expired.length === 0);
  button.classList.toggle("danger", expired.length > 0);
  button.classList.toggle("neutral", neutral);
  counter.textContent = String(expired.length);
  counter.hidden = expired.length === 0;
  const label = neutral ? "Selecione um veículo ativo para consultar os alertas" : expired.length ? `${expired.length} alerta(s) técnico(s) vencido(s)` : "Alertas técnicos em dia";
  button.title = label;
  button.setAttribute("aria-label", label);
}
function evaluateAlerts(notify = false) {
  ensureAlertData();
  renderAlerts();
  updateHomeAlertIndicator();
}
function deleteAlert(id) {
  const item = alerts.find((a) => a.id === id);
  if (!item) return false;
  const vehicle = vehicles.find((v) => v.id === item.vehicleId);
  if (vehicle?.ativo === false) {
    alert("Veículo inativo: os alertas estão disponíveis somente para consulta.");
    return false;
  }
  if (!confirm(
    "Excluir alerta técnico?\n\n" +
    "Os alertas são utilizados na programação das manutenções do veículo. " +
    "A exclusão removerá somente este alerta e sua programação futura e não apagará " +
    "os movimentos de manutenção já registrados.\n\n" +
    "Tem certeza de que deseja excluir?",
  )) return false;
  const stateBeforeSave = cloneDataState();
  alerts = alerts.filter((a) => a.id !== id);
  try {
    save();
    showToast("Alerta excluído. Os movimentos de manutenção foram preservados.");
    return true;
  } catch (error) {
    restoreDataState(stateBeforeSave);
    console.error("Falha ao excluir o alerta técnico:", error);
    alert("Não foi possível excluir o alerta. Nenhum dado foi alterado.");
    return false;
  }
}
function alertPanelSvg(name) {
  const icons = {
    oil: '<path d="M3 13h13l3-3h2M5 13l1-5h7l2 5M7 8V5h3v3M16 15c0 2-1.2 3-2.5 3S11 17 11 15"/>',
    brake: '<circle cx="12" cy="12" r="7"/><circle cx="12" cy="12" r="2.5"/><path d="M12 5v2m0 10v2M5 12h2m10 0h2M7 7l1.5 1.5m7 7L17 17m0-10-1.5 1.5m-7 7L7 17"/>',
    battery: '<rect x="5" y="6" width="14" height="13" rx="2"/><path d="M9 3v3m6-3v3M8 11h3m5 0h-3"/>',
    fluid: '<path d="M8 4h8l2 4v12H6V8zM9 4V2h6v2M12 10c-2 3-3 4.5-3 6a3 3 0 0 0 6 0c0-1.5-1-3-3-6z"/>',
    tire: '<circle cx="12" cy="12" r="8"/><circle cx="12" cy="12" r="3"/><path d="M12 4v2m0 12v2M4 12h2m12 0h2"/>',
    wrench: '<path d="M14.7 6.3a4 4 0 0 0-5-5L12 3.6 9.6 6 7.3 3.7a4 4 0 0 0 5 5L5 16l3 3 7.3-7.3a4 4 0 0 0-.6-5.4z"/>',
    calendar: '<path d="M7 3v3m10-3v3M4 8h16M5 5h14a1 1 0 0 1 1 1v14H4V6a1 1 0 0 1 1-1zM8 12h2m4 0h2m-8 4h2m4 0h2"/>',
    gauge: '<path d="M4 17a8 8 0 1 1 16 0M12 13l4-4M7 17h10"/>',
    search: '<circle cx="10" cy="10" r="6"/><path d="m15 15 5 5"/>',
    edit: '<path d="M4 20h4l11-11-4-4L4 16zM13 7l4 4"/>',
    trash: '<path d="M4 7h16M9 7V4h6v3m-8 0 1 13h8l1-13M10 11v5m4-5v5"/>',
  };
  return `<svg class="alert-line-icon" viewBox="0 0 24 24" aria-hidden="true">${icons[name] || icons.wrench}</svg>`;
}
function alertPanelItemIcon(item) {
  const text = normalizeText(`${item.description || ""} ${registers.find((r) => r.id === item.itemId)?.item || ""}`);
  if (text.includes("oleo")) return "oil";
  if (text.includes("pastilha") || text.includes("freio")) return text.includes("fluido") ? "fluid" : "brake";
  if (text.includes("bateria")) return "battery";
  if (text.includes("pneu")) return "tire";
  return "wrench";
}
function alertPanelStatus(item) {
  const raw = alertStatus(item);
  const labels = { PROGRAMADO: "Ativo", ATENÇÃO: "Atenção", VENCIDO: "Vencido", INATIVO: "Desativado" };
  return { raw, label: labels[raw] || raw, css: normalizeText(raw).replace(/\s+/g, "-") };
}
function alertPanelRemaining(item, vehicle) {
  const today = new Date(); today.setHours(0, 0, 0, 0);
  const date = dateOnly(item.dueDate);
  const days = date ? Math.ceil((date.getTime() - today.getTime()) / 86400000) : null;
  const currentKm = vehicle ? vehicleSummary(vehicle).last : 0;
  const dueKm = Number(item.dueKm || 0);
  return {
    days,
    km: dueKm ? dueKm - currentKm : null,
  };
}

function renderAlerts() {
  if (!$("#alertList")) return;
  ensureAlertData();
  const selectedVehicle = selectedVehicleObject();
  const panelName = $("#alertPanelVehicleName");
  const panelMeta = $("#alertPanelVehicleMeta");
  if (panelName) panelName.textContent = selectedVehicle?.nome || "Nenhum veículo selecionado";
  if (panelMeta) panelMeta.textContent = selectedVehicle
    ? `Placa: ${selectedVehicle.placa || "Não informada"} · KM atual: ${intFmt(vehicleSummary(selectedVehicle).last)}`
    : "Selecione um veículo na tela inicial";
  const newButton = $("#newAlert");
  newButton.disabled = !selectedVehicle || selectedVehicle.ativo === false;
  newButton.title = selectedVehicle?.ativo === false
    ? "Veículos inativos não podem receber novos alertas."
    : "Novo alerta técnico";

  const statusOrder = { VENCIDO: 0, ATENÇÃO: 1, PROGRAMADO: 2, INATIVO: 3 };
  const rows = alerts
    .filter((item) => item.vehicleId === selectedVehicle?.id && item.modelVersion === 2)
    .sort((a, b) => (statusOrder[alertStatus(a)] ?? 9) - (statusOrder[alertStatus(b)] ?? 9) || String(a.description).localeCompare(String(b.description), "pt-BR"));

  $("#alertList").innerHTML = rows.map((item) => {
    const status = alertPanelStatus(item);
    const remaining = alertPanelRemaining(item, selectedVehicle);
    const criterion = item.criterion || "BOTH";
    const controlLabel = criterion === "DATE" ? "Data" : criterion === "KM" ? "KM" : "Data e KM";
    const dateMetric = criterion !== "KM" ? `<div class="alert-card-metric"><span>${alertPanelSvg("calendar")}</span><div><small>Data</small><strong>${formatAlertDate(item.dueDate)}</strong><em>${remaining.days == null ? "Sem previsão" : remaining.days >= 0 ? `${remaining.days} dia(s)` : `${Math.abs(remaining.days)} dia(s) vencido`}</em></div></div>` : "";
    const kmMetric = criterion !== "DATE" ? `<div class="alert-card-metric"><span>${alertPanelSvg("gauge")}</span><div><small>KM</small><strong>${Number(item.dueKm) > 0 ? intFmt(item.dueKm) : "—"}</strong><em>${remaining.km == null ? "Sem previsão" : remaining.km >= 0 ? `${intFmt(remaining.km)} km` : `${intFmt(Math.abs(remaining.km))} km vencido`}</em></div></div>` : "";
    return `<article class="alert-panel-card" data-status="${status.raw}">
      <div class="alert-panel-card-body">
        <span class="alert-item-icon">${alertPanelSvg(alertPanelItemIcon(item))}</span>
        <div class="alert-card-content">
          <div class="alert-card-title-row"><h3>${esc(item.description || "Alerta técnico")}</h3><span class="alert-status-badge status-${status.css}">${status.label}</span></div>
          <div class="alert-card-metrics">
            ${dateMetric}
            ${kmMetric}
            <div class="alert-card-metric alert-control-metric"><span>${criterion === "DATE" ? alertPanelSvg("calendar") : criterion === "KM" ? alertPanelSvg("gauge") : `${alertPanelSvg("calendar")}${alertPanelSvg("gauge")}`}</span><div><small>Controle</small><strong>${controlLabel}</strong></div></div>
          </div>
        </div>
      </div>
      <div class="alert-card-actions">
        <button type="button" data-alert-view="${item.id}">${alertPanelSvg("search")}<span>Consultar</span></button>
        <button type="button" data-alert-edit="${item.id}" ${selectedVehicle?.ativo === false ? "disabled" : ""}>${alertPanelSvg("edit")}<span>Alterar</span></button>
        <button type="button" class="danger" data-alert-delete="${item.id}" ${selectedVehicle?.ativo === false ? "disabled" : ""}>${alertPanelSvg("trash")}<span>Excluir</span></button>
      </div>
    </article>`;
  }).join("") || '<div class="alert-panel-empty"><b>Nenhum alerta técnico cadastrado.</b><span>Use o botão “Incluir Alerta” para cadastrar a programação de manutenção.</span></div>';

  $$('[data-alert-view]').forEach((button) => button.onclick = () => openAlert(button.dataset.alertView, "view"));
  $$('[data-alert-edit]').forEach((button) => button.onclick = () => openAlert(button.dataset.alertEdit, "edit"));
  $$('[data-alert-delete]').forEach((button) => button.onclick = () => deleteAlert(button.dataset.alertDelete));
}
function fillAlertItems() {
  const f = $("#alertForm");
  const currentValue = f.itemId.value;
  f.itemId.innerHTML = alpha(
    registers.filter((r) => r.grupo === "MANUTENÇÃO" && r.ativo !== false),
    "item",
  ).map((r) => `<option value="${r.id}">${esc(r.item)}</option>`).join("");
  if ([...f.itemId.options].some((option) => option.value === currentValue)) f.itemId.value = currentValue;
}
function formatAlertDate(value) {
  const date = dateOnly(value);
  return date && !Number.isNaN(date.getTime()) ? date.toLocaleDateString("pt-BR") : "—";
}
function alertBaseForItem(vehicle, itemId) {
  const register = registers.find((r) => r.id === itemId);
  const related = movements.filter((m) =>
    (m.veiculo_id === vehicle.id || m.veiculo === vehicle.nome) &&
    m.grupo === "MANUTENÇÃO" &&
    ((m.item_id && m.item_id === itemId) || (!m.item_id && normalizeText(m.item) === normalizeText(register?.item))),
  ).sort(newestFirst)[0];
  return {
    date: String(related?.data_hora || new Date().toISOString()).slice(0, 10),
    km: Number(related?.hodometro_km ?? vehicleSummary(vehicle).last ?? vehicle.kmInicial ?? 0),
  };
}
function maintenanceAlertReference(row) {
  if (!row || row.grupo !== "MANUTENÇÃO") return null;
  const vehicle = vehicles.find((item) =>
    (row.veiculo_id && item.id === row.veiculo_id) || item.nome === row.veiculo);
  const register = registers.find((item) =>
    item.grupo === "MANUTENÇÃO" &&
    ((row.item_id && item.id === row.item_id) || normalizeText(item.item) === normalizeText(row.item)));
  return vehicle && register ? `${vehicle.id}|${register.id}` : null;
}
function synchronizeAlertsFromMaintenance(affectedRows = []) {
  const affected = new Set(affectedRows.map(maintenanceAlertReference).filter(Boolean));
  if (!affected.size) return 0;
  let updated = 0;
  alerts.forEach((item) => {
    const key = `${item.vehicleId}|${item.itemId}`;
    if (!affected.has(key)) return;
    const vehicle = vehicles.find((row) => row.id === item.vehicleId);
    if (!vehicle) return;
    const latest = alertBaseForItem(vehicle, item.itemId);
    const hasHistory = movements.some((row) => maintenanceAlertReference(row) === key);
    if (!hasHistory) return;
    item.baseDate = latest.date;
    item.baseKm = latest.km;
    item.dueDate = item.criterion !== "KM" && Number(item.recurrenceMonths) > 0
      ? addMonths(dateOnly(latest.date), Number(item.recurrenceMonths)).toISOString().slice(0, 10)
      : "";
    item.dueKm = item.criterion !== "DATE" && Number(item.recurrenceKm) > 0
      ? Number(latest.km) + Number(item.recurrenceKm)
      : 0;
    updated += 1;
  });
  return updated;
}
function deriveAlertBase(item, vehicle) {
  if (item?.baseDate || Number(item?.baseKm) > 0) {
    return {
      date: item.baseDate || new Date().toISOString().slice(0, 10),
      km: Number(item.baseKm || 0),
    };
  }
  const base = alertBaseForItem(vehicle, item?.itemId || "");
  if (item?.dueDate && Number(item.recurrenceMonths) > 0) {
    const due = dateOnly(item.dueDate);
    if (due) base.date = addMonths(due, -Number(item.recurrenceMonths)).toISOString().slice(0, 10);
  }
  if (Number(item?.dueKm) > 0 && Number(item.recurrenceKm) > 0)
    base.km = Math.max(0, Number(item.dueKm) - Number(item.recurrenceKm));
  return base;
}
function setAlertCriterion(value) {
  const f = $("#alertForm");
  const criterion = ["DATE", "KM", "BOTH"].includes(value) ? value : "BOTH";
  f.criterion.value = criterion;
  $$("[data-alert-criterion]").forEach((button) => {
    const selected = button.dataset.alertCriterion === criterion;
    button.classList.toggle("selected", selected);
    button.setAttribute("aria-pressed", String(selected));
  });
  f.recurrenceMonths.closest("label").classList.toggle("field-muted", criterion === "KM");
  f.recurrenceKm.closest("label").classList.toggle("field-muted", criterion === "DATE");
  updateAlertForecastPreview();
}
function setAlertStatusMode(value) {
  const f = $("#alertForm");
  const mode = value === "DISABLED" ? "DISABLED" : "ACTIVE";
  f.statusMode.value = mode;
  updateAlertForecastPreview();
}
function updateAlertForecastPreview() {
  const f = $("#alertForm");
  if (!f) return;
  const criterion = f.criterion.value || "BOTH";
  const baseDate = dateOnly(f.baseDate.value);
  const baseKm = Number(f.baseKm.value || 0);
  const months = Number(f.recurrenceMonths.value || 0);
  const kmLife = Number(f.recurrenceKm.value || 0);
  const vehicle = vehicles.find((v) => v.id === f.vehicleId.value);
  const currentKm = vehicle ? vehicleSummary(vehicle).last : baseKm;
  const today = new Date(); today.setHours(0, 0, 0, 0);
  const dueDate = criterion !== "KM" && baseDate && months > 0 ? addMonths(baseDate, months) : null;
  const dueKm = criterion !== "DATE" && kmLife > 0 ? baseKm + kmLife : 0;
  const days = dueDate ? Math.ceil((dueDate.getTime() - today.getTime()) / 86400000) : null;
  const kmRemaining = dueKm ? dueKm - currentKm : null;
  $("#alertForecastDate").textContent = dueDate ? formatAlertDate(dueDate.toISOString().slice(0, 10)) : "—";
  $("#alertForecastDateRemaining").textContent = days == null ? "Não utilizado" : days >= 0 ? `Em ${days} dia(s)` : `Vencido há ${Math.abs(days)} dia(s)`;
  $("#alertForecastKm").textContent = dueKm ? intFmt(dueKm) : "—";
  $("#alertForecastKmRemaining").textContent = kmRemaining == null ? "Não utilizado" : kmRemaining >= 0 ? `Restam ${intFmt(kmRemaining)} km` : `Vencido há ${intFmt(Math.abs(kmRemaining))} km`;
  $("#alertForecastDateCard").classList.toggle("inactive", criterion === "KM");
  $("#alertForecastKmCard").classList.toggle("inactive", criterion === "DATE");
  const expired = f.statusMode.value !== "DISABLED" &&
    ((criterion !== "KM" && dueDate && days <= 0) || (criterion !== "DATE" && dueKm > 0 && kmRemaining <= 0));
  $$("[data-alert-status]").forEach((button) => {
    const status = button.dataset.alertStatus;
    const selected = status === "EXPIRED" ? expired : !expired && status === f.statusMode.value;
    button.classList.toggle("selected", selected);
    button.classList.toggle("expired", status === "EXPIRED" && expired);
    button.setAttribute("aria-pressed", String(selected));
  });
  $("#alertObservationCount").textContent = String(f.observations.value.length);
}
function applyAlertItemDefaults({ preserveValues = false } = {}) {
  const f = $("#alertForm");
  const vehicle = vehicles.find((v) => v.id === f.vehicleId.value);
  const register = registers.find((r) => r.id === f.itemId.value);
  if (!vehicle || !register) return;
  if (!preserveValues) {
    const base = alertBaseForItem(vehicle, register.id);
    f.baseDate.value = base.date;
    f.baseKm.value = String(base.km || 0);
    const itemName = normalizeText(register.item);
    if (itemName.includes("oleo")) {
      f.recurrenceMonths.value = "6";
      f.recurrenceKm.value = "9000";
      f.leadDays.value = "15";
      f.leadKm.value = "1000";
      setAlertCriterion("BOTH");
    } else if (itemName.includes("bateria")) {
      f.recurrenceMonths.value = "36";
      f.recurrenceKm.value = "0";
      f.leadDays.value = "30";
      f.leadKm.value = "0";
      setAlertCriterion("DATE");
    }
  }
  updateAlertForecastPreview();
}

function setAlertFormMode(mode, item) {
  const f = $("#alertForm");
  const viewOnly = mode === "view";
  f.dataset.mode = viewOnly ? "view" : "edit";
  $("#alertFormTitle").textContent = viewOnly ? "Consultar Alerta Técnico" : item ? "Alterar Alerta Técnico" : "Novo Alerta Técnico";
  $("#alertTopSave").hidden = viewOnly;
  $("#saveAlertFromForm").hidden = viewOnly;
  $("#deleteAlertFromForm").hidden = viewOnly || !item;
  $("#deleteAlertFromForm").disabled = !item;
  $("#alertFooterCancel").textContent = viewOnly ? "Fechar" : "Cancelar";
  f.querySelectorAll('input:not([type="hidden"]), select, textarea').forEach((field) => {
    field.disabled = viewOnly || field.name === "group";
  });
  $$('[data-alert-criterion], [data-alert-status]').forEach((button) => {
    button.disabled = viewOnly || button.dataset.alertStatus === "EXPIRED";
  });
  f.classList.toggle("is-view-only", viewOnly);
}

function openAlert(id = "", mode = "edit") {
  const f = $("#alertForm"), item = alerts.find((a) => a.id === id);
  f.reset();
  f.elements.id.value = id;
  const selectedVehicle = item
    ? vehicles.find((v) => v.id === item.vehicleId)
    : selectedVehicleObject();
  if (!item && (!selectedVehicle || selectedVehicle.ativo === false)) {
    alert("Selecione um veículo ativo antes de incluir um alerta técnico.");
    return;
  }
  f.vehicleId.value = item?.vehicleId || selectedVehicle?.id || "";
  f.group.value = "MANUTENÇÃO";
  fillAlertItems();
  if (item?.itemId && [...f.itemId.options].some((option) => option.value === item.itemId)) {
    f.itemId.value = item.itemId;
  } else if (!item) {
    const defaultRegister = registers.find((register) => register.grupo === "MANUTENÇÃO" && register.padrao && register.ativo !== false)
      || registers.find((register) => register.grupo === "MANUTENÇÃO" && register.ativo !== false);
    if (defaultRegister && [...f.itemId.options].some((option) => option.value === defaultRegister.id)) f.itemId.value = defaultRegister.id;
  }
  const selectedRegister = registers.find((register) => register.id === f.itemId.value);
  const base = deriveAlertBase(item, selectedVehicle);
  const selectedName = normalizeText(selectedRegister?.item);
  const isBattery = selectedName.includes("bateria");
  const defaultCriterion = isBattery ? "DATE" : "BOTH";
  const criterion = item?.criterion || defaultCriterion;
  const defaultMonths = isBattery ? 36 : 6;
  const defaultKm = isBattery ? 0 : 9000;
  f.recurrenceMonths.value = String(item?.recurrenceMonths ?? defaultMonths);
  f.recurrenceKm.value = String(item?.recurrenceKm ?? defaultKm);
  f.baseDate.value = base.date;
  f.baseKm.value = String(base.km || 0);
  f.leadDays.value = String(item?.leadDays ?? (isBattery ? 30 : 15));
  f.leadKm.value = String(item?.leadKm ?? (isBattery ? 0 : 1000));
  f.observations.value = item?.observations || "";
  f.statusMode.value = item?.active === false ? "DISABLED" : "ACTIVE";
  $("#alertVehicleName").textContent = selectedVehicle?.nome || "Veículo não localizado";
  $("#alertVehicleMeta").textContent = `Placa: ${selectedVehicle?.placa || "Não informada"} · KM atual: ${intFmt(selectedVehicle ? vehicleSummary(selectedVehicle).last : 0)}`;
  $("#alertFormError").textContent = "";
  setAlertCriterion(criterion);
  setAlertStatusMode(f.statusMode.value);
  updateAlertForecastPreview();
  setAlertFormMode(mode, item);
  $("#alertDialog").showModal();
}
$("#newAlert").onclick = () => openAlert("", "edit");
$$(".alert-cancel").forEach((button) => button.onclick = () => $("#alertDialog").close());
$$("[data-alert-criterion]").forEach((button) => button.onclick = () => setAlertCriterion(button.dataset.alertCriterion));
$$("[data-alert-status]").forEach((button) => {
  button.onclick = () => {
    if (button.dataset.alertStatus === "EXPIRED") return;
    setAlertStatusMode(button.dataset.alertStatus);
  };
});
["recurrenceMonths", "recurrenceKm", "baseDate", "baseKm", "leadDays", "leadKm"].forEach((name) => {
  $("#alertForm").elements[name].addEventListener("input", updateAlertForecastPreview);
  $("#alertForm").elements[name].addEventListener("change", updateAlertForecastPreview);
});
$("#alertForm").itemId.addEventListener("change", () => applyAlertItemDefaults());
$("#alertForm").observations.addEventListener("input", updateAlertForecastPreview);
$("#deleteAlertFromForm").onclick = () => {
  const id = $("#alertForm").elements.id.value;
  if (id && deleteAlert(id)) $("#alertDialog").close();
};
$("#alertForm").onsubmit = (event) => {
  event.preventDefault();
  const f = event.target;
  if (f.dataset.mode === "view") return;
  const data = Object.fromEntries(new FormData(f));
  data.vehicleId = f.vehicleId.value;
  data.group = "MANUTENÇÃO";
  const current = alerts.find((a) => a.id === data.id);
  const vehicle = vehicles.find((v) => v.id === data.vehicleId);
  const register = registers.find((r) => r.id === data.itemId);
  if (!vehicle || vehicle.ativo === false)
    return ($("#alertFormError").textContent = "Alertas somente podem ser gravados para veículos ativos.");
  if (!register)
    return ($("#alertFormError").textContent = "Selecione o item de manutenção.");
  const criterion = data.criterion;
  const months = Number(data.recurrenceMonths || 0);
  const kmLife = Number(data.recurrenceKm || 0);
  const baseDate = data.baseDate;
  const baseKm = Number(data.baseKm || 0);
  if (criterion !== "KM" && (!baseDate || !(months > 0)))
    return ($("#alertFormError").textContent = "Informe a data base e a vida útil em meses.");
  if (criterion !== "DATE" && (!(baseKm >= 0) || !(kmLife > 0)))
    return ($("#alertFormError").textContent = "Informe o KM base e a vida útil em quilômetros.");
  const dueDate = criterion !== "KM" ? addMonths(dateOnly(baseDate), months).toISOString().slice(0, 10) : "";
  const dueKm = criterion !== "DATE" ? baseKm + kmLife : 0;
  const stateBeforeSave = cloneDataState();
  const active = data.statusMode !== "DISABLED";
  const object = {
    ...current,
    id: data.id || crypto.randomUUID(),
    modelVersion: 2,
    vehicleId: data.vehicleId,
    group: "MANUTENÇÃO",
    itemId: data.itemId,
    description: register.item,
    criterion,
    recurrenceMonths: months,
    recurrenceKm: kmLife,
    baseDate,
    baseKm,
    dueDate,
    dueKm,
    leadDays: Number(data.leadDays || 0),
    leadKm: Number(data.leadKm || 0),
    observations: String(data.observations || "").slice(0, 300),
    statusMode: active ? "ACTIVE" : "DISABLED",
    active,
  };
  if (current) Object.assign(current, object); else alerts.push(object);
  try {
    save();
    $("#alertDialog").close();
    showToast("Alerta técnico salvo com sucesso.");
  } catch (error) {
    restoreDataState(stateBeforeSave);
    console.error("Falha ao salvar o alerta técnico:", error);
    $("#alertFormError").textContent = "Não foi possível salvar o alerta. Verifique os dados e tente novamente.";
  }
};
function applyTheme(mode) {
  const dark = mode === "dark" || (mode === "auto" && matchMedia("(prefers-color-scheme: dark)").matches);
  document.documentElement.dataset.theme = dark ? "dark" : "light";
  localStorage.setItem("mycar_theme", mode);
  $$("[data-theme-mode]").forEach((button) => {
    button.classList.toggle("active", button.dataset.themeMode === mode);
    button.setAttribute("aria-pressed", String(button.dataset.themeMode === mode));
  });
}
$$("[data-theme-mode]").forEach((button) => {
  button.onclick = () => {
    const returnPage = pageTrail.at(-1);
    applyTheme(button.dataset.themeMode);
    if (currentPageId === "configuracoes" && returnPage && returnPage !== "configuracoes") {
      pageTrail.pop();
      go(returnPage, { fromBack: true, instant: true });
    }
  };
});
$$("[data-quick-theme]").forEach((button) => {
  button.onclick = (event) => {
    event.stopPropagation();
    applyTheme(button.dataset.quickTheme);
    headerMenu.hidden = true;
    menuBtn.setAttribute("aria-expanded", "false");
  };
});
applyTheme(localStorage.getItem("mycar_theme") || "dark");
const gpsRadiusSelect = $("#gpsSupplierRadius");
if (gpsRadiusSelect) {
  gpsRadiusSelect.value = String(gpsSupplierRadiusMeters());
  gpsRadiusSelect.onchange = () => {
    localStorage.setItem("mycar_gps_supplier_radius", gpsRadiusSelect.value);
  };
}
const systemTheme = matchMedia("(prefers-color-scheme: dark)");
systemTheme.addEventListener?.("change", () => {
  if ((localStorage.getItem("mycar_theme") || "dark") === "auto") applyTheme("auto");
});
let lastAiReport = null;

function escapeAiHtml(value) {
  return String(value ?? "").replace(/[&<>"']/g, (char) => ({
    "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;",
  })[char]);
}

function aiPeriodRows(vehicleId, start, end) {
  const vehicle = vehicles.find((v) => v.id === vehicleId);
  const name = vehicle?.nome || "";
  return movements.filter((m) => {
    const day = String(m.data_hora || "").slice(0, 10);
    return (m.veiculo_id === vehicleId || (!m.veiculo_id && m.veiculo === name)) &&
      day >= start && day <= end;
  });
}

function buildAiIndicators(vehicleId, start, end, analysisType) {
  const vehicle = vehicles.find((v) => v.id === vehicleId);
  const rows = aiPeriodRows(vehicleId, start, end);
  const movementMap = new Map();
  rows.forEach((row) => {
    const key = row.movimento_id || row.id;
    if (!movementMap.has(key)) movementMap.set(key, row);
  });
  const uniqueMovements = [...movementMap.values()];
  const expenseRows = rows.filter((m) => m.grupo !== "RECEITA");
  const incomeRows = rows.filter((m) => m.grupo === "RECEITA");
  const gross = expenseRows.reduce((sum, m) => sum + (+m.valor || 0), 0);
  const income = incomeRows.reduce((sum, m) => sum + (+m.valor || 0), 0);
  const odometers = uniqueMovements.map((m) => +m.hodometro_km || 0).filter(Boolean);
  const distance = odometers.length > 1 ? Math.max(...odometers) - Math.min(...odometers) : 0;
  const days = Math.max(1, Math.round((new Date(end + "T12:00:00") - new Date(start + "T12:00:00")) / 86400000) + 1);
  const completeFuel = rows.filter((m) => m.grupo === "COMBUSTÍVEL" && m.tanque_completo !== "NAO" && (+m.quantidade_litros || 0) > 0);
  const incompleteFuel = rows.filter((m) => m.grupo === "COMBUSTÍVEL" && m.tanque_completo === "NAO");
  const fuelLiters = completeFuel.reduce((sum, m) => sum + (+m.quantidade_litros || 0), 0);
  const fuelDistance = completeFuel.reduce((sum, m) => sum + (+(m.distancia_abastecimento_km ?? m.distancia_km) || 0), 0);
  const byGroup = {};
  rows.forEach((m) => {
    byGroup[m.grupo] = (byGroup[m.grupo] || 0) + (+m.valor || 0);
  });
  const byFuel = {};
  completeFuel.forEach((m) => {
    const key = m.item || "Não informado";
    byFuel[key] ||= { litros: 0, valor: 0, distancia: 0, registros: 0 };
    byFuel[key].litros += +m.quantidade_litros || 0;
    byFuel[key].valor += +m.valor || 0;
    byFuel[key].distancia += +(m.distancia_abastecimento_km ?? m.distancia_km) || 0;
    byFuel[key].registros++;
  });
  Object.values(byFuel).forEach((x) => {
    x.consumo_km_l = x.litros ? x.distancia / x.litros : 0;
    x.custo_km = x.distancia ? x.valor / x.distancia : 0;
  });
  const suppliersSummary = {};
  rows.filter((m) => m.grupo !== "RECEITA").forEach((m) => {
    const key = m.fornecedor || "NI - Não informado";
    suppliersSummary[key] ||= { registros: 0, valor: 0 };
    suppliersSummary[key].registros++;
    suppliersSummary[key].valor += +m.valor || 0;
  });
  const values = expenseRows.map((m) => +m.valor || 0);
  const avgValue = values.length ? values.reduce((a, b) => a + b, 0) / values.length : 0;
  const highValues = expenseRows.filter((m) => avgValue && +m.valor > avgValue * 2.5)
    .slice(0, 5).map((m) => ({ data: String(m.data_hora).slice(0, 10), item: m.item, valor: +m.valor }));
  const vehicleAlerts = alerts.filter((item) => item.vehicleId === vehicleId).map((item) => ({
    description: item.description,
    status: alertStatus(item),
    forecast: alertForecast(item),
    technical: true,
    active: Boolean(item.active),
  }));
  return {
    schema_version: 1,
    analysis_type: analysisType,
    vehicle: {
      id: vehicleId, name: vehicle?.nome || "Veículo", plate: vehicle?.placa || "", active: vehicle?.ativo !== false,
      consumption_reference: {
        ethanol_city: +vehicle?.consumoEtanolCidade || 0, ethanol_road: +vehicle?.consumoEtanolEstrada || 0,
        gasoline_city: +vehicle?.consumoGasolinaCidade || 0, gasoline_road: +vehicle?.consumoGasolinaEstrada || 0,
        diesel_city: +vehicle?.consumoDieselCidade || 0, diesel_road: +vehicle?.consumoDieselEstrada || 0,
      },
    },
    period: { start, end, days },
    sample: { movements: uniqueMovements.length, item_rows: rows.length, complete_refuels: completeFuel.length, incomplete_refuels: incompleteFuel.length },
    financial: { gross_cost: gross, income, net_cost: gross - income, cost_per_km: distance ? (gross - income) / distance : 0, daily_cost: (gross - income) / days },
    usage: { distance_km: distance, daily_km: distance / days, min_odometer: odometers.length ? Math.min(...odometers) : 0, max_odometer: odometers.length ? Math.max(...odometers) : 0 },
    fuel: { complete_liters: fuelLiters, consumption_km_l: fuelLiters ? fuelDistance / fuelLiters : 0, by_type: byFuel },
    costs_by_group: byGroup,
    suppliers: suppliersSummary,
    maintenance: rows.filter((m) => m.grupo === "MANUTENÇÃO").map((m) => ({ date: String(m.data_hora).slice(0, 10), item: m.item, odometer_km: +m.hodometro_km || 0, value: +m.valor || 0 })).slice(-30),
    alert_snapshot: vehicleAlerts,
    quality: { missing_supplier: rows.filter((m) => !m.fornecedor || /^N\.?I/i.test(m.fornecedor)).length, unusually_high_values: highValues },
  };
}

function aiHealthSummary(indicators) {
  const activeAlerts = (indicators.alert_snapshot || []).filter((a) => a.active);
  const critical = activeAlerts.filter((a) => /vencid|crític|atrasad/i.test(`${a.status} ${a.forecast}`)).length;
  const attention = Math.max(0, activeAlerts.length - critical);
  const missing = +indicators.quality?.missing_supplier || 0;
  const rows = +indicators.sample?.item_rows || 0;
  const incomplete = +indicators.sample?.incomplete_refuels || 0;
  const refuels = (+indicators.sample?.complete_refuels || 0) + incomplete;
  let score = 100;
  score -= Math.min(35, critical * 15);
  score -= Math.min(20, attention * 5);
  score -= rows ? Math.min(10, (missing / rows) * 20) : 5;
  score -= refuels ? Math.min(10, (incomplete / refuels) * 20) : 5;
  score = Math.max(0, Math.round(score));
  const status = score >= 85 ? "Excelente" : score >= 70 ? "Bom" : score >= 50 ? "Atenção" : "Crítico";
  return { score, status, critical, attention };
}

function aiRecommendationRows(items) {
  return (items || []).map((text, index) => {
    const priority = index === 0 ? "Alta" : index < 3 ? "Média" : "Baixa";
    const impact = priority === "Alta" ? "Redução de risco ou custo prioritário" : priority === "Média" ? "Melhoria operacional relevante" : "Aprimoramento preventivo";
    return `<tr><td data-label="Prioridade"><span class="ai-priority ai-priority-${priority.toLowerCase()}">${priority}</span></td><td data-label="Recomendação">${escapeAiHtml(text)}</td><td data-label="Impacto esperado">${impact}</td></tr>`;
  }).join("") || '<tr><td colspan="3">Nenhuma recomendação relevante.</td></tr>';
}

function aiAnomalyRows(items) {
  return (items || []).map((text, index) => {
    const level = /vencid|crític|risco|falha|muito acima/i.test(text) ? "Crítico" : index === 0 ? "Atenção" : "Normal";
    return `<li><strong>${level}:</strong> ${escapeAiHtml(text)}</li>`;
  }).join("") || "<li>Nenhuma ocorrência relevante.</li>";
}

function renderAiReport(report) {
  const k = report.indicators;
  const health = aiHealthSummary(k);
  const annualProjection = k.period.days ? (k.financial.net_cost / k.period.days) * 365 : 0;
  const topCost = Object.entries(k.costs_by_group || {}).sort((a,b) => b[1] - a[1])[0] || ["Sem dados", 0];
  const mainRisk = health.critical ? `${health.critical} alerta(s) crítico(s)` : health.attention ? `${health.attention} alerta(s) em atenção` : "Nenhum alerta crítico ativo";
  const mainRecommendation = (report.recommendations || ["Manter os registros atualizados."])[0];
  return `<p class="ai-meta"><b>Veículo:</b> ${escapeAiHtml(k.vehicle.name)} · <b>Período:</b> ${escapeAiHtml(k.period.start)} a ${escapeAiHtml(k.period.end)} · <b>Movimentos:</b> ${k.sample.movements}</p>
  <div class="ai-kpis"><div><small>Saúde veicular</small><strong>${health.score}/100 · ${health.status}</strong></div><div><small>Distância</small><strong>${intFmt(k.usage.distance_km)} km</strong></div><div><small>Custo líquido</small><strong>${money(k.financial.net_cost)}</strong></div><div><small>Custo por km</small><strong>${money(k.financial.cost_per_km)}</strong></div><div><small>Consumo médio</small><strong>${num(k.fuel.consumption_km_l,2)} km/L</strong></div><div><small>Confiança</small><strong>${escapeAiHtml(report.confidence)}</strong></div></div>
  <div class="ai-executive-cards"><div><small>Principal custo</small><strong>${escapeAiHtml(topCost[0])}</strong><span>${money(topCost[1])}</span></div><div><small>Principal risco</small><strong>${escapeAiHtml(mainRisk)}</strong></div><div><small>Principal recomendação</small><strong>${escapeAiHtml(mainRecommendation)}</strong></div></div>
  <section class="ai-report-section"><h4>1. Resumo executivo</h4><p>${escapeAiHtml(report.executive_summary)}</p></section>
  <section class="ai-report-section"><h4>2. Combustível e consumo</h4><p>${escapeAiHtml(report.fuel_analysis)}</p><small>${k.sample.incomplete_refuels} abastecimento(s) incompleto(s) excluído(s) do consumo em km/L e mantido(s) nos custos.</small></section>
  <section class="ai-report-section"><h4>3. Custos, uso e projeção</h4><p>${escapeAiHtml(report.cost_analysis)}</p><p><b>Projeção anual pelo ritmo do período:</b> ${money(annualProjection)}. A projeção é linear e não representa garantia de gasto futuro.</p></section>
  <section class="ai-report-section"><h4>4. Anomalias classificadas</h4><ul>${aiAnomalyRows(report.anomalies)}</ul></section>
  <section class="ai-report-section"><h4>5. Manutenção e alertas</h4><p>${escapeAiHtml(report.maintenance_analysis)}</p><p><b>Painel:</b> ${health.critical} crítico(s), ${health.attention} em atenção e ${(k.alert_snapshot || []).filter(a=>a.active).length} alerta(s) ativo(s).</p></section>
  <section class="ai-report-section"><h4>6. Fornecedores</h4><p>${escapeAiHtml(report.supplier_analysis)}</p></section>
  <section class="ai-report-section"><h4>7. Plano de ação priorizado</h4><table class="ai-action-table"><thead><tr><th>Prioridade</th><th>Recomendação</th><th>Impacto esperado</th></tr></thead><tbody>${aiRecommendationRows(report.recommendations)}</tbody></table></section>
  <section class="ai-report-section"><h4>8. Critérios e limitações</h4><p>${escapeAiHtml(report.limitations)}</p><small>Indicadores calculados pelo MyCar+. Interpretação gerada por Inteligência Artificial. Esta análise não substitui avaliação mecânica profissional.</small></section>`;
}

function openAiPrintableReport() {
  if (!lastAiReport) return;
  const reportBody = renderAiReport(lastAiReport);
  const documentHtml = `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1,maximum-scale=1"><title>Relatório MyCar+ Intelligence</title><style>
  :root{--navy:#12395b;--blue:#0788e8;--surface:#f1f7fb;--border:#c7d8e4;--text:#243746;--muted:#607080}*{box-sizing:border-box}html,body{width:100%;max-width:100%;overflow-x:hidden}body{font-family:Arial,sans-serif;color:var(--text);line-height:1.45;width:100%;max-width:185mm;margin:0 auto;padding:14px 10px 0;background:#fff;overflow-wrap:anywhere}h1,h2,h4{color:var(--navy)}.header{border-bottom:3px solid var(--blue);margin-bottom:16px}.header h1{margin-bottom:6px;line-height:1.15}.header p{margin-top:0}.ai-report-brand{display:flex;align-items:center;gap:12px}.ai-report-brand img{width:58px;height:58px;object-fit:contain;border-radius:12px;flex:0 0 auto}.ai-meta{color:var(--muted)}.ai-kpis{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:8px}.ai-kpis div{min-width:0;background:var(--surface);padding:10px;border:1px solid var(--border);border-radius:8px}.ai-kpis small{display:block;color:var(--muted)}.ai-kpis strong{display:block;margin-top:4px;overflow-wrap:anywhere}.ai-executive-cards{display:grid;grid-template-columns:repeat(3,minmax(0,1fr));gap:10px;margin:14px 0}.ai-executive-cards>div{min-width:0;border:1px solid var(--border);border-radius:10px;padding:12px;background:#f8fbfd}.ai-executive-cards small,.ai-executive-cards span{display:block;color:var(--muted)}.ai-executive-cards strong{display:block;margin:5px 0;overflow-wrap:anywhere}.ai-report-section{break-inside:avoid;margin:18px 0;max-width:100%}.ai-report-section p,.ai-report-section li{overflow-wrap:anywhere}.ai-action-table{width:100%;max-width:100%;border-collapse:collapse;table-layout:fixed}.ai-action-table th,.ai-action-table td{border:1px solid var(--border);padding:8px;text-align:left;vertical-align:top;overflow-wrap:anywhere}.ai-action-table th:nth-child(1){width:18%}.ai-action-table th:nth-child(2){width:48%}.ai-action-table th:nth-child(3){width:34%}.ai-priority{display:inline-block;border-radius:999px;padding:3px 8px;font-weight:800}.ai-priority-alta{background:#fde8e7;color:#9f201a}.ai-priority-média{background:#fff3d8;color:#845400}.ai-priority-baixa{background:#e8f5ee;color:#17663d}.footer{margin-top:25px;border-top:1px solid #ccd9e2;padding-top:8px;color:var(--muted);font-size:11px}.report-actions{display:flex;justify-content:center;gap:10px;margin:24px -10px 0;padding:14px 12px calc(14px + env(safe-area-inset-bottom));background:var(--navy)}.report-actions button{border:0;border-radius:8px;padding:11px 16px;font-weight:700;cursor:pointer;min-height:44px}.report-actions .primary{background:var(--blue);color:#fff}.report-actions .secondary{background:#fff;color:var(--navy)}.report-actions .danger{background:#b3261e;color:#fff}@page{size:A4 portrait;margin:14mm}@media print{html,body{overflow:visible}body{max-width:none;padding:0}.report-actions{display:none}}@media screen and (max-width:680px){body{padding:12px 10px 0}.header h1{font-size:21px}.header p{font-size:12px}.ai-meta{font-size:12px}.ai-kpis{grid-template-columns:repeat(2,minmax(0,1fr));gap:7px}.ai-kpis div{padding:9px}.ai-kpis strong{font-size:14px}.ai-executive-cards{grid-template-columns:1fr;gap:8px}.ai-report-section{margin:15px 0}.ai-report-section h4{font-size:16px;margin-bottom:7px}.ai-report-section p,.ai-report-section li{font-size:13px}.ai-action-table{display:block;border:0}.ai-action-table thead{display:none}.ai-action-table tbody{display:block}.ai-action-table tr{display:block;margin-bottom:10px;border:1px solid var(--border);border-radius:8px;overflow:hidden}.ai-action-table td{display:grid;grid-template-columns:minmax(100px,38%) minmax(0,1fr);gap:8px;width:100%;border:0;border-bottom:1px solid #e1e9ee;padding:8px;font-size:12px}.ai-action-table td:last-child{border-bottom:0}.ai-action-table td::before{content:attr(data-label);font-weight:800;color:var(--navy);font-size:11px}.report-actions{position:sticky;bottom:0;z-index:20;flex-wrap:wrap}.report-actions button{flex:1 1 145px}}@media screen and (max-width:390px){.ai-kpis{grid-template-columns:1fr}.report-actions button{flex-basis:100%}.ai-action-table td{grid-template-columns:minmax(92px,36%) minmax(0,1fr)}}
  </style></head><body><div class="header"><div class="ai-report-brand"><img src="${REPORT_LOGO_DATA_URI}" alt="MyCar+"><div><h1>Relatório Executivo de Gestão Veicular</h1><p><strong>Gerado com Inteligência Artificial</strong> · MyCar+ Intelligence · emissão ${new Date().toLocaleString("pt-BR")}</p></div></div></div>${reportBody}<div class="footer">Relatório gerado a partir dos dados informados no MyCar+. Os indicadores são calculados pelo MyCar+. A Inteligência Artificial interpreta os resultados e gera diagnósticos, tendências e recomendações. A qualidade das conclusões depende da consistência dos dados registrados.</div><div class="report-actions"><button type="button" id="aiReportPrint" class="secondary">Imprimir</button><button type="button" id="aiReportShare" class="primary">Compartilhar</button><button type="button" id="aiReportClose" class="danger">Fechar</button></div><script>(function(){function printable(){var clone=document.documentElement.cloneNode(true);clone.querySelectorAll('.report-actions,script').forEach(function(n){n.remove()});return '<!doctype html>'+clone.outerHTML}function printHtml(){var html=printable();try{if(window.parent&&window.parent.MyCarReportManager){window.parent.MyCarReportManager.print('RELATORIO_IA_MYCAR_PLUS',html);return}if(window.top&&window.top.MyCarReportManager){window.top.MyCarReportManager.print('RELATORIO_IA_MYCAR_PLUS',html);return}if(window.parent&&window.parent!==window){window.parent.postMessage({type:'mycar-print-report-html',jobName:'RELATORIO_IA_MYCAR_PLUS',html:html},'*');return}}catch(e){console.error('Falha ao acionar gerenciador de impressão:',e)}window.print()}function shareHtml(){try{var html=printable();if(window.parent&&window.parent.MyCarReportManager){window.parent.MyCarReportManager.share('RELATORIO_IA_MYCAR_PLUS',html);return}if(window.top&&window.top.MyCarReportManager){window.top.MyCarReportManager.share('RELATORIO_IA_MYCAR_PLUS',html);return}if(window.parent&&window.parent!==window){window.parent.postMessage({type:'mycar-share-report-html',jobName:'RELATORIO_IA_MYCAR_PLUS',html:html},'*');return}}catch(e){}try{var html=printable();var blob=new Blob([html],{type:'text/html;charset=utf-8'});var url=URL.createObjectURL(blob);var a=document.createElement('a');a.href=url;a.download='RELATORIO_IA_MYCAR_PLUS.html';document.body.appendChild(a);a.click();a.remove();setTimeout(function(){URL.revokeObjectURL(url)},1500)}catch(e){alert('Não foi possível preparar o arquivo HTML.')}}function closeMe(){try{if(window.parent&&window.parent.MyCarReportManager){window.parent.MyCarReportManager.close();return}if(window.parent&&window.parent!==window){window.parent.postMessage({type:'mycar-close-report'},'*');return}}catch(e){}try{window.close()}catch(e){}setTimeout(function(){if(!window.closed){try{history.length>1?history.back():location.replace('about:blank')}catch(e){}}},150)}document.getElementById('aiReportPrint').addEventListener('click',printHtml);document.getElementById('aiReportClose').addEventListener('click',closeMe);document.getElementById('aiReportShare').addEventListener('click',shareHtml);document.addEventListener('keydown',function(e){if(e.key==='Escape')closeMe()});})();<\/script></body></html>`;
  openReportDocument(documentHtml, {
    title: "Relatório com Inteligência Artificial",
    jobName: "RELATORIO_IA_MYCAR_PLUS",
    popupMessage: "Permita janelas pop-up para visualizar o relatório.",
  });
}


function helpManualHtml() {
  const source = document.getElementById("helpManualContent");
  if (!source) return "";
  const clone = source.cloneNode(true);
  clone.querySelectorAll("button,.help-share-actions").forEach((node) => node.remove());
  clone.querySelectorAll("details").forEach((detail) => detail.open = true);
  const generatedAt = new Date().toLocaleString("pt-BR", { dateStyle: "short", timeStyle: "short" });
  const developer = "Marcelo Ribeiro";
  const createdAt = "julho de 2026";
  return `<!doctype html><html lang="pt-BR"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>Manual de Ajuda — MyCar+ V${APP_VERSION}</title><link rel="icon" href="${REPORT_LOGO_DATA_URI}"><style>
  :root{font-family:Arial,sans-serif;color:#16324a;background:#e9edf0}*{box-sizing:border-box}body{margin:0;background:#e9edf0;color:#16324a}.manual-cover{width:min(210mm,100%);min-height:297mm;margin:0 auto;display:flex;flex-direction:column;align-items:center;justify-content:center;padding:22mm 18mm;background:linear-gradient(145deg,#0b2b4f 0%,#0e5487 52%,#0788e8 100%);color:#fff;text-align:center;position:relative;overflow:hidden}.manual-cover:before{content:"";position:absolute;inset:14mm;border:1px solid rgba(255,255,255,.3);border-radius:18px;background:rgba(255,255,255,.07)}.manual-cover-content{position:relative;z-index:1;width:100%;max-width:680px}.manual-cover img{width:190px;height:190px;object-fit:contain;border-radius:34px;filter:drop-shadow(0 16px 28px rgba(0,0,0,.28))}.manual-cover .brand{font-size:30px;font-weight:800;margin-top:18px}.manual-cover h1{margin:18px 0 28px;font-size:42px;line-height:1.08;letter-spacing:.04em}.manual-meta{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px;max-width:620px;margin:0 auto}.manual-meta-item{min-width:0;padding:12px 14px;border:1px solid rgba(255,255,255,.32);border-radius:12px;background:rgba(4,37,67,.28)}.manual-meta-item small{display:block;font-size:11px;line-height:1.2;text-transform:uppercase;letter-spacing:.08em;color:#d7ebfa}.manual-meta-item strong{display:block;margin-top:5px;font-size:16px;line-height:1.28;overflow-wrap:anywhere}.manual{max-width:900px;margin:20px auto;background:#fff;border:1px solid #d7e1e8;border-radius:16px;padding:28px;box-shadow:0 10px 28px rgba(15,50,75,.08)}.manual header{border-bottom:3px solid #1769aa;padding-bottom:16px;margin-bottom:20px}.manual-brand{display:flex;align-items:center;gap:14px;min-width:0}.manual-brand img{width:76px;height:76px;object-fit:contain;border-radius:16px;flex:0 0 auto}.manual-brand h1{margin:0;color:#12395b;font-size:26px;line-height:1.15}.document-meta{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:8px;margin-top:12px}.document-meta div{min-width:0;padding:8px 10px;border-radius:8px;background:#edf5fa;border:1px solid #c8ddea}.document-meta small{display:block;color:#667884;font-size:10px;text-transform:uppercase;letter-spacing:.05em}.document-meta strong{display:block;margin-top:3px;color:#12395b;font-size:13px;line-height:1.25;overflow-wrap:anywhere}details{border:1px solid #d7e1e8;border-radius:10px;margin:9px 0;padding:10px 12px;break-inside:avoid}summary{font-weight:800;color:#12395b}p{line-height:1.55}.footer{margin-top:24px;padding-top:12px;border-top:1px solid #d7e1e8;color:#667884;font-size:12px}.actions{display:flex;gap:10px;justify-content:center;margin-top:20px}.actions button{border:0;border-radius:9px;padding:11px 16px;font-weight:800;cursor:pointer}.actions .primary{background:#1769aa;color:#fff}.actions .secondary{background:#eef3f6;color:#12395b}@media(max-width:600px){.manual-cover{min-height:100vh;padding:26px 16px}.manual-cover:before{inset:12px}.manual-cover img{width:128px;height:128px;border-radius:26px}.manual-cover h1{font-size:30px;margin:14px 0 20px}.manual-cover .brand{font-size:24px}.manual-meta,.document-meta{grid-template-columns:1fr}.manual-meta-item{padding:9px 10px}.manual-meta-item strong{font-size:14px}.manual{margin:10px;padding:16px;border-radius:12px}.manual-brand{align-items:flex-start}.manual-brand img{width:58px;height:58px}.manual-brand h1{font-size:21px}}@page{size:A4 portrait;margin:0}@media print{html,body{background:#fff!important;-webkit-print-color-adjust:exact!important;print-color-adjust:exact!important}.manual-cover{display:none!important}.manual{box-shadow:none;border:0;border-radius:0;max-width:none;margin:0;padding:12mm}.actions{display:none}}
  </style></head><body><main class="manual"><header><div class="manual-brand"><img src="${REPORT_LOGO_DATA_URI}" alt="MyCar+"><div><h1>Manual de Ajuda — MyCar+</h1><div class="document-meta"><div><small>Versão</small><strong>${APP_VERSION}</strong></div><div><small>Desenvolvedor</small><strong>${developer}</strong></div><div><small>Criação do sistema</small><strong>${createdAt}</strong></div><div><small>Geração do documento</small><strong>${generatedAt}</strong></div></div></div></div></header>${clone.innerHTML}<div class="actions"><button class="primary" onclick="window.print()">Imprimir</button><button class="secondary" onclick="window.print()">Salvar como PDF</button></div><div class="footer">MyCar+ · Manual de Ajuda · V${APP_VERSION}</div></main></body></html>`;
}
async function shareHelpManual() {
  const html = helpManualHtml();
  if (!html) return alert("Não foi possível preparar o manual de ajuda.");
  const fileName = `MANUAL_AJUDA_MYCAR_PLUS_V${APP_VERSION.replace(".", "_")}.html`;
  try {
    const bridge = window.MyCarNative;
    if (isNativeApp() && bridge && typeof bridge.shareHtml === "function") {
      bridge.shareHtml(fileName.replace(/\.html$/i, ""), html);
      return;
    }
  } catch (error) { console.warn("Compartilhamento Android indisponível:", error); }
  const file = new File([html], fileName, { type: "text/html;charset=utf-8" });
  if (navigator.share && navigator.canShare?.({ files: [file] })) {
    try { await navigator.share({ title: `Manual MyCar+ V${APP_VERSION}`, files: [file] }); return; }
    catch (error) { if (error?.name === "AbortError") return; }
  }
  const url = URL.createObjectURL(file);
  const link = document.createElement("a");
  link.href = url; link.download = fileName; document.body.appendChild(link); link.click(); link.remove();
  setTimeout(() => URL.revokeObjectURL(url), 1500);
  showToast("Manual HTML preparado para download.");
}

document.addEventListener("click", (event) => {
  const menu = document.getElementById("vehicleSelectMenu");
  const toggle = document.getElementById("vehicleSelectToggle");
  if (menu && toggle && !menu.hidden && !event.target.closest(".vehicle-select-shell")) {
    menu.hidden = true; toggle.setAttribute("aria-expanded", "false");
    const arrow = toggle.querySelector(".vehicle-select-arrow"); if (arrow) arrow.textContent = "▼";
  }
});
document.addEventListener("keydown", (event) => {
  if (event.key !== "Escape") return;
  const menu = document.getElementById("vehicleSelectMenu");
  const toggle = document.getElementById("vehicleSelectToggle");
  if (menu && toggle && !menu.hidden) { menu.hidden = true; toggle.setAttribute("aria-expanded", "false"); toggle.focus(); }
});
const shareHelpButton = document.getElementById("shareHelpManual");
if (shareHelpButton) shareHelpButton.addEventListener("click", shareHelpManual);

function initializeAiModule() {
  const form=$("#aiAnalysisForm"); if(!form)return;
  const refreshVehicleContext=()=>{const vehicle=selectedVehicleObject(); $("#aiVehicleName").textContent=vehicle?`${vehicle.nome}${vehicle.placa?" · "+vehicle.placa:""}`:"Nenhum veículo selecionado"; $("#aiVehicleStatus").textContent=vehicle?(vehicle.ativo===false?"Veículo inativo · consulta histórica":"Veículo ativo"):""; if(!vehicle)return; const dates=movements.filter((m)=>m.veiculo_id===vehicle.id||m.veiculo===vehicle.nome).map((m)=>String(m.data_hora||"").slice(0,10)).filter(Boolean).sort(); if(!$("#aiStart").value||!$("#aiEnd").value){const range=typeof reportDefaultRange==="function"?reportDefaultRange():{start:dates[0]||"",end:dates.at(-1)||""}; $("#aiStart").value=range.start||dates[0]||""; $("#aiEnd").value=range.end||dates.at(-1)||"";}};
  refreshVehicleContext(); window.addEventListener("vehicle-app-ready",refreshVehicleContext);
  form.onsubmit=async(event)=>{event.preventDefault(); const error=$("#aiFormError"),result=$("#aiResult"),progress=$("#aiProgress"); error.textContent=""; const vehicle=selectedVehicleObject(),vehicleId=vehicle?.id||"",start=$("#aiStart").value,end=$("#aiEnd").value; if(!vehicleId)return(error.textContent="Selecione um veículo na tela inicial antes de gerar a análise."); if(!start||!end)return(error.textContent="Informe o período completo."); if(start>end)return(error.textContent="A data inicial não pode ser posterior à data final."); if(!navigator.onLine)return(error.textContent="Não foi possível gerar a análise inteligente. Verifique sua conexão com a internet e tente novamente."); const indicators=buildAiIndicators(vehicleId,start,end,$("#aiType").value); if(!indicators.sample.movements)return(error.textContent="Não existem movimentos para o veículo selecionado no período informado."); if(typeof window.mycarAiAnalyze!=="function")return(error.textContent="O Firebase AI Logic ainda não foi carregado. Atualize a página e tente novamente."); progress.hidden=false;result.hidden=true;$("#generateAiAnalysis").disabled=true; try{const report=await window.mycarAiAnalyze(indicators);lastAiReport={...report,indicators};$("#aiConfidence").textContent=`Confiabilidade: ${lastAiReport.confidence||"Não informada"}`;$("#aiReportContent").innerHTML=renderAiReport(lastAiReport);result.hidden=false;}catch(requestError){console.error(requestError);error.textContent=requestError.message||"Falha de comunicação com o serviço de IA.";}finally{progress.hidden=true;$("#generateAiAnalysis").disabled=false;}};
  $("#closeAiResult").onclick=()=>{$("#aiResult").hidden=true;}; $("#viewAiReport").onclick=()=>openAiPrintableReport(); $("#newAiAnalysis").onclick=()=>{lastAiReport=null;$("#aiResult").hidden=true;refreshVehicleContext();};
}
initializeAiModule();

window.addEventListener("load", () => setTimeout(() => document.getElementById("splashScreen")?.classList.add("hidden"), 650));

if ("serviceWorker" in navigator) {
  window.addEventListener("load", () => {
    navigator.serviceWorker.register("./sw.js").catch((error) =>
      console.warn("Não foi possível registrar o service worker:", error),
    );
  });
}
load().then(() => {
  evaluateAlerts(true);
  const params = new URLSearchParams(window.location.search);
  const page = params.get("pagina");
  if (page && document.getElementById(page)) go(page);
  if (params.get("acao") === "novo") {
    go("movimentos");
    setTimeout(openEntryGroupChooser, 0);
  }
  if (page || params.has("acao"))
    history.replaceState(null, "", window.location.pathname + window.location.hash);
  window.vehicleAppReady = true;
  window.dispatchEvent(new Event("vehicle-app-ready"));
}).catch((error) => {
  console.error("Falha ao carregar o banco MyCarPlus.xlsx:", error);
  document.getElementById("splashScreen")?.classList.add("hidden");
  const host = document.querySelector("main") || document.body;
  const alert = document.createElement("section");
  alert.className = "card";
  alert.style.margin = "20px";
  alert.innerHTML = `
    <h2>Falha ao carregar o banco de dados</h2>
    <p>O aplicativo não conseguiu abrir <strong>data/MyCarPlus.xlsx</strong>.</p>
    <p>${String(error?.message || error)}</p>
  `;
  host.prepend(alert);
});
